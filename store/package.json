{
    "name": "@hiero-hackers/automation-store",
    "version": "0.0.0",
    "private": true,
    "description": "The owned operational store decided by protocol 6.5 (design/operations/storage-decision.md): four independent tables in one SQLite file. Ratification pending under the stage-four review. Uses node:sqlite — Node 22.5+.",
    "type": "module",
    "engines": {
        "node": ">=22.5"
    },
    "scripts": {
        "test": "tsc --noEmit && vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "typescript": "^5.6.0",
        "vitest": "^4.0.0"
    }
}
