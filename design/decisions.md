# Decision and Evidence Register

> This register separates principles that current evidence supports from hypotheses that still need
> maintainer review or sandbox experiments. A document does not become approved merely because it appears in
> this repository or in pull request 23.

## 1. Status meanings

The register uses the following status values.

| Status | Meaning |
|---|---|
| `supported` | The audits, accepted proposal, and current research are strong enough to guide the next work. |
| `hypothesis` | The idea is technically plausible, but maintainers or experiments must still confirm it. |
| `reopened` | New evidence has challenged an earlier proposal, so it must not guide implementation yet. |
| `ratified` | Maintainers have explicitly approved the decision and recorded the date and evidence. |
| `replaced` | A later decision has replaced the earlier proposal. |

## 2. Supported product principles

| Identifier | Principle | Evidence |
|---|---|---|
| P1 | The product is one hosted GitHub App with repository-selected capabilities. | The accepted proposal and `design/goals.md` support this direction. |
| P2 | A repository must explicitly enable every workflow-changing capability, and every user-facing capability defaults to off. | Maintainer consent and the different C++, Python, and JavaScript workflows require explicit opt-in. |
| P3 | A capability does not call or import another capability. | The coupling audit shows that direct and hidden sibling contracts are difficult to test and change. |
| P4 | A capability receives only its own configuration and a narrow platform interface. | The C++ shared-config coupling and GitHub API mock drift support a smaller boundary. |
| P5 | GitHub remains authoritative for visible repository facts. | Labels, assignees, reviews, comments, and open or closed state must continue to reflect human edits. |
| P6 | The design does not assume that GitHub alone stores every operational recovery record. | Webhook delivery, multi-call recovery, and coordination require feasibility experiments. |
| P7 | Repository labels and fields are mappings or profile defaults rather than universal platform strings. | The three audited SDKs use materially different workflow models. |
| P8 | Testing begins with a development App and personal sandbox before any Hiero repository receives writes. | The permission and failure risks require an isolated validation path. |
| P9 | A production webhook receiver durably accepts work before it acknowledges the delivery. | GitHub requires a quick response and does not automatically redeliver every failed delivery, so an acknowledged in-memory event cannot provide reliable recovery. Demonstrated in the sandbox (experiment 6.2, 2026-07-23): a crash after acknowledgement but before durable acceptance lost the delivery while GitHub's ledger recorded success; an ack-first relay in the transport path recreates the same loss window with no crash at all. |

## 3. Earlier design proposals and their current status

The identifiers below preserve references from the existing documents. Their current status is explicit so
that older text cannot be mistaken for approval.

| Identifier | Earlier proposal | Current status | Required next evidence |
|---|---|---|---|
| D1 | GitHub and App-authored comments provide all durable state, so the App needs no owned operational store. | `replaced` | Rejected by experiment 6.5 (2026-07-23): deduplication, pending-effect detection, and coordination each required owned state, and GitHub's delivery ledger records success for deliveries the receiver lost (6.2). Replaced by the storage decision (`design/findings/storage-decision.md`): a small single-file owned store, with GitHub keeping all effect outcomes. Ratification pending per §5. |
| D2 | Issues and pull requests use separate workflow models, and cross-entity writes require an explicit operation. | `hypothesis` | Candidate capabilities must show which cross-entity reads and writes they need. |
| D3 | Every repository uses one fixed twelve-label taxonomy. | `replaced` | P7 replaces this proposal with stable internal meanings and repository mappings or optional profiles. |
| D4 | The platform removes only named managed labels and never removes a namespace prefix. | `supported` | Adapter tests must enforce the rule for every label operation. |
| D5 | The full Hiero workflow profile has one invariant for each position. | `hypothesis` | Repositories that want the profile must review the invariants before activation. |
| D6 | Human edits and capability requests use different rulebooks. | `hypothesis` | Concurrency and manual-edit scenarios must confirm the behavior for each selected capability. |
| D7 | The App does not reverse a newer human change unless maintainers approve a named exception. | `supported` | Effect tests must prove the precondition and newer-change checks. |
| D8 | Incoherent mapped states use the five classes described in `design/guides/manual-edits.md`. | `replaced` | The five-class scheme no longer exists: `design/guides/manual-edits.md` §3 was rewritten to a deliberately smaller first policy — no automatic repair, no guessing, a conflict verdict that changes nothing — and `packages/core/src/workflow/project.ts` encodes it (D35–D37). Detailed conflict categories return only if a selected capability demonstrates the need. This row cited document text that had silently drifted away; caught 2026-07-25. |
| D9 | Timeline timestamps can enforce the newer-fact rule at acceptable cost. | `supported` | Cost confirmed by experiment 6.4 (2026-07-23): a busy issue's full timeline is one request (up to one hundred events, about half a second), conditional re-reads cost nothing, and a fifty-repository sweep fits the measured budget. Timestamp reliability for the newer-fact rule still rides with the D6/D8 manual-edit scenarios. |
| D10 | Clock-triggered destructive actions always warn before they act and provide a simple reversal. | `supported` | Each destructive capability must provide its own warning, grace, cancellation, and rollback tests. |
| D11 | Removing `blocked` resets an inactivity clock instead of resuming it. | `hypothesis` | Maintainers must decide this policy for a profile that enables inactivity handling. |
| D12 | Capabilities provide structured comment content, while the platform owns managed comment identity and writes. | `supported` | The managed-comment sandbox test must prove idempotent update and authorship checks. |
| D13 | Exactly two comment types may carry metadata that the platform reads back. | `supported` | Narrowed by experiment 6.5 (2026-07-23): metadata is read back as effect identity and receipt — the marker is what made retry-after-check safe — but it is not operational storage and records nothing before a write lands. See `design/findings/storage-decision.md`. |
| D14 | Resolved managed comments are shortened instead of deleted. | `hypothesis` | Maintainers must review the contributor experience for the first real comment type. |
| D15 | GitHub closing references are the only supported issue and pull request link mechanism. | `hypothesis` | The first capability that needs links must confirm the repository policy and fork behavior. |
| D16 | Skill credit is organization-wide while each repository chooses issue difficulty. | `reopened` | A skill ladder is optional, and repositories that request it must decide credit scope and completion rules. |
| D17 | Configuration exposes only choices on which reasonable repositories differ. | `supported` | Each key must state the repository choice that justifies it and the safe validation range. |
| D18 | One process and business-hours operation are sufficient because later sweeps repair missed work. | `supported` | Experiments 6.2 and 6.5 (2026-07-23): a single durable-first process plus the owned journal and a reconciliation sweep over the deliveries API (thirty-day retention) recovered every induced loss, including deliveries GitHub records as successfully delivered. Two conditions attach: detection requires the owned store (the ledger never flags a loss), and availability beyond business hours remains an operator decision under Q1/Q13. |
| D19 | A hosted App is preferable to copied repository workflows. | `supported` | The project still needs an operator and hosting decision before production deployment. |
| D20 | One adapter controls rate limits, retries, pacing, and conditional reads. | `supported` | Measured by experiment 6.4 (2026-07-23): uniform one-unit pricing, free 304 re-reads, and a content-creation secondary limit that arrives with no `retry-after` header near eighty writes per minute. One consequence is now explicit: the adapter must own retry and backoff itself — Octokit's default plugins absorb exactly these signals invisibly. |
| D21 | Every failure class has one primary audience and a clear recovery message. | `supported` | The first implementation must verify the channel and permission needed for each message. |
| D22 | Rollout uses a personal sandbox, a Hiero Hackers sandbox, shadow mode, reversible writes, and explicit kill switches. | `supported` | Owners and entry criteria must be recorded before each environment is used. |
| D23 | A capability declares its configuration, inputs, intents, permissions, triggers, and operational needs. | `hypothesis` | The first two candidate capabilities must fit the contract without receiving a raw GitHub client. Experiment 6.3 (`FINDING(config-capability-registry-gap)`) adds a requirement: the platform needs a capability registry checked at configuration load, or configurations enabling unknown capabilities pass validation silently. Experiment 6.5 adds another: each declared effect should name its idempotency class, since a lost-response retry duplicates comment creation but is harmless for label addition. |
| D24 | Current-state checks and comment records can recover every interrupted multi-call operation. | `replaced` | Rejected by experiment 6.5 (2026-07-23): current state cannot see pending work (an absent effect is indistinguishable from one never requested), and after a lost response a naive retry duplicated the effect on the first attempt. Replaced by the storage decision's model — the intent journal detects, a GitHub re-read resolves. Ratification pending per §5. |
| D25 | The threat controls in `design/guides/threat-model.md` are sufficient for the first release. | `hypothesis` | A red-team pass and sandbox evidence must confirm the controls before a real pilot. |
| D26 | The twelve GitHub label strings are fixed in code and cannot be configured. | `replaced` | P7 replaces this proposal. Code may keep stable meanings, while repositories configure mappings or select a profile. |
| D27 | Comment metadata is the App's write-ahead log and only durable operational state. | `replaced` | Rejected by experiment 6.5 (2026-07-23) on observed grounds: it records nothing before a write lands, covers only comment-shaped effects, and cannot coordinate — the two-worker race duplicated the effect even with the read-check this proposal relies on, because GitHub offers no conditional create. Each write-ahead write would also cost into the unsignaled secondary limit measured in 6.4. Replaced by the storage decision. Ratification pending per §5. |

### Hypotheses surfaced by the pure-logic implementation

Coding the design prose into the implementation packages (`core/`, `store/`, `executor/`) forced
ambiguities into concrete choices. Each choice is tagged `FINDING(...)` in the source at the exact place the assumption was made, and
the invariant tests in `packages/core/test/` exercise the chosen behavior. The code is evidence that the choice is
*coherent*, not that it is *right* — each row still needs the named review.

| Identifier | Choice the code had to make | Current status | Required next evidence |
|---|---|---|---|
| D28 | `blocked` is an orthogonal pause flag, not a workflow position: an item keeps its position while blocked, and unblocking restores it unchanged. `taxonomy.md` §2 lists `blocked` as a meaning, but neither state diagram contains it, and `safety.md` treats it as a pause. (`packages/core/src/workflow/state.ts`, `FINDING(taxonomy-blocked)`.) | `hypothesis` | Maintainers must confirm the flag model when reviewing the workflow profile, or the diagrams must gain explicit `blocked` positions. |
| D29 | Manual entry into any state is recorded as observed reality to reconcile, not as a requestable transition: the exhaustive transition matrix rejects module requests for edges the diagrams do not contain, while human edits may still land anywhere. The prose rule "every state has a non-module way in" implies edges the diagrams omit. (`packages/core/src/workflow/reference.ts`, `FINDING(taxonomy-manual-entry)`.) | `supported` | The manual-edit scenarios under D6/D8 must confirm reconciliation matches maintainer expectations for each profile state. |
| D30 | Grace periods have a floor of one day (`MIN_GRACE_DAYS = 1`): a destructive clock-triggered action cannot be configured to warn and act in the same instant. `safety.md` §4 requires "safe" bounds but names none. (`packages/core/src/safety/destructive.ts`, `FINDING(safety-grace-floor)`.) | `hypothesis` | Maintainers must ratify the floor value (or a different one) when the first destructive capability is reviewed under D10. |
| D31 | The no-configuration mode is named `observe`: with no reviewed configuration, the App reads and reports but performs no workflow-changing writes. `schema.md` §2.2 requires the behavior but does not name the mode; `observe` was chosen over `disabled` because the platform still watches and audits. (`packages/core/src/config/schema.ts`, `FINDING(config-no-config-mode)`.) | `hypothesis` | The configuration review under Q14 must confirm the mode name. That fail-closed validation always lands in `observe` is now demonstrated: experiment 6.3 (2026-07-23) drove every invalid fetched configuration — bad value, unknown key, wrong schema version — to zero configuration and `observe`, identical to the unit fixtures. |
| D32 | The stage-three feasibility experiments (build plan §6.1–§6.6) are capability-independent and may run while the stage-two capability-ranking gate is still open. None of the six protocols references a specific capability; the ranking is consumed by stage four's review, not by the experiments. The experiment protocols (local evidence records under `experiments/`, untracked by design) are the evidence that no step depends on the ranking. | `supported` | If any protocol step turns out to need a capability choice mid-run, this row is `reopened` and that step waits for the stage-two gate. |
| D33 | The rule-5 comparison lives in the core, and an exact-timestamp tie goes to the human: `evaluateWrite` refuses when the newest human change on the touched state is at or after the request's `causeObservedAt` (`>=`, not `>`), with the shell required to exclude the causing event itself when supplying `latestHumanChangeAt` — a human edit that both triggers the request and is the newest touch is the cause, not a conflict. GitHub timestamps have second granularity, so exact ties are plausible. (`packages/core/src/safety/write.ts`, `FINDING(safety-human-tie)`.) | `supported` | Maintainers must confirm the tie-break and the causing-event exclusion when the first capability's executor tests are reviewed under D7. |
| D34 | Label mappings are fully injective: `parseConfig` rejects any two meanings mapped to the same label string. `schema.md` §3 forbids "two incompatible meanings" sharing a label but never defines *incompatible*; full injectivity is the strictest defensible reading, and the observation projection (D35) depends on label→meaning being unambiguous. A same-entity-only rule (`ready` and `needsReview` sharing one "attention" label, disambiguated by entity) would be a deliberate relaxation. (`packages/core/src/config/sections.ts`, `FINDING(config-label-injectivity)`.) | `hypothesis` | The configuration review under Q14 must define "incompatible" — confirm full injectivity or ratify the same-entity relaxation. |
| D35 | The observation projection's three readings, encoded together in `packages/core/src/workflow/project.ts`: a position meaning of the other entity's flow observed on an item is left alone and reported as ignored, never a conflict (`FINDING(observe-cross-entity)` — `manual-edits.md` §3 rule 1's "unrelated labels" read to cover the other flow); `blocked` with no position is a legal observation projecting to "no position, paused" (`FINDING(observe-blocked-alone)` — D28's orthogonal flag applied at the observation boundary); and a closed item keeps whatever position labels it carries, unrepaired, with the state machine's closed guard refusing transitions regardless (`FINDING(observe-closed-position)` — silence on closure per `manual-edits.md` §6). | `hypothesis` | The D6/D8 manual-edit scenario review confirms all three readings together; the blocked reading ratifies or falls with D28. |
| D36 | Folded into D35 (2026-07-25): `blocked` with no position label. | `replaced` | See D35. |
| D37 | Folded into D35 (2026-07-25): closed items keep their position labels. | `replaced` | See D35. |
| D38 | Fail-closed validation is whole-file: one error anywhere yields no configuration, dropping the repository to `observe` and stopping every working capability until the file is fixed. Last-known-good was rejected because an older automation decision must not outlive a newer human edit (a maintainer's attempt to disable a behavior would fail while the behavior keeps running); partial salvage was rejected because an uninterpretable fragment can change the meaning of valid-looking fragments (the reason §2.7 rejects unknown keys) and a half-running state is the hardest to debug. The loud full stop is acceptable maintainer experience only with two shell-owned mitigations: the configuration report (`projections.md` §3) within seconds of the breaking edit, and PR-time validation as a check run on any PR touching the file, so invalid configuration normally never reaches the default branch. (`packages/core/src/config/parse.ts`, `FINDING(config-fail-closed-granularity)`.) | `hypothesis` | Maintainers must confirm the full-stop-with-report experience during the ring-one pilot (Q8), and the PR-time validation check must exist before any repository runs `active`. |
| D39 | An active kill switch refuses even pure observations: the kill-switch check precedes the observation short-circuit in `evaluateWrite`, so "stop" means stop reading-and-recording, not merely stop writing. `safety.md` §5 does not say which reading wins; the total stop is encoded because a kill switch is an operator's emergency brake, and residual activity after pulling it costs trust exactly when trust is lowest. The rest of the check order carries no outcome semantics — it only decides which machine-readable `code` is reported when several rules fail at once, and the verdict-code tests freeze that reporting order as contract. (`packages/core/src/safety/write.ts`, `FINDING(safety-killswitch-observations)`.) | `hypothesis` | The kill-switch design under D22 must confirm the total-stop semantics — in particular whether operator alerts and security controls (which `manual-edits.md` §5 exempts from an item-level block) are also exempt from a kill switch, which this code does not model. |
| D40 | Body-regex failure classification is snapshot evidence, not contract: the rules in `packages/core/src/github/failures.ts` match prose GitHub sent on 2026-07-23, and GitHub may reword it at any time. The mitigation is structural plus operational: an unrecognized 403 degrades into the explicit `forbiddenUnrecognized` class carrying the body verbatim (never a fabricated diagnosis), and a periodic sandbox re-probe must re-validate the observed fixtures — a rise in `forbiddenUnrecognized` operator reports is the signal that the catalogue needs a new observation run. (`packages/core/src/github/failures.ts`, `FINDING(failures-prose-snapshot)`.) (`FINDING(secondary-limit-no-wait-signal)` — the 403 secondary limit carries no `retry-after` header, so the wait is inferred from body prose and a documented one-minute floor.) | `supported` | The operations review under Q1/Q13 must schedule the re-probe cadence and name the owner of fixture refresh. |
| D41 | Effect claims are leases, not permanent locks: `claim` atomically takes over a claim whose timestamp is at or before the caller's stale threshold. The earlier claim that journal plus read-back makes live takeover safe was falsified by the 2026-07-28 risk review: a still-live worker can resume from a pre-takeover snapshot or have a non-idempotent request in flight while its successor sees absence and resends. SQLite ownership checks cannot cancel a remote GitHub request. (`packages/store/src/store.ts`; `packages/executor/src/recovery.ts`; `FINDING(store-claim-lease)`.) | `reopened` | Stage four must choose and test an explicit overlap contract: renewal and adapter deadlines shorter than the lease, takeover delayed beyond the maximum in-flight lifetime, and an operator stop rather than resend when absence cannot be established safely. A deterministic overlapping-worker oracle is required. |
| D42 | The effect journal carries a durable per-call `attempt` counter, and a `done` row is immutable to `intent`: re-declaring a still-open call increments the counter in the same single-statement upsert, while acknowledged history can never regress to `sent` (the original `INSERT OR REPLACE` could silently un-happen a receipt, sending recovery to re-resolve completed work). This amends the ratification-pending four-table schema by one column, implementing the storage decision's own "retries with bounded history" cell — which the decided schema claimed but did not support — and making retry bounds (core's `MAX_RATE_LIMIT_ATTEMPTS`) survive restart instead of resetting with the process. A count, not per-attempt rows: forensic history was not demanded by any 6.5 recovery. Pre-ratification store files are not migrated. (`packages/store/src/store.ts`, `FINDING(store-journal-attempts)`.) | `supported` | The stage-four storage ratification reviews the amended `effect_journal` schema alongside the original four-table decision. |
| D43 | The reconciliation sweep the design leans on (D18) has a concrete store API: `schedule` gains a `claimed_at` column stamped by `claimDue`, `requeueStuck(claimedBefore)` atomically returns stuck `running` rows to `pending` (stuckness judged by claim age, never due time, so a backlog catch-up is not stolen from; requeued work re-fires through the normal `claimDue` path — no parallel firing mechanism), and `openIntents(before)` lists every unresolved `sent` journal row as the recovery loop's worklist. A slow-but-alive schedule handler can be requeued and fire twice — accepted on D41's grounds: the journal plus GitHub re-read is the duplicate-prevention layer. The requeue threshold is the sweep's operations decision, as is retention: `seen_delivery`, `done` journal rows, and released-claim leftovers grow without bound until the sweep design names pruning windows (the deliveries API's own retention is thirty days; the store's must be decided, not defaulted). (`packages/store/src/store.ts`, `FINDING(store-sweep-api)`.) | `supported` | The stage-four storage ratification reviews the amended `schedule` schema, and the sweep design under D18 sets the requeue threshold, retention windows, and D41's lease duration together. |
| D44 | A call re-sent `MAX_CALL_ATTEMPTS = 5` times without a confirmed outcome stops retrying and surfaces to the operator as unresolved. The storage decision requires "retries with bounded history" but names no bound — the same unnamed-limit pattern as the grace-period floor (D30). The bound reads the journal's durable attempt counter (D42), so it survives restarts; the crash grid demonstrates convergence well inside it. (`packages/executor/src/recovery.ts`, `FINDING(executor-attempt-bound)`.) | `supported` | The stage-four storage ratification confirms the bound value alongside D42's counter. |
| D45 | An open journal row that no longer matches the current plan (seq beyond the plan, or a different intent at that seq) surfaces as unresolved and is never remapped: `manual-edits.md` §9 rules that intents created under an old configuration revision are invalid, so the executor stops rather than guess a correspondence between old and new plans. (`packages/executor/src/recovery.ts`, `FINDING(executor-stale-plan)`.) | `supported` | The configuration-revision design under Q14 must define how surfaced stale effects are closed out (operator action, automatic abandon after review, or re-issue under the new revision). |
| D46 | The recovery loop's exactly-once guarantee is proven only relative to a CONSISTENT read-back: the crash grid's fake world answers `readBack` with perfect read-after-write consistency, but real GitHub reads can lag writes, and a stale "absent" immediately after a landed create would make the loop duplicate despite following every rule. The loop is correct given the precondition; the precondition is not free. The real port owes a confirmed-fresh read (or a bounded delay and re-read) before answering "absent". (`packages/executor/src/recovery.ts`, `FINDING(executor-readback-consistency)`; the harness declares where its world is kinder than GitHub.) | `supported` | Measured by protocol 6.7 (2026-07-25, `design/findings/read-after-write.md`): 40/40 trials — comment→list and label→read — visible on the FIRST immediate read; no staleness observed. The freshness rule is encoded (`packages/executor/src/policy.ts`): "present" on first sight, "absent" only after two reads ≥1 s apart (~2× observed p95), because a wrong "absent" duplicates a non-idempotent call. Remaining for stage four: the port implements the rule; re-measure if the resolver ever uses GraphQL or search reads (unmeasured; search indexing is known to lag). |
| D47 | Closure is recorded as a REASON on the work-item state, not as a position: `closedBy` is `merged`, `closedByHuman`, or `completedByLinkedMerge`, `null` while open, read from GitHub's own fields (`merged_at`, `state_reason`) and never written as a label. `taxonomy.md` §5 wrote "The pull request closes or merges" as one edge, discarding the distinction at exactly the point downstream policy needs it: progression credits a merged linked pull request and not an abandoned one (`design/guides/capabilities/progression.md`), and the audited C++ post-merge cleanup is gated `merged == true` (`design/audit/services-cpp.md`). Modelled orthogonally for the same reason as `blocked` (D28) — as a meaning it would become mappable, and a merged pull request still carrying `status: needs review` would project as a two-position conflict, silencing observation on the items most worth reporting. (`packages/core/src/workflow/state.ts`, `packages/core/src/workflow/project.ts`, `FINDING(taxonomy-closure-reason)`.) | `hypothesis` | The D6/D8 manual-edit scenario review confirms the reason set; the first capability that consumes closure (progression or post-merge cleanup) proves the three values are sufficient and that no fourth is needed for an automation-initiated close. |
| D48 | Three corrections to the pull-request flow, all found by reading the tables against the audit rather than against the prose. (1) `readyToMerge → needsRevision` was MISSING: an approved pull request whose checks broke had no path out except one asserting new commits, yet the audited Sibling Conflict Re-check re-reads every open pull request's `mergeable` state when a *different* pull request merges and swaps its status label (`design/audit/services-cpp.md`) — checks break with no push. (2) `needsReview → needsRevision` accepted only `checksFailed`, though §5 labels that edge "New evidence requires contributor action" and the audited PR Review Label Applicator performs it on a `changes_requested` review; `reviewRequestedChanges` is added, and observing it requires the `pull_request_review` subscription experiment 6.6 found absent. (3) `newCommitsInvalidatedApproval` is renamed `approvalInvalidated` — the old name bundled a trigger with its consequence and could not express a dismissed review or a changed base. The first implementation's narrowing in (2) was made silently, with no `FINDING` tag, which is why it never reached this register. (`packages/core/src/workflow/transitions.ts`, `FINDING(taxonomy-approved-checks-broke)`, `FINDING(taxonomy-review-cause)`, `FINDING(taxonomy-approval-cause)`.) | `hypothesis` | Maintainer confirmation that the audited C++ behaviors belong in the profile, and the `pull_request_review` subscription decision under the permission ceiling. All three assume `readyToMerge` is a STORED position; if the open §10 question resolves to "derived", these edges collapse into that derivation and the corrections become moot rather than wrong. |
| D49 | Reopening is a closure CLEAR, not a transition, and it lives outside the edge tables: closing never removes the position labels (D35), so a reopened item returns exactly where it was and no position moves — the same shape as `blocked` (D28), and the reason the first implementation's "except reopen (not modelled yet)" comment could not be resolved inside the tables. One invariant falls out of D47's reason: a **merged pull request can never reopen**, which GitHub enforces and `applyReopen` refuses as `mergedNotReopenable` rather than omitting. Reopening an already-open item is refused (`notClosed`) rather than absorbed as a silent no-op. (`packages/core/src/workflow/reference.ts`, `FINDING(taxonomy-reopen)`.) | `hypothesis` | The D6/D8 manual-edit scenario review confirms that a reopened item should resume its old position rather than re-enter triage — the alternative reading, and the one a repository whose triage is the entry gate might prefer. |
| D50 | Two mechanical hardenings, neither carrying taste. (1) Transition causes are scoped per entity (`IssueCause`, `PrCause`): the first implementation shared one flat union, so `triageCompleted` on a pull request type-checked and was refused only at runtime — the same "make misuse unrepresentable" rule `ids.ts` applies to delivery ids, and no cause is legal in both flows except `humanClosed`. (2) The doc-drift detector `core/README.md` promised now exists: `packages/dev/checks/test/doc-drift.test.ts` parses the state diagrams out of `taxonomy.md` and asserts they match the tables edge for edge, in both directions. It compares (from, to) pairs only — arrow prose stays human-written and causes stay covered by the exhaustive matrix — so a cause added without a doc edit still slips through, but a whole edge no longer can. This is the gap that let D48's missing edge sit in both artifacts undetected, and the same class of gap as the D8 drift caught on 2026-07-25. (`FINDING(taxonomy-entity-scoped-causes)`.) | `supported` | None required for the mechanism itself. The remaining drift surface is named above: causes, and the register's own prose. Generating the diagrams from the tables outright would close it and stays the cheaper long-term option. |
| D51 | Ordering evidence for rule 5 is three-valued, not two: `latestHumanChangeAt` is a `Date`, `null` ("the shell checked and found none"), or `"unknown"` ("ordering could not be established"), and `"unknown"` refuses with `humanOrderingUnknown`. The first implementation collapsed the last two into `null`, so unavailable evidence read as "no conflict" and the write **applied** — the exact opposite of `manual-edits.md` §2 ("if reliable ordering evidence is unavailable, the safe default is to return a conflict and do nothing"). Not hypothetical: D9 records that timestamp reliability for the newer-fact rule is still open, so unknown ordering is an expected state. (`packages/core/src/safety/write.ts`, `FINDING(safety-ordering-unknown)`.) | `supported` | The shell must distinguish the two when it computes the value; a shell that reports `null` for a failed lookup silently restores the old behaviour, so this is an executor-boundary test under D7 rather than a maintainer question. |
| D52 | The clock-triggered destructive gates are inescapable, and the kill switch is reported first on that path too. `evaluateWrite` refuses `clockTriggeredDestructive` outright (`wrongEntryPoint`); the general rules moved to an internal function that both entry points share. Found by audit, and it was the module's headline claim: `evaluateWrite` previously answered `apply` for a destructive request in an otherwise-permissive active context, so "a destructive action cannot fire without a recorded warning and an elapsed grace period" was enforced by which function the caller happened to call. Separately, the kill switch was reached only via the general rules at the END of `evaluateDestructive`, so an operator who had pulled the emergency brake was told `noWarning` — a refusal either way, but D39 freezes the verdict codes as contract and claims kill-switch-first. (`packages/core/src/safety/destructive.ts`, `FINDING(safety-destructive-entry-point)`, `FINDING(safety-killswitch-order)`.) | `supported` | None for the mechanism. The exhaustive sweep now crosses action class, capability-link state, and the remaining context dimensions (5,120 combinations), asserting `apply` per combination rather than only checking an aggregate count; a destructive request can never apply from the general path. |
| D53 | `WriteContext` names the capability its enablement flag describes, and a mismatch against `request.capability` refuses (`capabilityMismatch`) before any policy check — a malformed pair is not a policy question. The request had always named a capability that no code read, while the context asserted a bare `capabilityEnabled` boolean about "the" capability, with nothing tying them together: a shell could answer the enablement question about a different capability entirely and rule 1 would pass. This converts an unnoticed attestation into a checked fact at no cost. (`packages/core/src/safety/write.ts`, `FINDING(safety-capability-link)`.) | `replaced` | None. Reduces, and does not remove, the attestation debt named in `core/README.md`: `preconditionHolds` remains capability-specific and unverifiable in core. | **Replaced 2026-08-05 by D73's derivation:** `evaluateWrite` now takes the request and the reviewed configuration and reads the capability from `request.capability`, so a context describing a different capability cannot be constructed. The `capabilityMismatch` code, its check, and its tests are deleted — the state is unrepresentable rather than caught.
| D54 | The `immediatePreventive` action class has no dedicated explanation/reversal gate, so `evaluateWrite` refuses it with `preventiveGateUnavailable`. The class remains in the design because the approved-moderation issue-lock use case is real, but an unimplemented safety contract cannot inherit the weaker reversible-change path. (`packages/core/src/safety/destructive.ts`.) | `supported` | The first capability that requests an immediate preventive action must specify and test what "explains immediately" and "simple maintainer reversal" require before the refusal can be replaced by an applying gate. |
| D55 | Label-mapping injectivity is judged case-insensitively and ignoring surrounding space, because that is how GitHub judges label-name uniqueness. Exact-string comparison accepted `status: ready` and `Status: Ready` as mappings for two different meanings — one label on GitHub, therefore one label carrying two meanings, which is precisely the ambiguity D34 exists to prevent and which the observation projection depends on being impossible. The configured spelling is preserved for writes; only the uniqueness key is folded. The old code deferred case to "the shell's concern", but the shell does not exist and D34's recommendation to reviewers claims full injectivity, so the implementation was weaker than the register said. (`packages/core/src/config/sections.ts`, `FINDING(config-label-case)`.) | `supported` | Rides with D34's review: confirming full injectivity now also confirms the folding rule. If reviewers ratify the same-entity relaxation instead, the folding rule survives unchanged — it constrains sameness, not scope. |
| D56 | An absent `mode` defaults to `observe`; a present but empty one is a validation error. `raw.mode ?? "observe"` silently accepted `mode:` with no value — YAML parses that to null — and chose a mode on the maintainer's behalf. The mode it chose was the safe one, which is why this is minor, but silently interpreting malformed input is the pattern §2.7 and D38 reject everywhere else in the same file. (`packages/core/src/config/sections.ts`, `FINDING(config-null-mode)`.) | `supported` | None. Consistent with D38: the maintainer sees an error and a configuration report rather than a mode they did not write. |
| D57 | An intent may require any permission its capability declares, repository- or organization-scoped. The check compared only against `permissions.repository`, so a grant legitimately declared under `organization` produced "which the capability does not declare" — a false failure that would have blocked any org-scoped capability, and `progression` needs organization-wide data by its own module document. (`packages/core/src/capability/declaration.ts`, `FINDING(contract-intent-org-permissions)`.) | `supported` | None; this was a defect against `contract.md` §1 as written, not a policy choice. The permission *ceiling* remains stage-four policy and is unaffected. |
| D58 | The retirement tombstone is enforced in code, not just documented: `get` is the sole declaration lookup and returns `undefined` for retired capabilities, while `describe` exposes only report-safe name and retirement metadata. `activeNames` existed but nothing obliged a caller to consult it, and the old `get` handed back a retired declaration ready to run — so "a retired capability never activates" was a rule a caller had to remember. The reporting path can no longer yield an activatable declaration. (`packages/core/src/capability/declaration.ts`, `FINDING(contract-retired-enforcement)`.) | `supported` | The stage-five effective-configuration report uses `describe` so it can say "enabled, but retired"; activation uses the only declaration lookup, `get`. |
| D59 | A `conflict` projection carries `blocked`, `closedBy`, and ignored cross-entity meanings alongside the conflicting positions. It previously lost context exactly when reporting needed it most. No write behavior changes: a conflict still produces no `WorkItemState` and cannot reach a write. (`packages/core/src/workflow/project.ts`, `FINDING(observe-conflict-context)`.) | `supported` | None. Feeds the configuration and operator reports the D38 mitigations require. |
| D60 | A destructive warning is authority for exactly one request snapshot: action class, capability, dated cause, target item, and change. `createDestructiveWarning` copies those values into frozen primitive data, including numeric timestamps, so neither a shared target object nor a mutable `Date` can change the warning after issuance. It also records the earliest action time, cancellation action, and reversal. `evaluateDestructive` rejects cross-request reuse, warnings recorded before their observation, and earliest-action timestamps shorter than the full grace period. Without this binding, an old warning for one item authorized a new destructive request for another. (`packages/core/src/safety/destructive.ts`, `FINDING(safety-warning-binding)`.) | `supported` | None for the mechanism. The durable warning store and executor must reconstruct warnings through the factory and preserve the primitive snapshot without weakening the comparison. |

| D61 | The capability vocabularies are CLOSED platform catalogues, not per-capability extensions: `packages/core/src/capability/catalogue.ts` enumerates every observation, resolver, and intent operation, and a capability chooses from them. This answers contract.md §9's first open question ("select the first concrete observation, resolver, and intent types") and it was not a preference — per-capability intent shapes are unimplementable at the far end, because the adapter would need an executor for a type it has never seen. P3 falls out as a consequence rather than a rule to police: capabilities that share no vocabulary have nothing to call each other through. (`packages/core/src/capability/catalogue.ts`.) | `hypothesis` | Stage four confirms the catalogue is the right closure point, and the first ratified capability proves the initial vocabulary is sufficient. A capability that needs an operation outside the catalogue extends the catalogue by review, which is the intended cost. |
| D62 | The platform owns an operation's idempotency class; the capability's declaration is a redundant statement that must MATCH. `capability/declaration.ts` accepted any class on any intent name, so a capability could declare `postManagedComment` as `idempotent` — and the executor trusts that class to pick its retry rule, so the declaration would route a lost response down the blind-retry path and reproduce experiment 6.5's demonstrated comment duplication, the exact failure the class exists to prevent. A per-capability field cannot be authoritative about a per-endpoint fact. `checkAgainstCatalogue` rejects the mismatch at registry build; `idempotencyOf` is what the planner reads. (`packages/core/src/capability/catalogue.ts`, `FINDING(runtime-idempotency-declared-not-checked)`.) | `supported` | None for the mechanism — the failure mode is demonstrated and the check is tested. Stage four folds `checkAgainstCatalogue` into the platform's registry build so no shipped declaration can skip it. |
| D63 | `ActionClass` enters through the intent, with a per-operation FLOOR. safety.md's action classes had no supplier anywhere in the capability contract, so `evaluateWrite` — which requires one — could not be fed from an intent at all: the safety engine and the capability contract were written against each other and never met. It cannot be purely per-operation either, because `unassign` is a `reversibleStateChange` when a human asks and a `clockTriggeredDestructive` when a clock does, and only the capability knows which. The catalogue therefore names a minimum and the intent may declare stricter; laxer is refused. A capability may be more careful than the platform requires and never less. (`packages/core/src/capability/catalogue.ts`, `FINDING(runtime-action-class-floor)`.) | `reopened` | Reopened 2026-09-01: the intent-side channel no longer exists — `Intent` carries no action class and `decide()` reads only the catalogue's `actionClassFloor` (`packages/core/src/engine/decide.ts`), so "the intent may declare stricter" describes removed code, and the `unassign` duality this row names is unrepresented: a clock-triggered unassign runs under the reversible floor while `safety/destructive.ts` has no production caller. Stage four must either restore a stricter-only declaration channel or give clock-triggered unassignment its own catalogue operation with a destructive floor. |
| D64 | A destructive intent carries its own warning record (`DestructiveDetail`), and a non-destructive one carrying a warning is an ERROR. `evaluateDestructive` refuses without a warning, but contract.md §3's intent had no field for one, so a destructive intent could never have passed the gate it was designed for. The warning rides on the intent rather than being fetched by the planner because the capability is what decides an item is stale, so it is what must show its warning — a planner that looked the record up itself could pair a warning with an intent that was never about it. The reverse check matters more: a warning attached to a non-destructive intent reads as a grace period that no gate will ever consult, because `evaluateWrite` does not look at the field. (`packages/core/src/capability/intent.ts`, `FINDING(runtime-destructive-intent-has-no-warning)`.) | `supported` | None for the mechanism. The first destructive capability confirms the four fields are sufficient; it is deferred well past the November milestone by the build plan. |
| D65 | `idempotencyKey` has exactly one derivation, in `deriveIdempotencyKey`: capability, repository, item, operation, and the CAUSE with its timestamp — deliberately not the desired payload, so a capability that recomputes a slightly different comment body for the same occasion does not create a second comment. contract.md §3 required the field and never said what produced it, while the store's primary key silently depended on the answer: two intents that collide are one already-done effect and the second is never performed, and the same intent keyed differently on redelivery duplicates. Batch-level collisions are refused in the planner, which is the only place a whole batch is visible — the store cannot see them, because from its side a collision is indistinguishable from a legitimate redelivery. (`packages/core/src/capability/intent.ts`, `packages/executor/src/planner.ts`, `FINDING(runtime-idempotency-key-underived)`, `FINDING(planner-key-collision)`.) | `hypothesis` | Stage four confirms the derivation, in particular that the cause timestamp is the right occasion boundary. A capability whose cause is not event-shaped (a sweep over items with no per-item event) would need a different occasion key, and `inactivity` is the probe that will test it first. |
| D66 | One intent produces one plan. The executor claims per `effectId`, so grouping is load-bearing and contract.md never chose. Separate plans, because a shared one couples unrelated outcomes three ways: a crash mid-plan blocks siblings behind a claim they have no relationship to; D45's revision guard invalidates the whole plan on a configuration change, discarding intents that were still valid; and the journal's contiguous `seq` imposes an order the capability never asked for. Grouping can be added later if a capability needs atomic ordering; ungrouping a shared plan after the fact cannot. (`packages/executor/src/planner.ts`.) | `hypothesis` | A capability that genuinely needs two effects to land together, or not at all, would reopen this. None of the candidate capabilities does. |
| D67 | The repository mode is authoritative from the REVIEWED configuration, and a planner whose rechecked `WriteContext.mode` disagrees refuses (`modeMismatch`) rather than picking a winner. The mode existed in two places — `RepositoryConfig.mode` and the `WriteContext` that `evaluateWrite` actually reads — with nothing requiring agreement, so a shell that assembled the context from anywhere else would write in `active` on a repository whose reviewed file said `dry-run`, and every test would still pass because each half is self-consistent. This was not reasoned out in advance: the first composition test set the configuration to `dry-run`, received a plan, and exposed it. Goal 2 makes the configured value authoritative, so a disagreement is a shell defect and must surface; silently overriding the context would repair the symptom and leave the defect unobserved. (`packages/executor/src/planner.ts`, `FINDING(planner-mode-duplicated)`.) | `replaced` | None for the guard. Stage five's shell must source the context's mode from `parseConfig` output and nowhere else; this row is the reason that is a requirement rather than a convention. | **Replaced 2026-08-05 by D73's derivation:** the planner no longer carries a mode to compare, because the safety engine reads mode from the configuration directly. `modeMismatch` and its test are deleted for the same reason as D53's.
| D68 | Dry-run and observe stop at PLANNING, before a plan exists — not at the port. An unapplied plan would still be journalled, and the recovery loop would later find an open intent it is obliged to resolve against GitHub for a write that was never meant to happen: manufactured operator work out of a mode whose whole promise is that nothing happens. Safety rule 10 is therefore a planning decision, not an execution one. (`packages/executor/src/planner.ts`.) | `supported` | None. The behaviour is tested end to end against the real store — a dry-run batch leaves `openIntents` empty. |
| D69 | The single declared idempotency class per intent holds only while every catalogue operation is SINGLE-CALL. `IntentDeclaration` carries one class; `PlannedCall` carries one per call; and contract.md §5 permits multi-call plans, which mix them (create-comment then add-label is non-idempotent then idempotent). Every operation is one call today, so the constraint is recorded rather than repaired. The first multi-call operation must move the class from the intent declaration onto the call, or plans will retry under the wrong rule. (`packages/executor/src/planner.ts`, `FINDING(planner-per-call-idempotency)`.) | `hypothesis` | Whichever capability stage four ratifies: if any of its effects needs more than one call, this row must be resolved before the effect is built. |
| D70 | **P3 is now tested in code.** An earlier `design/build-plan.md` revision recorded P3 as "a supported principle on paper and an untested claim in code" and deferred the toggle matrix past November on the grounds that one capability cannot violate it. That is right about the arithmetic and wrong about the prerequisite: P3 is a STRUCTURAL property, so three disposable stubs test it exactly as well as three shipped capabilities would. The run exists (`packages/probes/test/engine-matrix.test.ts`): all eight subsets of three deliberately dissimilar capabilities, asserting that each one's intents, explanations, and resolver calls are identical regardless of which neighbours are enabled, that a disabled capability is never evaluated, and — as a negative control against a vacuous pass — that each capability alone still produces work. The isolation is structural rather than asserted: a capability is reachable only through its own projected view. | `hypothesis` | The result transfers to shipped capabilities only insofar as they use the same boundary, which is the point of putting the boundary in `core`. Stage four should confirm that this discharges §12's post-November commitment; the matrix should be re-run with the first real capability substituted for its probe. |
| D71 | A capability sees which meanings are MAPPED, never what the repository calls them. contract.md §6 says a capability "refers to internal meanings rather than repository label strings", and passing `mappings.labels` into the capability view would have satisfied every type while quietly breaking that rule. `CapabilityView` therefore reports availability only, and omits three further things a capability must not decide: the repository mode (rule 10's job), its own enabled flag (it is never evaluated while off), and any other capability's block. A capability that never sees a label string cannot hard-code one. (`packages/core/src/capability/boundary.ts`.) | `supported` | None for the projection. Whether `mappedMeanings` is enough for a capability that wants to explain itself in the repository's own words is a stage-four question — the managed comment is rendered by the platform, which does have the mapping. |
| D72 | A `DestructiveWarning` is rebuilt at act time from the STORED warned cause, never from the current request. D60 makes the warning a branded object only `createDestructiveWarning` can construct — a real within-process guarantee, and one that cannot survive the journey a warning actually makes: it is issued in one process run and acted on days later across restarts, so it must be persisted as plain data and rebuilt, and the brand is a runtime symbol that does not serialize. Rebuilding it from the current request satisfies every type and makes `warningMatchesRequest` compare a value with itself — a safety check that can never fail, which is worse than none. `DestructiveDetail` therefore carries the warned cause and its observation time, and the planner rebuilds from those. The seam narrows D60's comparison to the causal fields: item and change derive from the same intent at both ends and cannot diverge, while the cause is the one part a capability can get wrong, and getting it wrong is exactly the reuse D60 forbids. Found by merging this branch onto D60 rather than by either change alone. (`packages/core/src/capability/intent.ts`, `packages/executor/src/planner.ts`, `FINDING(runtime-warning-cannot-cross-the-store)`.) | `hypothesis` | Stage four confirms that plain-data persistence plus rebuild is the intended reading of D60 rather than a weakening of it, and stage five's store design decides which columns hold the warned snapshot. |
| D73 | `WriteContext.capabilityEnabled` DUPLICATES `CapabilityConfig.enabled` with nothing comparing them, and is the last unguarded member of a pattern the register has now hit four times: a fact stored in two layers, free to disagree, closed by adding a comparison. D53 added `capabilityMismatch` for the capability NAME and D62 added a catalogue check for the idempotency CLASS, but consent itself — the boolean that decides whether a repository agreed to this capability at all — is still a bare attestation. A shell can pass `capabilityEnabled: true` for a capability the reviewed configuration disables; D53's name check passes, because the names match, and goal 2's "no configuration means no workflow-changing writes" is defeated by a field nobody compares. Found by reading the four duplicates together rather than by any one of them failing. The recommended repair is NOT a fifth guard: `WriteContext` should be DERIVED from the parsed configuration and the declaration the caller already holds, which retires D53's and D67's guards as well and makes this state unrepresentable rather than merely checked. (`packages/core/src/safety/write.ts`, `packages/core/src/config/schema.ts`.) | `hypothesis` | Stage four decides between the derivation and a fifth comparison. The derivation is the recommendation and is proposed as its own packet item; until it lands, the shell must source `capabilityEnabled` from `parseConfig` output and nowhere else — the same standing requirement D67 places on `mode`. |
| D74 | One internal representation for an instant: epoch MILLISECONDS, converted only at the two real edges — GitHub in, SQLite out. A single logical moment currently crosses three encodings between capability and journal: `Date` through most of core (`WriteRequest.causeObservedAt`, `DatedCause.observedAt`, every observation payload), `number` inside the destructive warning (`warnedAtMs`, `earliestActionAtMs` — D60 chose primitives to avoid aliasing a mutable `Date`), and canonical ISO strings in the store, which validates the exact `Date.toISOString()` shape defensively because it cannot trust its caller. The executor takes `now: () => string` while the planner beside it takes `now: Date`. Each choice was locally right — `Date` is ergonomic, milliseconds are immutable, ISO sorts lexicographically in SQLite — and the aggregate is a conversion at every boundary plus a validator that exists only because the boundaries disagree. Milliseconds are the honest internal form: immutable like D60 requires, comparable without parsing, and needing no format check. ISO stays where it earns its keep, in the store's sort order. | `hypothesis` | Stage four accepts or rejects the single representation. If accepted, the store's timestamp validator is deleted rather than ported, and D60's aliasing rationale is satisfied by the type rather than by convention. Cost is honest: most test fixtures construct both forms today. |
| D75 | One result convention, and machine-readable codes on configuration errors. Core currently discriminates failure three ways — `ok` in `config/`, `capability/declaration.ts` and `capability/intent.ts`; `outcome` in `safety/` and the executor; `kind` in `workflow/project.ts` and the planner — and carries two incompatible error payloads: bare `readonly string[]` from `parseConfig` and `validateDeclaration`, versus `code` plus `reason` everywhere else. The discriminant spread is ordinary inconsistency; the payload split has a concrete consequence. D38's fail-closed granularity is acceptable only because of two named mitigations — the effective-configuration report and PR-time validation as a check run — and both are downstream consumers that can only ECHO prose, because configuration errors carry no code to group, count, link, or localise by, while every refusal elsewhere in core does. The mitigation that makes D38 humane is weaker than it needs to be, for a reason that is purely a type choice. Landed 2026-08-05 as `ConfigError { code, message, path }` — eleven codes over fifteen error sites, each carrying the dotted path a check run needs to annotate a line (`FINDING(config-error-codes)`, `packages/core/src/config/schema.ts`, `packages/core/src/config/sections.ts`). The discriminant half of this row — collapsing `ok`/`outcome`/`kind` into one word — was deliberately NOT done: it churns every consumer for a consistency nobody is blocked on, where the payload split had a blocked consumer in D38's configuration report. | `hypothesis` | Stage four picks one discriminant and rules that every failure carries a `code`. The prose stays prose — `safety.test.ts`'s convention of asserting `reason.length > 0` and never the wording is the right one and should extend rather than change. Mostly mechanical; no behaviour moves. |
| D76 | Every exported enumeration is declared ONCE and its union derived from it — `export type X = (typeof XS)[number]` — and a set keyed by a union is written as a mapped type, so adding a member breaks compilation until every consumer is updated. `REPOSITORY_MODES` lived in the configuration module while a hand-written `RepositoryMode` union lived in the safety module: the same four strings in two files with nothing linking them, and a `value as RepositoryMode` cast in the parser quietly covering the seam. Adding a mode to the array alone would have let `parseConfig` accept a value the safety engine's type had never heard of. `PROFILE_EDGES` had the same shape more mildly, listing `issue` and `pullRequest` literally instead of mapping over `EntityKind`. Fixing the first also removed the type-only import cycle D73 introduced, because a repository mode belongs to configuration and nothing in `config/schema.ts` now imports from `safety/write.ts`. This is the FIFTH sighting of one fact stored in two places — after D53, D62, D67 and D73 — so the repair is not another comparison but a test: `packages/dev/checks/test/enumerations.test.ts` now fails on any exported const array in `core/src` with no union derived from it, with a negative control proving the detector can fail. Found by reading, which does not scale; the next one will be found by the suite. (`packages/core/src/config/schema.ts`, `packages/core/src/safety/write.ts`, `packages/core/src/workflow/transitions.ts`, `FINDING(config-mode-union-derived)`.) | `supported` | None for the mechanism — the invariant is enforced and controlled. The pattern's recurrence is the thing worth a decision: five instances in one package suggests it should be named in `design/guides/testing.md` as a class to check for, alongside the tier definitions. |
| D77 | The shell supplies OBSERVATIONS; core computes every CONCLUSION. `WriteContext` carried five facts, two of which core already knew: `itemBlocked` duplicated what `project.ts` computes from the same observed meanings, and `installationHasPermission` was a boolean summary of a comparison core could make itself. Both are now derived — `isBlocked` is defined once in `workflow/state.ts` and used by the projection and the safety engine alike, and `missingPermissions` compares an intent's requirements against the grant the shell reports. The derivation is not only safer, it is more useful: a refusal now names the missing grant, where a boolean could only ever say that one was missing. `RepositoryConfig` also gains its own `revision` — the executor guards every in-flight effect on that string and D45 rules old-revision intents unresumable, yet the parsed configuration carried no identity, so two configurations could share a revision with nothing able to notice. `PermissionGrant` moved to `packages/core/src/github/permissions.ts`, both because `safety/` needed it without importing `capability/` and because a `scope:level` string is a GitHub fact. This is the eighth and last sighting of one fact stored twice — after D53, D62, D67, D73, D76 and the two closed here — and the first closed by REMOVING the second copy rather than comparing it. (`FINDING(config-revision-detached)`, `FINDING(safety-context-derived)`; `packages/core/src/safety/types.ts`, `packages/core/src/safety/rules.ts`, `packages/core/src/workflow/state.ts`, `packages/core/src/github/permissions.ts`, `packages/core/src/config/schema.ts`.) | `supported` | One framing question for stage four: `RepositoryConfig` now carries a field no maintainer writes, so a parsed configuration is no longer a valid document — the round-trip property test had to strip the stamp. The alternative is a wrapper (`{ revision, document }`), which is purer but adds indirection at every `config.mode` read and reintroduces two things that can drift. The stamp was chosen; the wrapper is the named alternative. |
| D78 | The workflow transition tables are RULES, and `screenIntent` enforces them. D29 already said capabilities move along documented edges while humans may land anywhere; nothing enforced the first half. The tables were exhaustively tested, checked against the design document by `doc-drift.test.ts`, and corrected three times by D48's audit — with ZERO callers. A capability could put `readyToMerge`, a pull-request meaning, on an issue, and both the intent screen and the safety engine passed it, because safety asks about permission, mode, blocking and human conflict, never about workflow legality. The missing link was one field: `Intent.cause` was `string` while `ISSUE_CAUSES` and `PR_CAUSES` had been closed, entity-scoped unions all along. Only the MOVING operation gets a typed cause — `applyMappedLabel` carries a `TransitionCause`; a comment and an unassign have reasons but not transitions, and keep the free-text dated cause that identifies the occasion. The check is self-contained in `screenIntent`: entity kind, the claimed `from` (the same `expected` safety rechecks as `preconditionHolds`), the desired `to`, and the cause. Two new screen codes: `meaningWrongEntity` and `transitionNotOnMap`. (`packages/core/src/capability/intent.ts`, `packages/core/src/capability/catalogue.ts`.) | `hypothesis` | The review should confirm the tables are rules rather than a profile — the alternative, and the one P7 and the build plan's "optional Hiero workflow profile" language point at, is that they are one repository family's documentation and belong outside `core/`. The type change immediately proved its own worth: `intake` stopped compiling until someone named its cause from the map, and the answer was `intakeObserved`. Whether every capability can express its reason in the closed set is now a compile-time question rather than one somebody has to remember to ask. |
| D79 | Only a human may pause an item: `screenIntent` refuses a capability that applies the mapped `blocked` meaning, with its own code `pauseNotCapabilityWritable`. Pausing is the ONE write whose blast radius is other capabilities — `itemBlocked` is not a rule about the item, it is a rule about every capability's access to it — so a capability able to set it would hold a veto over the others, acquired through shared state and without calling them. That is the coupling P3 forbids in its least visible form: the vetoed capability sees `itemBlocked` and has no way to learn who caused it. It is also the consistent reading of D54, which already refuses `immediatePreventive` outright pending an explanation-and-reversal gate: "detect a problem, freeze the item" is exactly that class, and letting a capability reach the same outcome by writing a label would be a hole in D54. The refusal is deliberately NOT `meaningWrongEntity` — `blocked` is legal on both entity kinds, and telling a maintainer they got the entity wrong when they actually tried to withhold an item from every capability would be a false reason; a test pins the rule order so the two cannot be swapped silently. Nothing about pausing changes, only who may do it. (`packages/core/src/capability/intent.ts`.) | `hypothesis` | Stage four confirms that capabilities never pause. The named alternative is to allow it for a capability that can show an immediate explanation and a simple reversal — which is D54's gate, so the answer is to build that gate rather than to reopen the label path. A capability that genuinely needs to stop work is the evidence that would reopen this. |
| D80 | `removeMappedLabel` is deleted from the operation catalogue, and `applyMappedLabel` means SET the position rather than add a label — the adapter removes the position label the item previously held as part of realising it, which is D4's rule that the platform removes only named managed labels. The operation had no legitimate use left once D78 and D79 landed, and no capability ever declared it. Removing a POSITION is not on the map: every `to: null` edge is a closure, and D47 makes closure a reason read from GitHub, never a label the platform writes — so leaving a position without closing is not a documented move, and a position CHANGE is an apply. Removing `blocked` is unpausing, which needs the same authority as pausing and D79 reserves to humans. Removing a label a human misapplied is exactly the automatic repair D29 and D35 refuse. An operation with no legal use is dead vocabulary in a closed catalogue (D61) — the kind that gets used later without anyone rechecking whether it was ever allowed, which is how the transition tables went unenforced for months. Deleting the name is safe in a way deleting a CAPABILITY name is not (D58): operations never appear in a repository's configuration, so no reviewed file can fail closed over this. (`packages/core/src/capability/catalogue.ts`, `packages/executor/src/planner.ts`.) | `hypothesis` | The evidence that would reopen it is a capability that must take a position OFF an item without closing it or replacing it — at which point the answer is a documented edge to `null` with a non-closure cause, not an unscreened operation. The ambiguity that made this operation look necessary was `applyMappedLabel` reading as "add a label"; that is now stated as "set the position" in the catalogue. |
| D81 | An observation carries the PROJECTION, not a flattening of it. `workflow/project.ts` distinguishes a CONFLICT — more than one own-flow position, which D35 forbids repairing — from a position carrying its meaning, blocked flag, closure reason and the ignored cross-flow meanings, and it is tested exhaustively over all 128 meaning subsets. The observation payload discarded that: it carried a bare `meanings` list plus separate `blocked`, `closed` and `merged` booleans, so the conflict signal died at the boundary and a capability handed an issue a human had put in two positions could not tell it from a clean one. The payload's own comment said "`null` when it conflicted" — behaviour the type never had, which is how long the gap had been there. `blocked` was also a DUPLICATE the shell had to keep consistent with the same meanings it derived from, the ninth sighting of one fact stored twice after D53, D62, D67, D73, D76 and D77; `closed` and `merged` were likewise restatements of `state.closedBy`. Carrying `ObservationProjection` removes all four. Capabilities now handle three outcomes rather than two — conflict, position, nothing — which is correct, because the situation has three cases and the third was silently collapsed into the second. The type change proved it immediately: both probes had to be given conflict handling they did not have, and `intake` would have triaged a conflicted issue. (`packages/core/src/capability/catalogue.ts`, `packages/probes/src/`.) | `hypothesis` | One question follows: `Intent.expected.meaningsPresent` is now a restatement of something the capability was just handed, which is the same duplication in a new place. The screen's `positionConflict` check (D78) reads that claim rather than the projection, so a capability that copies one of two positions still validates the wrong edge. Closing it means the screen reading the observation, which the intent does not currently carry. |
| D82 | A documented example is only documentation until something runs it. `design/contracts/config-schema.md` §3 showed two YAML configurations and no code had ever read YAML — `parseConfig` takes `unknown`, and every caller so far handed it an object literal. Both examples were checked and both parsed, by nobody's doing: the on-disk format was unspecified, so a document could fail in ways the error catalogue had no word for, and the only signal would have been a maintainer's confusion. Two artifacts now close this, and the split between them was learned the expensive way. `docs/examples/` holds the four VALID documents — the adoption ladder, with the observe→active step asserted to change the mode and not the vocabulary — read by `packages/dev/checks/test/examples.test.ts` through the entry point the shell will use. The REJECTIONS began there too, as one file per `ConfigErrorCode`, and scored the new module at 0.00% mutation: Stryker's sandbox contains `core/` and nothing above it, so root-level fixtures ran under vitest, passed, and could not kill a mutant — the third silent-skip of this shape, after the `src/*.ts` mutate glob and the capability boundary being covered only from `probes/`. They are now an in-package corpus (`packages/core/test/config/documents.ts`, thirty documents, several shapes per code), exhaustive by a mapped type over the union so adding a code fails compilation until a document reaches it (D76's rule applied to a catalogue). Three findings from writing it: `duplicateKey` earned its own code as the only malformed document that SUCCEEDS — YAML keeps the last value, so `mode: observe` followed by `mode: active` parses cleanly into a repository that writes; the alias limit THROWS rather than returning, so a hostile file in a pull request would have crashed the shell instead of producing a finding — converted, keeping the property that every rejection is a value, with the budget pinned at ten by a twenty-alias document the library's own default would accept; and a first draft appended `linePos` to messages that already carry position and excerpt — the TENTH one-fact-twice after D53, D62, D67, D73, D76, D77 and D81, caught because mutation flagged the guard as untestable, and fixed by deleting the duplication rather than testing it. Core gains its first runtime dependency (`yaml`, zero transitive). (`packages/core/src/config/document.ts`, `packages/core/src/config/schema.ts`, `packages/core/test/config/documents.ts`, `docs/examples/`.) | `hypothesis` | Two. The dependency is the reviewable half: the alternative is YAML living in the shell, which keeps core clean and leaves syntax errors outside the catalogue that every other configuration problem is in. And `ConfigError.path` is a dotted path into a mapping, so a file that never became a mapping has none — position lives in the library's message. A structured `position` field is the named alternative, deferred because it widens a type every consumer reads for one producer's benefit; the PR-time check run is the consumer that will decide it. |
| D83 | `docs/` exists for maintainers who will not read much: three pages, config first, written in the confident register of a shipped product rather than the cautious one of a prototype — the mode ladder is offered as a choice, not prescribed as a rollout. `configuration.md` is a per-key definition list (type, required, default, allowed) after CodeRabbit's shape, plus the CONTEXT the tables cannot carry: why label mappings exist at all, and that the required set depends on which capabilities you enable. Every table restates a closed vocabulary the code owns — modes, meanings, the error catalogue, the refusal codes — which is one fact in two places, aimed at a reader who cannot run the compiler that would catch the drift. So the tables are locked instead of trusted: `packages/dev/checks/test/docs.test.ts` parses each table and requires set equality with the union (mapped types where no runtime array exists, D76's rule). `TOP_LEVEL_KEYS` moved to `schema.ts` and is exported so the definition list can be held to it in both directions — a reference guide's characteristic failure is not being wrong but being INCOMPLETE, and a key nobody documented is discovered by a maintainer when their file is rejected, and the grouping in `troubleshooting.md` — "on purpose" versus "needs you" — is asserted against `report/convert.ts`'s severity classification, so the docs cannot tell a maintainer to relax about something the platform classifies as a problem. Boundary rule: `docs/` never cites a D-number or a source file; when a page needs a why, it links to `design/`. One deliberate exception: `docs/to-do.md` is maintainer-facing by design — it records what the user-facing pages cannot yet say truthfully and cites the register rows that unblock them, so the docs' gaps are as auditable as their claims. It is not linked from the user index. (`docs/quickstart.md`, `docs/configuration.md`, `docs/troubleshooting.md`, `packages/dev/checks/test/docs.test.ts`, with `docs/README.md` a three-link index because GitHub renders it on directory navigation.) | `hypothesis` | The pages state what the built logic does, but none of it has run against a real repository, and `docs/README.md` says so in its first line. The claim to revisit at stage four is the audience assumption itself — that config-first, table-first is what SDK maintainers want. The interviews are the test. PR #121's 2026-08-21 audit corrected the implementation claim: the suite locks the named identifier columns and troubleshooting severity groups, not every table cell or explanatory behavior; the pages now state that narrower scope and keep prose review-owned. |
| D84 | A capability must declare the mapped meanings it needs. Writing the user-facing configuration guide asked a question the platform cannot answer: which labels must a maintainer map to use a given capability? `CapabilityDeclaration` states triggers, config keys, observations, resolvers, intents, permissions and operational needs — not meanings. So the requirement is discovered at RUNTIME, when the capability finds its meaning unmapped and skips itself with an explanation, and the maintainer's experience of enabling `intake` against an unmapped repository is silence plus a buried report line. With meanings declared, `parseConfig` gains the check it obviously wants — an enabled capability whose meanings are unmapped is a configuration error with a path, caught by the PR-time check run before anything runs — and `docs/` gains a per-capability requirement table that can be locked like every other list. The gap is documented honestly in `docs/configuration.md` rather than papered over. Related: `design/contracts/config-schema.md` §227 still leaves the reviewed file's path unchosen, and a confident quickstart cannot have a blank there; `.github/hiero-automations.yml` is used throughout `docs/` as the working assumption. | `hypothesis` | Two decisions for stage four, both small and both blocking a claim the docs already make. The declaration change adds one field, one validator rule and one error code. The path needs choosing, not designing — the docs assume `.github/hiero-automations.yml` and every example will need a rename if that is wrong. |
| D85 | Tests about the REPOSITORY live in their own workspace package, `checks/`, not inside core. Four files in `packages/core/test/` read outside the package: `docs.test.ts` (reads `docs/`), `examples.test.ts` (reads `docs/examples/`), `doc-drift.test.ts` (reads `design/`), and `repo-artifacts.test.ts`, which reads the whole repository root and imported nothing from core's `src/` at all. The costs were not hypothetical: core's suite failed when a markdown file changed; the rejection fixtures scored `document.ts` at 0.00% because Stryker's sandbox is `core/` and root-level files are not in it (D82); and core source had begun to justify an export by naming `docs/`. The boundary now points one way — `checks/` depends on core through its public barrel (all nine imports were already public; none needed opening), and core does not know the repository exists. `checks/` has no `src/`, which is the point: nothing in it can or should kill a mutant, so nobody will again mistake a repository check for a package test. Verified during the move: `transitions.ts` holds 95.56% mutation without `doc-drift.test.ts` in the package, so the relocation cost core no coverage, and CI's `pnpm -r test` picks the package up with no workflow edit. The citation invariant caught all six stale paths the move created, including in its own D-rows. | `supported` | The inclusion rule for the future: a test that reads another package's files, or the repository root, goes in `checks/`; a test that can kill a mutant in `core/src` stays in `core/test`. The named risk is drift toward a junk drawer — `checks/` accepting behaviour tests because it is easy; the no-`src/` shape is the guard, and the trigger to revisit is the first test there that imports more than core's barrel. |
| D86 | The top level holds workspace packages and exactly three knowledge roots — `design/` (internal why), `docs/` (users), `examples/` (users, executable) — and nothing else. `audit/` and `planning/` predated any such rule: the first was fieldwork evidence frozen since 2026-07-17 with nothing marking it frozen, the second was four roadmap files whose genre already had a second home (the ratification packet sat in `design/` while the stage-four packet sat in `planning/`). Both moved under `design/`, each gaining the README it never had — `design/audit/README.md` declares the fieldwork read-only and rules that corrections are new decision rows, not edits to evidence; planning's own index (since retired) separated living from historical, which is the whole treatment of legacy documentation: never deleted, always labelled. `experiments/` needed nothing — untracked by design (D32), its conclusions already migrated. The move exposed a blind spot worth more than the move: sixteen documents cited `audit/…` and `planning/…` paths and the citation invariant stayed green, because it validated only TypeScript paths — and its package alternation did not know `checks/` existed either. Both fixed: repo-rooted document paths (`design|docs|examples/….md|.yml`) are now checked with the same rigour as source paths, which is only possible BECAUSE the top level is closed — an open top level has no alternation to write. A new invariant enforces the sentence itself, with `audit`/`planning` as the negative control. (`packages/dev/checks/test/citations.test.ts`, `packages/dev/checks/test/toplevel.test.ts`, `design/audit/README.md`.) | `supported` | The named cost: `design/` grows heavier, and if a future document class is neither internal rationale nor user-facing it will strain the three-root rule — the first candidate to watch is operator-facing runbooks when the App ships, which may deserve a fourth root, added deliberately rather than by drift. |
| D87 | The lab's future is decided by trigger, and its present is guarded by two checks. The plan as recorded: `experiments/` stays as it is until work on `core/github/events.ts` begins; at that trigger it becomes a tracked workspace package (`lab/`) carrying its own protocols — methods live with the instrument, conclusions keep migrating to `design/` — whose first scenario (7.1) captures real webhook payload bodies, scrubs them, and promotes them to normalizer fixtures, because the era-1 payload copies exist only in the private unscrubbed archive — complete bodies with commit-author emails, as its own README warns — and scrubbing after the fact cannot make them trustworthy as tracked fixtures. When the adapter ships, the lab gains its third career: a scheduled credentialed probe that re-verifies the perishable facts and stamps a tracked result file a `checks/` lock reads, so `probedAt` becomes a date a cron writes. This amends D32's "the harness is disposable" — true for feasibility, false for a harness needed three times. Checkable today, and checked: `packages/dev/checks/test/never-tracked.test.ts` (then `lab.test.ts`) asserts git tracks nothing under the lab's local-only layer (originally all of `experiments/`) (the never-tracked rule was one .gitignore line that `git add -f` bypasses silently — it protects sandbox credentials), and `provenance.test.ts` locks `packages/core/src/github/README.md`'s provenance table to `BODY_PATTERNS`. Writing the second lock found the table already wrong: the README credited experiment 6.4 for `installationSuspended`, which the code stamps 6.1 — the third one-fact-twice inside `github/` alone, caught by connecting the two copies, as every one before it was. | `hypothesis` | The triggers are the decision: no lab work before `events.ts` starts, no conformance cron before the adapter ships. The named risk is the capture scenario leaking sandbox identifiers into tracked fixtures — scrubbing rules must be written into protocol 7.1 before the first capture, not after. |
| D88 | The lab is built: `experiments/` is renamed `lab/` and becomes the sixth workspace package, with D87's trigger pulled forward by the owner's decision on 2026-08-06. Tracked: the era-1 protocols (methods stay with the instrument), `src/scrub.ts` — protocol 7.1's scrubbing rules as code, written and tested BEFORE any capture exists, which was D87's one condition — `src/capture.ts`, an HMAC-verified zero-dependency receiver that scrubs every payload before anything touches disk and writes only to the untracked `evidence/pending/`, with reviewed captures landing directly in `packages/dev/testkit/fixtures/` — the owner asked why a tracked waypoint (evidence/curated) existed at all, and the answer was that it should not: the capture trigger IS the normalizer trigger, so a waypoint would hold fixtures for no interval, while being exactly the evidence-outside-the-package shape that scored 0.00% mutation in D82. The lab tracks no evidence. Never tracked: `harness/` (era-1 code and the private archive), `evidence/` bulk, `.env` — the never-tracked check follows the new layout and covers all of `evidence/`. The scrubber's contract is deterministic REPLACEMENT, not deletion: the same account maps to the same placeholder everywhere in a payload, URLs still contain the logins their payload names, and workflow content — actions, numbers, titles, label strings — passes through untouched, because a fixture that lost its referential structure tests nothing. One correction landed on the way: D87 claimed the era-1 evidence held no payload bodies; in fact complete bodies exist in the private archive with commit-author emails (its README's own warning), which strengthens rather than weakens 7.1 — unscrubbed-at-source copies can never become tracked fixtures, so fresh captures through the scrubber are the only trustworthy path. The citation invariant caught a pointer in protocol 6.1 still naming `failures.ts` at its pre-D73 location outside `github/` within minutes of the protocols becoming tracked — the strongest argument for tracking them, demonstrated by them. (`lab/`, `packages/dev/checks/test/never-tracked.test.ts`, `packages/dev/checks/test/citations.test.ts`, `.gitignore`.) | `supported` | Era 2's exit is protocol 7.1's: every observation kind carries a curated payload and `events.ts` tests import them, with every fixture born inside core. Era 3 stays where D87 left it — no conformance cron before the adapter ships. |
| D89 | Two moves from auditing the lab against the package boundary, plus the naming rule for `checks/`. (1) Webhook signature knowledge leaves the lab: `capture.ts` privately held GitHub's signing scheme — header name, `sha256=` format, HMAC over the raw body, constant-time compare — and the shell must verify the identical thing before it acks (P9), which made it the next one-fact-twice on a schedule. Now `packages/core/src/github/signatures.ts` (DOCUMENTED knowledge, so no `probedAt`, unlike its perishable neighbours), exported through the barrel, consumed by the lab, at 100% mutation. Getting there taught a testing lesson worth keeping: two mutants survived because breaking `signBody` crashed test COLLECTION — headers were built at describe-time — and vitest reports a collection crash as "no tests", which Stryker scores as survived; the fix is lazy fixture construction, so a mutant dies on an assertion. (2) `packages/dev/checks/test/` becomes one-file-per-invariant — `sources`, `mutation-coverage`, `enumerations`, `citations`, `toplevel`, shared `repository.ts` — because the original artifact-invariants file had absorbed seven unrelated describes and the next reader would have added an eighth. The naming rule going forward: a file per watched TARGET (`docs`, `examples`, `lab`) or per INVARIANT, and a target earns a subdirectory only when it needs a second file. (`packages/core/src/github/signatures.ts`, `packages/dev/checks/test/repository.ts`, `packages/dev/checks/test/citations.test.ts`.) | `supported` | The signature scheme is GitHub's published contract, so unlike `failures.ts` there is no re-probe obligation — but the shell's consumption is the real test, and if the shell needs a different shape (streaming bodies, say) the function signature is the thing to revisit, not the location. |
| D90 | EXPERIMENT — one meaning vocabulary, entity-ness as facts. The complexity audit of 2026-08-06 measured two of core's five complexity sources to one root: meanings exist twice — config's flat `MappableMeaning` and workflow's hand-written per-entity unions — with no type-level relationship, so `screenTransition` bridges the two vocabularies with six `as`-casts in seventy lines, each safe only because of an `includes()` guard the compiler cannot see. When code routes around the type system, the static safety was paid for and not received. Hypothesis: a `MEANING_FACTS` table in `config/schema.ts` (mapped over the union, so exhaustiveness is compiler-checked), with `IssueMeaning`/`PrMeaning` DERIVED from it by conditional type and two predicates (`isIssueMeaning`/`isPrMeaning`) replacing every cast, removes the duplication and the cast cluster while keeping all compile-time guarantees — the always-false-comparison bug class stays a compile error, `blocked` stays out of the position unions by derivation, and D50's entity-scoped causes are untouched (genuine vocabulary, not duplication). `EntityKind` moves to config beside the table so `MeaningFlow` does not restate its literals. Exit criteria, decided before implementation: (1) the full suite passes unchanged — behaviour preservation is the definition of success; (2) `screenTransition` contains zero type assertions; (3) the derivation corner fits in one commented block a reader can skip; (4) mutation score on the touched files holds the 90 gate. Abandonment criterion: if the derived types misbehave badly enough to need casts of their own, the branch is abandoned and this row records why. RESULT (same day): all four exit criteria met. Suite green with behaviour unchanged (six packages; core grew 374→381 tests, all additions); `screenTransition` contains zero type assertions; the derivation corner is one commented block in `workflow/positions.ts`; mutation on the touched files: `schema.ts` 100%, `positions.ts` 100%, `intent.ts` 94%. The derivation DID misbehave once, instructively: a type annotation on `MEANING_FACTS` widened every `flow` to the union and collapsed the derived types to `never` — the fix is `as const satisfies`, and the comment on that line now says the correctness of the whole derivation hangs on it. The mutation gate then earned its keep twice: four surviving mutants marked the cause predicates as untestable from the screen (the edge table backstops the same verdict, so only direct predicate tests can see them), and four no-coverage mutants showed the new cross-flow refusal branches had no end-to-end test — both closed, which is how the refactor ended with MORE verified behaviour than it started, not merely equal. | `supported` | Costs accepted in advance: one dense type-derivation corner (relocated complexity, not removed), and churn across workflow/capability/config plus their tests. Costs avoided by the derived-types middle path, for the record: loss of editor-time narrowing, rule-instead-of-shape for blocked, and signature self-documentation — the pure-data variant was considered and rejected for exactly those. |
| D91 | The vertical slice is closed in pure logic: a delivery GitHub actually sent — captured 2026-08-07 under protocol 7.1, scrubbed twice (the exhaustive read added description-blanking and deterministic sha replacement to the scrubber after the identifier passes missed both), reviewed, and promoted to `packages/dev/testkit/fixtures/` — travels normalize → capability → screen → safety → report with zero network and zero invented payloads. The new piece is `packages/core/src/engine/events.ts`: label strings die into meanings via the reverse lookup (whose injectivity precondition D55's validator rule had already guaranteed, years of luck compressed into one sentence), state fields die into a `ClosureReason`, and the label set dies into the projection — a capability can no longer learn GitHub's wire format exists. Three verdicts, all total: `observation`, `ignored` (pushes and pings are the system working), and `malformed` with a MACHINE CODE — the first draft carried prose only, and the mutation gate exposed it structurally: seven guard clauses collapsed into one observable, so their mutants were indistinguishable; D75's every-failure-carries-a-code rule was the fix, not more tests. Two honest limitations recorded in code: `completedByLinkedMerge` is unobservable from the webhook alone (timeline API, adapter era — closed issues read `closedByHuman`, the conservative reason), and `staleItemsDue` is queried rather than delivered, so it joins with the scheduler. One named cost: `events.ts` type-imports the observation shapes from `capability/`, which type-imports `PermissionGrant` back — a type-only edge each way, erased at runtime, chosen over duplicating the payload shapes. `packages/core/test/slice.test.ts` is the closure: fixture in, `problems() === []` out, with dry-run telling the same story as a notice. (`packages/core/src/engine/events.ts`, `packages/core/src/config/labels.ts`, `packages/core/test/slice.test.ts`, `packages/dev/testkit/fixtures/`, `lab/protocols/7.1-capture.md`.) | `hypothesis` | The next slice wraps this one: the shell's HTTP accept (P9's store half already landed from a contributor in #69), the executor applying the verdict, the adapter writing it. The type-only edge is the thing to re-examine when `github/` splits into its own package — the split trigger its README already records. |
| D92 | EXPERIMENT — core exports one verb. The pipeline (normalize → evaluate → screen → safety → report) is hand-wired four times — `slice.test.ts`, `scenario.test.ts`, the probes harness, the future shell — and the wiring is the most duplicated un-owned thing in the project. Worse, the current API makes callers RESTATE two conclusions the observation already contains: `WriteContext.observedMeanings` and `preconditionHolds` are derivable from the projection core is simultaneously holding, and nothing compares the restatement to the source — a shell asserting a stale precondition is accepted today. That is D77's rule applied to every layer except the API itself, and the twelfth one-fact-twice, hiding in a signature. Hypothesis: an `engine/` module owning composition — `decide(delivery, config, externals) → { report, approvedIntents }` with externals reduced to the three genuinely external facts (kill switch, installation grants, human-change ordering as caller-supplied lookups; resolvers as scripts, the probes-harness pattern) — collapses the duplication, finishes D77 by making the safety context derived rather than asserted, retires the boundary generics (the engine and an intent factory become the ergonomics; the screens stay the enforcement), and lets the shell be transport. Migration is strangler-shaped with the breaking change LAST: (1) build beside, touching no signature; (2) parity — the engine's report deep-equals every hand-wiring on identical inputs, any mismatch stops the work and is itself a finding; (3) absorb consumers one PR each, executor's planner last since the decide/planner boundary (decisions out, API shapes in executor) is the one open design question; (4) only as sole caller may `WriteContext` narrow to three facts — the lying shell becomes unwritable; (5) the four-story renames (vocabulary/rules/engine/report), mechanical. Exit criteria, before code: slice test ~180→~30 lines asserting the SAME report; probes harness thins to a wrapper; the stale-precondition-asserting shell cannot be expressed against the new API; suite green throughout; engine and derivation ≥90 mutation. Abandonment: if decide() needs its own configuration surface to stay general, composition is not ready to be owned, and this row records that instead. | `hypothesis` | Phase results append here. PHASES 1–2 (2026-08-07, same session as the row): the engine exists beside everything — `packages/core/src/safety/world.ts` (the derivation, 95% mutation; its round-trip tests include the conflict-branch closure trap) and `packages/core/src/engine/decide.ts` (the verb, 91% combined with 11 accepted prose-literal survivors), no existing signature touched. THE PARITY GATE HOLDS: on the real `issues.opened` fixture, decide() reproduces the hand-wired slice report finding-for-finding, and the flagship test passed on first run — the derivation caught a capability claiming `awaitingTriage` absent on a delivery that shows it present, with no caller asserting anything (`preconditionStale`, engine-derived). One design note deferred to phase 3: an approved intent's own `explanation` is not surfaced as a finding — parity with the hand-wirings, which only surfaced verdicts; whether it should be is a report-design question, not an engine one. Suite: 641 tests green across six packages. PHASES 3(d)+3(b) (same day): the intent FACTORY (`packages/core/src/capability/factory.ts`, 100% mutation) — occasion bound once, explanation unskippable, omitted `expected` vacuous by default, destructive passthrough with the key absent-not-undefined — and with it the phase-1 design note RESOLVED: an intent that acts or would act (apply and record-only) surfaces its explanation as a finding beside its verdict; refusals keep their reasons unaccompanied. Both wirings evolved together and parity still holds. All three probes rewritten on the factory with zero test churn — 32/32 unchanged, the drop-in proof. The toggle matrix now ALSO runs through decide() (`packages/probes/test/engine-matrix.test.ts`): P3 isolation holds at the engine level for every subset, measured on approved-intents-plus-findings — the production composition, not the harness. One tension surfaced and parked for 3(c), visible in that matrix rather than hidden: `inactivity` claims `closed: false` against the unprojected `staleItemsDue`, which the engine's derived preconditions refuse (`preconditionStale`) where the planner's caller-vouched context accepted — the sweep path needs either shell-verified openness at act time or a projected stale observation, and that is the planner absorption's first question. The old harness matrix stays until 3(c) retires the harness with the planner. Remaining: 3(a) seeded as issue #72; 3(c) DESIGNED by full inspection, ready to execute. The planner's five jobs, dispositioned: its screens and its evaluateWrite call are duplicates of the engine — DELETE; its evaluateDestructive routing (with D60's warning rebuilt from the stored warned cause, and the clock) is a DECISION and MOVES into decide() — `DecideExternals` gains `now`, and `describeChange`'s §2.6 target wording moves with it; its batch checks (`duplicateIdempotencyKey`, `mixedRepositoryBatch`) and translation-representability check (`mappedLabelMissing`) are genuinely the planner's — KEEP, as `planApproved(approved, {repository, config})` returning plans plus planner-origin refusals, with `planningReport` retiring in favour of decide()'s report plus appended planner findings. One correction already landed ahead of the rest because it was independent and safe: decide() now reads `requiredPermissions` from the catalogue (`INTENT_OPERATIONS`), the platform-owned source (D62) — the declaration's copy was the redundant restatement. The stale-sweep tension resolves by act-time deferral, not by weakening claims: for UNPROJECTED observations the engine treats `expected` as unverifiable-here and passes it through — the claim rides inside `AdapterCommand.expected`, which the adapter rechecks against live GitHub at write time, the only place a sweep's openness claim can honestly be checked. PHASE 3(c) EXECUTED (2026-08-07): the engine gained the destructive gate (clock in externals, warning rebuilt from the stored cause exactly as the planner did it — the grace trio and the cause-drift refusal now pinned in the engine's own suite, four-for-four on first run) and the act-time-deferral rule replaced vacuous-only for unprojected observations. The planner halved as designed: 518 → ~230 lines, screens/safety/destructive deleted, `planApproved` returning plans plus its three translation-owned refusals, `plannerFindings` for appending to a decision's report, `planningReport` retired. `describeChange` moved into the engine and is exported and pinned — its outputs feed both sides of the warning snapshot symmetrically, so only a direct test can see them. Executor tests 50 → 44 (the six retired were the duplicated safety paths), composition rewritten decide→planApproved→executor, the harness matrix deleted in favour of the engine matrix (inactivity now warns THROUGH the engine — the 3(b) tension resolved, visible in the same test that raised it), `world.ts` thinned to config builder + subset enumerator + observation alias. Engine holds 92% mutation with the destructive branch covered. The one remaining hand-wiring is the slice test's, kept deliberately as the parity specification (#72 hands it to a contributor). PHASE 4 EXECUTED (2026-08-07): the payoff, upgraded from the plan. Rather than merely narrowing `WriteContext`, the two derivable facts became the BRANDED `DerivedWorld` (`packages/core/src/safety/world.ts`, absorbing the derivation from the engine): constructible only through `deriveWorld(projection, claims)`, whose brand symbol the barrel does not export and whose deep path core's `exports` map blocks from outside the package — a shell asserting a stale precondition now has no type to assert it with, D60's warning-brand pattern applied to the world itself. `WriteContext` is three external facts plus the branded world; `ExpectedFacts` became an alias of safety's `ClaimedFacts`, putting the claim's definition in the checker's module; `assertedWorld` exists deep-only for the rule suite's precedence grid, documented as such. Safety at 93% mutation. With this, D92's exit criteria are met in full: the lying shell is unwritable, the wiring has one owner, parity held throughout, and every phase's gates ran green. PHASE 5 EXECUTED AS THE SUBSET THE GRAVITY SUPPORTS (2026-08-07), and the remainder DECLINED with reasons rather than forced: `events.ts` moved `github/` → `engine/` — normalization is the pipeline's first stage, and the move retired the capability⇄github type-only cycle D91 had priced (the planned adapter-era subscription list takes the name `subscriptions.ts`, exempted until it exists). The fuller vocabulary/rules re-layering was inspected and refused: `project.ts` is pinned below safety by `world.ts` (moving it mints a real cycle), the screens are entangled with the intent types in a way that makes extraction surgery rather than renames, and `safety/` already IS the rules story under its own name. The four stories exist — vocabulary (catalogue + meanings + facts tables), rules (safety + screens + the map), engine (events + decide), report — as the READING of core, recorded here and in the module READMEs, not as directory churn chasing a diagram. Structure followed substance exactly as far as substance led; D92 closes with all five phases disposed. POST-CAMPAIGN CLEANUP (audit №3, 2026-08-07): the third audit caught the campaign's one unexecuted promise — the 3(d) retirement — and the concentration it deliberately created. Both settled: `decide()` decomposed from a 182-line body into an 86-line orchestrator over named steps (`gateIntent`, `destructiveOrWrite`), behavior pinned by the parity suite throughout; the boundary generics RETIRED BY UNIFICATION rather than deletion — inspection showed `IntentFor`/`ObservationFor` are shallow Extracts serving the load-bearing `Capability` signature, and the true complaint was the cast idiom, so `intentFactoryFor(declaration, occasion)` constrains the operation to the declaration and its output assigns with no cast, killing every `as IntentFor` in the probes; and `toEngine()` owns the erasure argument once, retiring the `as unknown as EngineCapability` idiom at every consumer. Deliberately untouched, as the audit advised: the five-switch operation checklist (named in `catalogue.ts`, consolidation waits for a real fourth operation) and the provenance density (a standing choice). Engine 93%, factory 100%. The shell now inherits idioms worth copying. Original consumer list, for the record: the former executor planner tests (safety cases retire to core's suite; translation and batch cases stay), the former probes composition test (decide→planApproved pipeline), the old harness toggle-matrix (superseded by the engine matrix), and `world.ts` thins to `configEnabling`+`subsets`. `boundary.test.ts` is untouched. The named risk: absorbing the planner (3c) discovers the boundary is wrong — which is why it is sequenced last and inspected before touched. PHASE 3(A) EXECUTED (2026-08-11, #72): the row's one remaining hand-wiring retired to the parity specification it was always meant to become. `slice.test.ts`'s narrative now runs through `decide()` — one capability, fixture in, assertions on the decision — and the hand-wired pipeline (`screenIntent`, `evaluateWrite`, `explanationFinding`, `verdictFinding`) survives in exactly one place, the parity test, as the specification `decide()` is held to rather than duplicated plumbing. 182 → 147 code lines (comments and blanks excluded); nothing in `core/src` changed. |
| D93 | The transport shell exists and decides nothing (`shell/`, 2026-08-07). The seventh package composes entirely from seams that already existed; verify (core's signatures) before durable accept (the store's intake, contributor PR #69) before the 202; then claim → `parseConfigDocument` → `decide()` → persisted record → complete, a crash mid-decision RELEASING the claim so the delivery is retried rather than lost or double-run. Configuration lives at `automations.yml` in the REPOSITORY ROOT; Q14's path half, decided this date: the file configures the automation platform, not GitHub, and everywhere else in the design GitHub is an adapter detail, so a `.github/` home would contradict the architecture at its most user-visible spot (experiment 6.3's fail-closed mechanics are path-independent; the root-file precedent of tools like CodeRabbit was weighed against the `.github/` convention of Dependabot, and root won on identity, not visibility). A broken config FAILS CLOSED as a `configRejected` record and the delivery completes; redelivery cannot fix a config; the fixed file arrives as its own future delivery. At the time, reports persisted to a filesystem sink rather than a store table; D110 later replaced that choice: dry-run's product is the report, and the ratified four-table schema stayed untouched until a report table was a decided need rather than a default. Externals ship as stubs with the shape of the truth, each a named hole the read-only adapter work packet fills behind an existing seam (`ConfigSource`, `DecideExternals`, later the executor's read-back); GitHub decisions do not move into the shell when they land; composition and seam-contract adaptation may change. The one honesty cost is recorded in code and here: `latestHumanChangeAt` stubs to `null` rather than the safe-conflict `"unknown"`, because `"unknown"` refuses every write and would bury dry-run's real findings under a uniform refusal; so dry-run reports OVERSTATE what would apply until timeline evidence arrives. The 2026-08-30 zero-stub rehearsal closed that honesty cost: live default-branch config, installation grants, linked-issue reads, and timeline evidence produced a dry-run report with `newerHumanChange` for a later human label. Out of the first slice, deliberately: the scheduler (`staleItemsDue` arrives as a second caller of `decide()`, not a second pipeline), active mode (the shell does not even import the executor), multi-repository routing. The probes ride as the wired capabilities at the entry point only; sandbox-era wiring that dies with the probes, replaced by real capabilities without moving capability logic into the shell. (`packages/shell/src/receiver.ts`, `packages/shell/src/processor.ts`, `packages/shell/src/config.ts`, `packages/shell/test/shell.test.ts`.) | `hypothesis` | The definition of done runs as a test: a delivery GitHub actually sent (the captured `issues.opened` fixture) travels socket → verify → durable row → 202 → `decide()` → a canonical report over real HTTP and real SQLite in `packages/shell/test/shell.test.ts`; a locally signed reconstruction of sandbox pull request #2 then traversed the live adapter and persisted a `dry-run` decision with config revision `sha256:1fd133a6686c` and `newerHumanChange`. The shell→probes dependency remains an entry-point-only cost. The original filesystem sink was append-only and unqueryable; D110 later made SQLite canonical, and the current reduction removed that sink. The `exports` map probes always lacked (nothing had ever imported it) is added, which is itself the first proof a consumer exists. |
| D94 | Per-file difficulty is FLAT — four audits of sanding left every re-read suspect (`packages/core/src/capability/boundary.ts`, `packages/core/src/report/convert.ts`, `packages/core/src/safety/rules.ts`) reading clean — and the residual difficulty has RELOCATED to between the files. Ranked: (1) the vocabulary load, ~20 terms held before any single file reads, glossary defines them separately and separate definitions don't teach a system; (2) `packages/store/src/store.ts` at 752 lines, the workspace's largest file by 2×, four unrelated domains in one class; (3) three alarm-bell spots in `packages/core/src/engine/decide.ts` — `Parameters<typeof …>` utility types where named types exist, and an uncommented `as never` triple; (4) `safety/` as the only load-bearing core module without a README (the three-door topology lives in a header comment). Named and left alone deliberately: the D90 derivation corner, catalogue provenance density, and the register's chapter-length rows — all standing choices with their reasons on file. Actions executed same day: THE TRACE (a narrated-delivery document under `design/`, **deleted 2026-08-17** once core's glossary and the architecture diagrams covered its two jobs) — one real delivery followed socket → canonical SQLite report with every term introduced at the moment it acts, every quoted artifact generated by running the actual fixture through the actual pipeline, linked as station 0 of core's reading path — and the decide() clarity fixes (named `EngineProjection`/`WriteRequest`/`WriteContext` params, erasure pointer at the `as never` triple). Deferred with trigger: the store split waits for the scheduler era, when the schedules table gains its first real caller. Open offer, not yet ratified: a 25-line `safety/` README. | `supported` | The audit's stop-condition from №4 (reopen when a new package lands or a reader reports difficulty) fired correctly — both happened. Next reopen trigger: the read-adapter work packet landing, or the trace failing a newcomer (its real test is the mentee). |

| D95 | The seven workspace packages live under `packages/`, and the top-level rule tightens with them (2026-08-08). The trigger was a reader's report — the top level had grown to twenty-one entries, twelve of them directories, and nothing distinguished *the code* from *the knowledge about the code* at the moment of first glance. D86's sentence survives, generalised: a top-level directory holds workspace packages, or it is one of the three knowledge roots. The package root is now DERIVED from the workspace file rather than spelled, so the rule stays true whether packages sit under one prefix or at the top level, and `pnpm-workspace.yaml` lists entries rather than globbing `packages/*` — the checks package reads that file as the one source of truth for what a package IS, and a glob would hand it a pattern where it needs paths. What the move did NOT touch is the reason it was affordable: every cross-package import goes through `@hiero-hackers/automation-*`, so not one `import` line changed, and CI drives everything by `pnpm --filter <name>`, so no workflow step moved. Three classes of breakage did exist and each was closed by measurement rather than by eye: the six path escapes in `checks/` (repoRoot gained a level), 182 repo-rooted citations across seventeen documents (rewritten mechanically, with the citation invariant as the completeness oracle — it went to zero matches the moment the packages moved, which is exactly how it should fail), and nine stale path citations inside code comments. THE SAFETY-CRITICAL EDIT, recorded because it was one command away from a leak: `.gitignore` names `lab/harness/`, `lab/evidence/` and `shell/data/`, and for the minutes between the `git mv` and the ignore-rule update, unscrubbed capture payloads and the sandbox App's credentials were visible to `git status` — the never-tracked check enforces those exact rules and would have failed the build, but the ordering lesson stands: move the ignore rules in the same breath as the directory. Two eslint ignores were added for the same never-tracked trees, which had been making local `pnpm lint` disagree with CI on precisely the machines doing the work. (`pnpm-workspace.yaml`, `packages/dev/checks/test/toplevel.test.ts`, `packages/dev/checks/test/links.test.ts`, `.gitignore`.) | `supported` | The named cost, argued before the move and still true: the flat list — core, store, executor, shell, probes, lab, checks — read like a description of the system, and one generic directory now stands where that description was; `ls packages/` and the README's list recover it, judged a fair trade for a root a newcomer can take in at a glance. Contributor cost: open PRs touching moved paths need a rebase. Deliberately NOT moved: `examples/` into `docs/` — executable documentation and prose are different contracts (D82), and the file counts never supported the premise (five files each; the heavy root is `design/` at forty-seven). |
| D96 | Markdown links resolve from the document that carries them — the eighth repository invariant (`packages/dev/checks/test/links.test.ts`, 2026-08-08). D95 could not be verified by eye: moving a package silently breaks every RELATIVE link inside it, the text still reads correctly, and nothing fails. The citation invariants check repo-rooted paths (`packages/core/src/….ts`, `design/….md`) and had never once resolved a parent-relative markdown target. Writing the check to verify D95's own work immediately found twenty dangling links, and the composition of that number is the argument for the invariant: eleven were caused by the move in progress, three by the *same session's* `.github/` relocation of `SECURITY.md` (which would have shipped broken — the author's own edit, invisible to the author), and six PREDATED the day entirely. Those six are the indictment: `probes/README.md` pointed at `../experiments/README.md`, a directory renamed to `lab/` two reorganisations earlier (D88); `design/decisions.md` and a planning packet pointed one level wrong; two lab protocols pointed at design documents that were never lab's neighbours. Every one survived two directory reorganisations and a full documentation audit because no instrument had ever looked. Resolution is per-file — the way a reader's click resolves it — with web, mail and anchor targets skipped as unresolvable rather than assumed broken. | `supported` | Same family as the mutate glob (D89) and the citation checks (D86): silent when wrong, so it needs a test rather than care. This is the fourth member of that family and the count is itself the finding — "silent when wrong" is not a rare shape in a documentation-heavy repository, it is the default one, and the standing question for any new cross-reference format is which instrument would notice it going stale. |

| D97 | The knowledge roots are two, not three: `examples/config/` became `docs/examples/` (2026-08-08). D86 gave examples its own root on the reasoning that it is documentation which RUNS, and that reasoning was weak in a way worth recording, because it was weak in a specific direction: executability is a property of the FILES, enforced by a walking test that can point anywhere, and was never an argument about location. The root was really justified by `examples/` being a family — and the family has exactly one permanent member. The check that settles it is structural rather than aesthetic: users of this platform CONFIGURE, they do not extend it. Capabilities ship inside a closed catalogue and must appear in `knownCapabilities`, so a "write your own capability" example can never be user-facing (the contributor walkthrough it would be lives in `packages/core/src/capability/README.md`); a sample decision record is OUTPUT, belonging inline in docs or as a fixture; and self-hosting examples are operator-facing, which D86 already predicted would earn a fourth root rather than joining this one. With no second member possible, four YAML files and a README are part of the CONFIGURATION DOCUMENTATION they sit beside — `docs/quickstart.md` and `docs/configuration.md` were already the only things linking to them. The redundant `config/` layer went with the move: inside `docs/`, next to `configuration.md`, the subject needs no restating. One invariant repair the move forced, and it is the interesting part: `citations.test.ts` matched repo-rooted document paths as `(design|docs|examples)/…`, and with examples no longer a top-level root that alternation began reading docs-RELATIVE link targets such as `examples/active.yml` as repo-rooted paths and reporting four perfectly good links as dangling. The alternation is now exactly the two roots that exist; relative targets are `links.test.ts`'s job (D96), and `docs/examples/…` still matches through `docs/`. | `supported` | The cost, stated plainly: a reader scanning a repository root for `examples/` finds one fewer signpost, and D86's sentence has now been restated twice in three days. Against that, a root that will never hold a second kind of thing is a category invented for one member. Deliberately unchanged: `packages/dev/checks/` and `packages/dev/lab/` stay workspace packages — both are pnpm members by necessity (their own runners, their own dependency on core's barrel, D85), and "workspace member" is precisely what `packages/` denotes. The lab carries a watch item rather than a change: 718 tracked lines of protocol documents against 318 of TypeScript, kept together because protocol 7.1 is a procedure EXECUTED by the scrubber beside it and separating a runbook from its tool is how the two drift; if the protocols keep growing while `src/` stays at two files, the split (protocols to `design/`, tool stays a package) becomes right. |
| D98 | The properties ghost was a TIMEOUT, not a counterexample (2026-08-08). Core's fast-check properties, then gathered in one `properties.test.ts` at the test root and since folded into the module suites they exercise (`packages/core/test/config/parse.test.ts` holds the heavy pair), failed three times across two days — always inside a full `pnpm -r test`, never once in isolated replay, and always under a FIXED seed. The first diagnosis (a seed-dependent collision class in the label generator) was real as a bug class and was fixed, but was already recorded as invalidated when the failure recurred; this row closes it properly. The measurement that settles it: `valid-by-construction configs parse ok` takes 1.56 s and its neighbour 1.94 s on an IDLE machine, against vitest's 5 s default — margins of 3.2x and 2.6x. `pnpm -r test` runs seven packages concurrently over core's own parallel workers, where a 3x slowdown is unremarkable. The reasoning that makes this more than a guess is the fixed seed itself: identical inputs on every run mean an input-dependent counterexample would fail DETERMINISTICALLY, including in isolation, which is precisely what never happened — while a timeout is load-dependent by construction and would behave exactly as observed. Recorded as the best-supported explanation rather than a confirmed one: the failing run's error text was not captured before the process exited, and that lost evidence is the reason this took three occurrences. The fix is 30 s of headroom applied PER TEST, not to core's vitest config, so a genuine hang anywhere else in the package still fails fast. | `hypothesis` | The generalisable lesson is about instrumentation, not timeouts: a test that fails rarely and only under load needs its failure TEXT captured on the first occurrence, because the third occurrence is no more informative than the first if nobody kept the message. Reopen if the file fails again with the headroom in place — that would falsify this row outright and point back at the generator. |

| D99 | The never-tracked rule is an invariant over LAYERS, not a fact about the lab (2026-08-08). The gap was an asymmetry, found by auditing the CI and check surface rather than by an incident: `lab.test.ts` asserted that git tracks nothing under the lab's local-only layer, and the shell — which landed a day earlier — had no equivalent, although its layer is arguably the more dangerous of the two. `packages/shell/data/` holds the operational SQLite store, which keeps the RAW webhook payload bytes until a delivery completes, and the decision journal, which names real repositories and issues; neither is scrubbed, because scrubbing is the lab's job and the shell has none. D95 already recorded the near-miss that proves this matters: for the minutes between the `git mv` and the ignore-rule update, both layers were visible to `git status`, and only one of them had a test that would have failed the build. Generalising took three assertions per layer rather than one, each catching a different failure mode: the rule is still WRITTEN (fails when someone deletes it, which is before anything leaks rather than after); the rule still WORKS, via `git check-ignore` (catches a later negation, or the D95 case exactly — a rule reading `lab/harness/` after the directory became `packages/dev/lab/harness/`); and nothing under it is TRACKED (the invariant proper, and the only one a `git add -f` cannot slip past). Verified by breaking it: staling the shell's rule fails the first two checks and passes the rest, which is the shape a stale-rule regression actually has. The file split follows D89's naming rule rather than convenience — a general invariant should not live in a file named for one of its targets — so `lab.test.ts` became `never-tracked.test.ts` for the invariant and `provenance.test.ts` for the github table lock it had been sharing a file with since D87. (`packages/dev/checks/test/never-tracked.test.ts`, `packages/dev/checks/test/provenance.test.ts`, `.gitignore`.) | `supported` | The generalisable point is about WHERE a check is filed, not what it asserts: filed under the target that first needed it, an invariant silently fails to cover the second target that arrives, and nobody notices because the original check is still green. The trigger for the next review is a third local-only layer — the adapter era will want one for credentials — which should join the table rather than earn a file. Still open from the same audit and not addressed here: `persist-credentials: false` is missing from every checkout in `ci.yml`, there is no `dependency-review-action`, and the shell's signature-verification path has no mutation gate (#39 predates the package). |

| D100 | Two CI hardening gaps from the same audit, both of them follow-through rather than new policy (2026-08-08). (1) `persist-credentials: false` was set in `scorecard.yml` and in NONE of `ci.yml`'s five checkouts, so the token was written into `.git/config` and left readable to every later step in every job. It matters most in the mutation job, whose own comment already reasons carefully about never handing fork code anything it could exfiltrate — the argument was on file and the flag was not, which is the characteristic shape of this defect class: the thinking happened, the one line did not. (2) `pnpm audit` answers "is anything we already depend on known-bad", after the fact; nothing answered "does this pull request ADD something known-bad", before merge. `actions/dependency-review-action` now does, at the same `moderate` floor so the two gates cannot disagree about what counts, and gated on `pull_request` because only a pull request has a base to diff against. The part worth recording is neither line of YAML but the invariant that followed: the `ci.yml` comment claims every checkout sets the flag, and a claim in this repository becomes an invariant — so `workflows.test.ts` grew a fourth lock, verified by deleting the flag from the mutation job alone and watching it name the exact line. That is also the honest reason the check exists: this class does not regress by someone removing a flag, it regresses by a NEW job being added without one, and the test reads the whole directory. | `supported` | Deliberately not included: a `deny-licenses` list. Denying copyleft in an Apache-2.0 project is conventional, but it is a policy decision with real consequences for contributors, and inventing one inside a hardening commit is how policy arrives without anyone agreeing to it — it wants its own decision. Also still open from the audit: `harden-runner` in `ci.yml` (informational until enforced), a secret-scanning step (check whether GitHub's native scanning is already enabled before spending an issue on it), DCO enforcement (sign-offs are inconsistent across this repo and `hiero-x402`), and the shell's signature-verification path having no mutation gate — #39 predates the package and should be widened rather than duplicated. |

| D101 | CodeQL scans this repository, committed rather than configured out of band (2026-08-08, #42). The issue's timing argument had already fired by the time it was picked up: it asked for scanning BEFORE the I/O layers arrive, and `packages/shell/` landed the day before with an HTTP endpoint that parses webhook bodies from anyone and handles a signing secret. A committed `codeql.yml` rather than GitHub's default setup, for the same reason every action here is SHA-pinned — what runs against this repository should be reviewable in a pull request. Two divergences from the `hiero-x402` reference, both deliberate: the matrix scans `actions` alongside `javascript-typescript`, because this repository makes four security claims about its own workflow YAML and that YAML deserves the scrutiny its TypeScript gets; and `paths-ignore` is passed as CodeQL's ANALYSIS config rather than a trigger-level key of the same name — the two are different mechanisms and the issue's wording conflated them, though the practical effect here is documentary either way, since both excluded trees are untracked and CI never checks them out. `security-events: write` is declared on the analyze job alone, never at the workflow level, so no other step can inherit it. Writing it exercised the invariants twice, which is the part worth recording. First, the workflow-hygiene check gave a FALSE PASS: it reads `git ls-files`, the new file was unstaged, and a check that silently skips what it cannot see is the same silent-when-wrong shape as the mutate glob (D89) — `git add -N` made it visible and the allowlist then correctly demanded the write permission be declared. Second, once visible, it failed on the author's own comment, which used the literal fork-trigger string while describing what this repository avoids; the check greps rather than parses, and the fix was to reword rather than to weaken it, because any occurrence of that string in a workflow file is worth a human look. | `hypothesis` | Unverified until the first run: whether the initial scan is clean. The acceptance criterion is that findings are triaged in the pull request — fixed, or dismissed with a written reason — not left open and unread, and this row should be amended with what the first run actually said. The pin is `v4.37.6` (`5595ccaf`), newer than x402's `v4.37.1`; the org may want them aligned. Deliberately not adopted from the reference: `step-security/harden-runner`, which stays on the audit list rather than arriving unremarked inside a scanning change. |

| D102 | Comments have a rule, and it lives in a skill rather than in folklore (2026-08-08). The trigger was a reader asking how to write a minimal docstring for `packages/core/src/config/schema.ts`, and the first honest finding was that the standard this repository had been applying all week — compress narratives, keep constraints, cite the row — was **written down nowhere**: not in `design/guides/testing.md`, not as a register row. It had been operating as practice, which is why density drifts back the moment nobody is watching. Four earners, and nothing else: IDENTITY (every exported type gets one line — a name alone is ambiguous), CONSTRAINT (break this and something fails silently elsewhere), ORIENTATION (a competent reader cannot predict what this does without running it), NON-OBVIOUS WHY (the shape looks arbitrary and is not). History and restatement never earn one; history is what this register is for, and the deletion test is "remove it — what breaks?" Two rules govern form rather than content, and both came from the reader rejecting drafts that satisfied the content rule and still read badly. First, SHORT DECLARATIVE SENTENCES, one idea each: the draft that failed welded three inferences together with an em-dash, and the fix was two plain sentences carrying identical facts — register prose in code reads as unclear even when it is correct. Second, EXPLAIN AT THE TOP, KEEP THE BODY SCANNABLE: a four-line block in the middle of an interface destroys the one thing an interface is good for, so a field comment must fit on one line or move into the type's docstring. Applied to `schema.ts`: 223 lines to 186, comments 51% to 41%, code byte-identical (proved by stripping comments from both and diffing). Three latent defects surfaced during the pass, none by a test: a `schema.md §2.2` citation orphaned above `cleanRecord` while describing `NO_CONFIG` twenty lines below; an open FINDING floating after the constant it questions; and, in the author's own first draft, the footnote kept while the fact was deleted — "Exported because…" survived and "capability names double as configuration keys" did not. Sequence joined the same skill rather than earning a second one: the two always co-fire, since reordering declarations always rewrites the comments that introduce them, and two skills with overlapping triggers means one gets picked and half the job is missed. Its rules came from the same file. Dependencies read downward — `TOP_LEVEL_KEYS` used `keyof Omit<RepositoryConfig, …>` sixty lines before `RepositoryConfig` existed, which compiles because TypeScript hoists types and is exactly why it rots unseen. Inputs before outputs. Sections in the header's own order, with banners once a file carries three concerns. And the header had been claiming "types and constants only" while `cleanRecord` sat in the middle of it. (`.claude/skills/docstrings/SKILL.md`, `packages/core/src/config/schema.ts`.) | `supported` | The rule has a home and an executable check for one part of it only: `citations.test.ts` rejects an invented D-row or a bare filename that does not exist, which is why the skill's own worked example had to stop naming files it made up. The rest — earner classification, sentence length, comment placement — is review, not automation, and pretending otherwise would be the theatre this project avoids. The trigger to revisit is the first file where the four earners produce an answer a reader still calls unclear; that happened three times while writing this row, and each time the fault was form rather than content. |

| D103 | `config/` regrouped so each file answers one question (2026-08-08). The trigger was a reader saying the directory felt disorganised, and the audit found they were right in a specific way: `schema.ts` answered three questions — what words, what shape, what comes back — while the section checker answered one and a half, holding the error CONSTRUCTOR whose TYPE lived next door. Every producer paid for that split in imports; `document.ts` read `err` from one file and `ConfigError` from another, for one concern. Four changes. `results.ts` is new and holds the error vocabulary with `err`, `Checked` and `checked`, reuniting the types with the only thing that builds them; it is barrel-exported BY NAME rather than with `*`, because `Checked` is how sections speak to each other and adding it to core's public surface by accident is exactly what `export *` does. The section checker became `sections.ts`, which is what its own header always claimed it was. The label lookup became `labels.ts`, ending a collision where the section reader for the `mappings:` block lived somewhere else entirely and always had — the two names meant opposite directions of the same relation. The section functions became `read*`, because nothing there parses: `document.ts` already did that, and reusing the prefix made one word cover two unrelated activities. `cleanRecord` and `NO_CONFIG` moved to `parse.ts`, their only consumers, which restores `schema.ts` to declarations only — a claim its header had made and then been quietly edited to stop making, when the honest fix was to move the code. The rule this settles, now in the docstrings skill: A FILE ANSWERS ONE QUESTION; when it answers two, split regardless of size, and do not split for a future you are guessing at. Size was the wrong test in both directions — `sections.ts` is the biggest file in the directory at 268 lines and the most coherent, six functions of one shape, while the error types were 35 lines and had needed their own home all along. (`packages/core/src/config/results.ts`, `packages/core/src/config/sections.ts`, `packages/core/src/config/labels.ts`, `packages/core/src/config/README.md`.) | `supported` | The costs, named. One more file in an already fine-grained directory, now seven. Two renames that broke citations in four documents including this register — caught by the citation invariant rather than by review, which is the argument for having it. And an earlier answer of mine reversed in the open: I first refused an error file as "35 lines, too small", which priced the wrong unit. A note for future rows: this one had to describe two renamed files WITHOUT naming them, because the bare-filename invariant rejects any `.ts` that no longer exists — the register is history, so the convention is to describe an old file rather than name it. The trigger to revisit is `sections.ts` acquiring a section reader that does not look like its siblings — at that point it stops answering one question and the coherence test says split. |

| D104 | `workflow/` regrouped, and the comment-drift it exposed (2026-08-09). Reading the directory with D103's coherence test, one file answered four questions: positions, causes, closure, and item state. Its own header said so — "half of it is DERIVED **and** half is owned" — which is the skill's own signal that a file is two. It is now three: `positions.ts` (derived from `config`'s facts table, nothing restated), `causes.ts` (owned; `config` has no notion of causes, because a repository maps labels to positions and never configures transitions), and `state.ts` (pause, closure, and the item's condition). `CLOSURE_CAUSES` went with the causes on a reader's challenge, correctly — it is a subset of them — while `closureReasonFor` stays on the state side because the reverse direction would be a cycle, `causes.ts` importing nothing. The split answers a question a reader had actually asked: why `workflow/` looked like it duplicated `config/`. It does not, and never did after D90 — but a file that derived half its vocabulary and owned the other half could not say so in one line, and now `positions.ts` does. FOUR ORPHANED DOCSTRINGS surfaced in one directory, which is the finding worth more than the split. The vocabulary file opened with a header describing `ClosureReason` eighty lines below, which already had its own. `transitions.ts` ended with a docstring for `applyTransition`, which lives in `reference.ts`. `project.ts` cited a config filename that has never existed in this layout. And `applyTransition` — one of the most important functions in core — was documented as "the edge tables, exposed read-only for the doc-drift check", because `PROFILE_EDGES` moved to `transitions.ts` and left its docstring behind. `transitions.ts` had also accumulated four multi-line history blocks INSIDE its edge arrays, one indented at column 1 mid-literal, destroying the one property an edge table has: that you can read it as a table. Directory totals 625 → 559 lines, comments 37% → 30%, code byte-identical throughout. (`packages/core/src/workflow/positions.ts`, `packages/core/src/workflow/causes.ts`, `packages/core/src/workflow/state.ts`, `packages/core/src/workflow/README.md`.) | `supported` | The blind spot this names: NO invariant can see any of those four orphans, because the citation checks read markdown and never `.ts` comments. Four instances in one directory says the class is not confined to `workflow/`, and a grep for history markers finds ten more across `core/src`. A check is conceivable — a comment naming a symbol that no longer exists in the file — but it would need to distinguish a description from a reference, so it is recorded as a known gap rather than promised. Also recorded: `reference.ts` had three exported symbols with no identity line at all, and the placement skill gained the rule for when a directory earns a README, derived from the four that already exist. |

| D105 | `workflow/` split by LIFECYCLE, and the callback that read as a cycle is gone (2026-08-09). A reader called the directory loopy, and measurement said the imports were not: the graph is acyclic and `causes.ts` imports nothing. The loop was somewhere else, in two places. First, one file held the directory's most-used function and its least-used one: `canTransitionIssue`/`canTransitionPr` are live — `capability/intent.ts` screens every requested position change through them — while the state-machine walk had ZERO production callers and was reachable only from tests. Nothing said which half was load-bearing, so a reader spent their attention on the half nothing executes. Second, the walk took `verdictFor` as a parameter, so a caller had to fetch `canTransitionIssue` from that same file and hand it back in. Not a cycle; reads exactly like one. Both are fixed by one move. The legality question joined the tables it queries — a table is an answer and `canTransition*` is how you ask, which is one question, not two — and the walk became `reference.ts`, a file whose name and header state plainly that no production code calls it: test oracle today, the adapter's read-back conformance checker later (D93). That let the callback die, because the spec file can now name both tables directly and expose `applyIssueTransition` and `applyPrTransition`. THE TRADE, stated because it is a choice rather than a cleanup: this accepts an EIGHTH per-flow pair, and in exchange no single-letter type parameter survives on any public signature — the generic walk stays, private. Twenty-three generic declarations existed largely to avoid that eighth pair, which was the expensive side of the bargain. The seven-going-on-eight pairs stay and are now stated as a cost in the directory's README rather than left to look accidental: entity-scoping makes a pull-request cause on an issue a compile error rather than a runtime rejection (D50), and a reader who is not told that reads deliberate repetition as mess. (`packages/core/src/workflow/transitions.ts`, `packages/core/src/workflow/reference.ts`, `packages/core/src/workflow/README.md`.) | `supported` | Costs. The test migration was mechanical and went wrong twice in ways worth recording: a non-greedy regex matched across two call sites and rewrote a pull-request call as an issue one — caught by the compiler, but only because the flows have distinct types, which is D50 paying for itself in an unplanned way — and four multi-line sites needed hand editing. ESLint then caught two dead imports the compiler was happy with, which is the narrow band that rule was adopted for. Deliberately NOT done: `closureOf` and `isPausedByProjection` in `project.ts` remain two accessors over one asymmetry, because `ObservationProjection` nests `blocked`/`closedBy` under `state` on one branch and hoists them on the other. Flattening both branches would delete both accessors and a documented trap, but it changes what `reference.ts` consumes, so it wants its own row. |

| D106 | `safety/` tidied, and the comment-drift class confirmed repo-wide (2026-08-09). The directory needed no regrouping: five files, five questions, one each, acyclic with `world.ts` as the local root. What it lacked was ordinary care. TEN exported symbols carried no identity line, including `evaluateWrite` — the front door of the whole layer — which had nothing above it but the file header. Two files ended in DANGLING docstrings for code living elsewhere: `rules.ts` closed with a block about `evaluateWrite` (in `write.ts`, whose own header already said it), and `types.ts` closed with a block about rule scope and precedence, which belonged to `rules.ts` and was absorbed there rather than deleted. `types.ts` also claimed to exist so three named files could depend on it without depending on each other, and one of those three has never existed. A stacked pair in `destructive.ts` put D60's explanation of what a warning IS onto the brand field, leaving the interface undocumented; it moved up to the type it describes. Code byte-identical throughout. The directory also gained the README audit №5 asked for eight rows ago: the two doors and why their asymmetry is information, why the wrong door is a verdict rather than a bypass (D52), and the one idea `DerivedWorld` and `DestructiveWarning` share — where a claim would otherwise be trusted, make the trusted value impossible to construct. | `supported` | The finding is now a count rather than an anecdote: across three directories this is the FIFTH file ending in a docstring for code in another file, and the fifth stale filename inside a comment. D104 raised it as a suspicion; three directories in a row confirm it, and no invariant sees any of it because the citation checks read markdown and never comments. `capability/` and `engine/` are unaudited and will almost certainly carry the same defects. Deliberately not renamed: `types.ts` is a kind-name and technically fails the placement skill's own name-for-the-question rule, but it misleads nobody looking for a verdict type, and renaming would cost citations in two rows for no reader gain — an accepted inconsistency rather than one pretended away. Process note: this row was written twice. The first attempt used a shell double-quoted python string containing backticks, the shell expanded them as commands, and the register was overwritten with 271 lines of garbage — recovered whole with `git checkout`, because it was committed. Use quoted heredocs. |

| D107 | `report/` audited: the first directory that needed nothing structural (2026-08-09). Two files and a barrel, acyclic, `finding.ts` the record and `convert.ts` the adapters that turn what each part of core already returns into one. No regrouping and no renames. It DID get a README, and the reasoning behind briefly not giving it one is the useful part: the placement skill said a directory earns a README when the relationships need more than the barrel header can carry, three lines covered it here, so the rule said skip. The reader overruled that, correctly, on a ground the rule had missed entirely — a README renders in the directory listing on GitHub and a docstring in the barrel does not, so the test was asking whether the content existed rather than whether anyone would meet it. The rule now says every source directory gets one, sized to the directory. A rule that produces a confident wrong answer on its first real prediction is worth more as a correction than as a success. Two comment defects only, both the familiar shapes: the file header opened with the story of why the module was created ("core already produced one for every decision and dropped all of it"), and `configFindings` carried a paragraph about configuration errors having once been a single undifferentiated code — true history, now stale, and D75 holds it. The central type `Finding` also had no identity line while its `code` field carried six, so the explanation moved up to the type and lost the archaeology. 262 → 256 lines, comments 40% → 38%, code byte-identical. This is also the first audited directory with NO docstring dangling at end-of-file, after five in a row. | `supported` | Running tally: four of core's six source directories audited — `config/`, `workflow/`, `safety/`, `report/`. `capability/` and `engine/` remain, and both are larger and older than anything done so far, so the base rate says they carry the same defects. The `tail -3` check added to the skill after `safety/` would have found all five dangling blocks in one command; it is worth running across both remaining directories before starting, rather than discovering them one file at a time. One methodological note for whoever runs it: that check false-positives on a barrel, whose last line is an export whose path contains a slash. Noisy, not wrong, and cheaper than the defect it finds. |

| D108 | `capability/` audited, and split at the seam its own README had been describing with an "and" (2026-08-09). Five files, six questions: `declaration.ts` answered both "what does a capability say about itself" and "which capabilities does the platform know about". The second is now `registry.ts` — the admitted set, the names `parseConfig` consumes, and the two lookups whose return types enforce the tombstone rule rather than trusting callers to consult `activeNames` (D58). The split paid immediately in reading order: `TypedDeclaration` and `declareCapability` had been sitting eighty lines below `CapabilityDeclaration`, separated by registry code, which is exactly the ordering damage interposed foreign material does. One shared private helper was the cost — `duplicates` served both the per-declaration checks and the cross-declaration name check — and rather than export it through a barrel that uses `export *`, the registry counts names itself in four lines. Twenty-one exported symbols carried no identity line, including `Intent` and `Capability`, the two types every capability author meets first; that is the largest single defect class any of these audits has found. `declaration.ts` also opened with a docstring for `PermissionGrant` wedged between the file header and the imports, describing a type that lives in `github/permissions.ts` and is already documented there in nearly the same words — the dangling-block class of D106 and D107, but at the TOP of a file, where the `tail -3` check cannot see it. Three multi-line field blocks were lifted to their type docstrings and compressed on the way: the one on `applyMappedLabel` was nineteen lines inside `IntentCatalogue`, hiding a three-key shape. `intent.ts` was reordered so the refusal-code vocabulary precedes the function that returns it. Verification: code is byte-identical in `catalogue.ts`, `boundary.ts` and `factory.ts`, `intent.ts` is the same declaration set reordered, and the declaration/registry split loses nothing but the rewritten duplicate-name check. 1075 → 1087 lines across seven files, comments steady at 32% — the first audited directory to GROW, because twenty-one missing identity lines cost more than the history removed. | `supported` | Running tally: five of core's six source directories audited; `engine/` remains. The identity-line miss is invisible to both greps the skill offers, because a symbol with no comment produces no line to match — a scan for exports whose preceding line is not `*/` would find it in one command, and is worth adding to the skill before `engine/`. Deliberately not done: `packages/core/test/capability/declaration.test.ts` still covers `createRegistry` and was left where it is, because this package's tests already do not mirror its source files (four test files for six sources in `config/`), and splitting them to match would impose a convention nothing else follows. The trigger to reopen is a second registry concern arriving — quota or ordering — at which point the test file should follow the source split rather than lead it. |

| D109 | `engine/` audited, and the one file that was two (2026-08-09). The largest file in `core/src` at 346 lines held two questions: the composition it claims in its own header, and the type-erased shape a capability is called through. The second half had external consumers who never touch the first — the shell's entry point imports only the erasure, and the shell's own types import only the erased capability — so a reader arriving with "how do I hand my capability to the engine?" had to walk the whole gate pipeline to find out. It is `invoke.ts` now, holding the erased shape, the blessed cast, the resolver source type, and the handle a capability is given. The composition keeps the gates, because a gate is a step of the pipeline rather than a separate question, and splitting it would put the pipeline and its steps in different files. THE SPLIT FORCED A BARREL DECISION and that is the part worth recording: the handle had been a private class, and moving it made it an export whether anyone wanted one or not. The barrel now names its exports instead of `export *`, following the `Checked` precedent (D103) — a shell that could construct a handle could answer a capability's resolver queries for it. EIGHT exported symbols carried no identity line, `decide` itself among them, which is the `evaluateWrite` shape from D106 repeating in the front door of the whole package. The barrel header named a file that has never existed in this directory: it credited the safety derivation to a source file, when the derivation moved into `safety/world.ts` during D92 phase 4 and the header was never updated. That is the SIXTH stale filename in a comment across four directories. The same residue had stranded two test files — the normalizer's suite still sat under `test/github/` a phase-5 move later, and the derivation's suite sat under `test/engine/` testing neither engine file — both now mirror what they cover, with the real captured fixtures deliberately left under `test/github/` because observed GitHub facts are that directory's business. The directory also gained the README every sibling already had. (`packages/core/src/engine/invoke.ts`, `packages/core/src/engine/README.md`, `packages/core/src/engine/decide.ts`, `packages/core/src/engine/events.ts`.) | `supported` | Costs and cautions. The handle's constructor narrowed from the whole externals object to just the resolver source, which is a CODE change inside what was otherwise a reorganisation — small, and the honest alternative was a type-only cycle between the two new files, but it is the kind of change a comment pass is supposed to refuse and it is recorded rather than folded in silently. `EngineObservation` and `DecideInput` stay barrel-exported although nothing outside core names them, because they are the front door's own signature and a caller building the argument should be able to name its type. Deliberately not done: no third file for the gates, and no attempt to reconcile `invoke.ts` with `capability/boundary.ts` — the two are the erased and typed sides of one boundary, the headers now point at each other, and merging them would put the engine's implementation inside the layer it calls. Process note: this audit ran CONCURRENTLY with another session editing `capability/` and `github/` in the same working tree, the audit that became D108, which reverted two of these files mid-pass and briefly produced a duplicate export. Recovered by re-applying, but the lesson is the obvious one — two agents, two worktrees. |

| D110 | Canonical report persistence and delivery completion are one store transaction, and the owned database has an explicit version contract (2026-08-09). The forcing crash was between two individually durable writes: shell appended a filesystem report, then separately marked the delivery done; retry saw pending work and appended the same report again. The split sources of truth are gone. `completeDeliveryWithReport` rechecks GUID, event name, payload digest, `processing` state, and current claim token under `BEGIN IMMEDIATE`; inserts one `delivery_report` row keyed by GUID; marks the delivery done and clears its payload; then commits. The report row retains the committing token, so same-token/same-JSON retry returns `alreadyCompleted`, while released, stale, or stolen tokens return `notOwned` and same-token/different-JSON returns `reportConflict`. No filesystem projection follows the commit; SQLite is the only canonical report authority. This keeps authority in the store without moving a decision into I/O: core still constructs the report, shell still owns ordering, and store owns only the durable state transition. The same change establishes `PRAGMA user_version = 4` as the smallest SQLite-native schema marker. Version-zero files are accepted only when their full table/column fingerprint matches one of the three schemas this repository actually created; ordered migrations and each version update run in one transaction; unknown shapes and versions above 4 fail closed. (`packages/store/src/schema.ts`, `packages/store/src/store.ts`, `packages/shell/src/processor.ts`.) | `supported` | The owned schema grows from the four-table recovery minimum proved by 6.5 to five tables for a boundary that experiment did not exercise. No foreign key is added; finalization is the explicit two-table transactional exception to the former independent-hot-path rule. Migration cannot manufacture facts: identity-only deliveries become completed legacy identities with unknown event/digest and therefore conflict on redelivery; original journal rows get attempt 1 and revision `legacy:unknown`; tokenless running schedules return to pending; deliveries completed before version 4 may have no report. The one-report guarantee is exactly for version-4 finalizations. Retention must delete the delivery and report together. Proof is at the real boundary: exact old-schema fixtures, interruption after every migration, worker exits after report insert/delivery update/commit, and two separately connected worker threads racing stale and current tokens. Reopen if report retention diverges from delivery retention or if a second report consumer needs a query/export contract beyond `deliveryReports`. |
| D111 | The test tree's naming conventions became a lock (2026-08-16). The branch had established the rules one commit at a time — specs mirror the modules they hold to account, test support is named for its question, shared helpers graduate to the testkit — but every rule lived in commit messages and docstrings, and this repository's own standard for structure is the test that would fail if a directory drifted from its description. `packages/dev/checks/test/placement.test.ts` now locks the tree's navigability: in any package with a `src/`, a spec either carries the name of the module it specs or is registered in the check with the question it answers (listed rather than discovered, the workspace-file shape); a registration expires the moment a mirror module appears and fails when its file is gone; and no file under any `test/` is named by kind, the ban the source tree already lives by. Three renames brought the tree under the rule before it was enforced: the checks' shared support module was named for its kind and is now `repository.ts` (the question it answers: how does a check see the repository?); the shell's slice spec now carries `shell.ts`'s name, as the mirror rule this branch itself established requires; and the label-lookup spec carried the name of a config section it never touched — the test-side survivor of exactly the D103 confusion, now `labels.test.ts` beside the module it specs. The check is one-way on purpose: a src module without a same-named spec is not an offence, because whether its behaviour dies in a sibling suite is mutation coverage's question (D82), not naming's. (`packages/dev/checks/test/placement.test.ts`, `packages/dev/checks/test/repository.ts`, `packages/shell/test/shell.test.ts`, `packages/core/test/config/labels.test.ts`.) | `supported` | The deliberately-declined half is recorded in the testkit's header: suites that make their directory in `beforeEach` keep calling `mkdtempSync` directly, because `afterEach` runs when a test fails, so the leak `withTempDir` exists to close never opens there and a wrapper would rename two lines without removing either. The named risk is the registration list drifting into an allowlist of convenience — it holds ten entries at birth; the trigger to revisit is the first entry whose recorded question is really one module's name wearing a story. |
| D112 | The three packages that never ship are filed under `packages/dev/` (2026-08-16). The trigger was readers counting seven equal packages and stopping there: the two-tier structure — a delivery path and the scaffolding that holds the repository honest — existed only in prose qualifiers, and `ls packages/` contradicted every sentence that explained it. Now the listing says it structurally: `core`, `store`, `shell`, `probes`, `dev/`. The move cost almost nothing where it looked expensive, because the repository's own conventions had already paid for it: every check derives its package list from the workspace file, so three edited entries carried `placement`, `toplevel`, and the manifest gate unchanged, and the architecture gate computes a package's ROLE as the basename of its directory, so the layer table never noticed the filing. The one genuine subtlety was the cruiser: its rules are path-keyed, and the negative-control fixture tree mirrors packages FLAT under its own root, so every role pattern gained an optional `dev/` segment — one pattern governing both shapes is also the proof the rules are about roles, not about where a role is filed — and the barrel rule's `$1` now captures the importing package's full directory including its `dev/` segment, without which the three dev packages would have collapsed into one unit the rule could not see between. The lab's ignore rules moved in the same edit as the lab, and `git check-ignore` confirmed the new paths before anything else changed — the D95 window, this time held shut. The checks' own `repoRoot` climb gained a fourth level, and the link invariant caught the five outbound relative links the sweep missed, which is the argument for having it. (`packages/dev/checks/test/repository.ts`, `packages/dev/checks/test/placement.test.ts`, `.dependency-cruiser.cjs`.) | `supported` | Deliberately not moved: `probes/`, although it is also non-production, because it is already scheduled to leave at stage four and filing a package under a new home weeks before deleting it is churn with no reader. Register and audit rows keep their old paths — the register is history. Process note: the repository checks read `git ls-files`, so with the move unstaged they see the old tree and fail on paths that no longer exist; verification ran with `GIT_INDEX_FILE` pointed at a scratch copy of the index with the renames staged, leaving the real staging area untouched. The trigger that reopens this shape is the second shipped capability: `packages/capabilities/` would then join `dev/` as the second group, the workspace top level closes as spine-plus-groups, and the optional-segment pattern in the cruiser is already the pattern that filing needs. If `dev/` ever acquires a package that ships, the name has failed and the filing is wrong, not the rule. |
| D113 | The citation invariant no longer derives its idea of a path from the current layout, and the duplication audit's confirmed findings are applied (2026-08-16). The forcing discovery: D112's move left thirteen register citations pointing at the packages' old homes, and the check that exists to catch exactly that said nothing — its path matcher built an alternation from the workspace file, so a citation of a MOVED package stopped matching instead of turning up dangling. A checker that adapts to the layout it is supposed to police forgives precisely the change it should flag. The matcher in `packages/dev/checks/test/citations.test.ts` is now layout-independent — any `packages/…/(src|test)/….ts` shape is a citation and existence is the only judge — with a negative control asserting a pre-move path still MATCHES. Hardening it immediately surfaced fifteen more invisible citations: the register names `packages/executor/src/planner.ts`, `packages/executor/src/policy.ts` and `packages/executor/src/recovery.ts`, which are designed, not built. Those are now an explicit `PLANNED_PATHS` set with the same self-cleaning arrival test the NAME check gives planned filenames, so the exemption cannot outlive its future. Alongside: the checks' shared module `repository.ts` now owns `docsDir` and `exampleFiles()` — the D112 move had to edit four hand-walked `../../../` derivations one by one, which is the cost this removes; two tests in `document.test.ts` that rebuilt corpus rows byte-for-byte are gone (each asserted strictly less than the corpus driver already does), one of two `capabilityUnknown` rows differing only in the unknown name is merged into the survivor; `builders.ts` gains `triageConfig` (ending two byte-identical local wrappers) and a `settings` parameter on `configWith` (the one field probes' generator had that it lacked), adopted across `boundary.test.ts` and `scenario.test.ts`. | `supported` | Deliberately kept: the corpus's mild near-duplicate rows — its own policy tolerates several shapes per code, and trimming there buys little; probes' `configEnabling` stays a separate generator (the cycle refusal recorded in the testkit header, and a genuinely different shape). The stated count in `docs/examples/README.md` was the one unlocked fact in a compiler-locked paragraph and is now no count at all. Two known gaps carried, not closed: the architecture fixture tree exercises every cruiser rule NAME but not every regex branch (a `test/`-path importer, the `checks` alternatives), and "no HMAC outside `packages/core/src/github/signatures.ts`" is a convention no check reads. Reopen the first if the rule set grows past what the name-set assertion can honestly cover. |
| D114 | The testkit gained the hook-shaped temp-dir helper D111's declined half refused (2026-08-16). That refusal weighed a wrapper that would rename the `beforeEach` pair without removing it; by the time the shell's suites joined the store's, six files carried the identical lifecycle — the two hooks, the two `let` declarations, and the `node:fs`/`node:os`/`node:path` imports behind them — and `useTempDir` deletes all of it rather than renaming any of it: a suite keeps only its own work in its own hooks, and the handle's `dir`/`file()` reach the current test's directory. Registration order carries the safety the hand-rolled pairs had: called at the suite's top, its `beforeEach` runs before the suite's own and, `afterEach` being LIFO, its removal runs last — after the suite has closed its SQLite handles. `withTempDir` stays as the callback form for life that begins and ends inside one test, which is the interleaving suites' shape. The testkit header's declined-decision paragraph is rewritten to this boundary; the six suites are migrated with their prefixes and every assertion unchanged. (`packages/dev/testkit/src/index.ts`, `packages/store/test/store.test.ts`, `packages/store/test/schema.test.ts`, `packages/store/test/delivery-intake.test.ts`, `packages/store/test/delivery-finalization.test.ts`, `packages/shell/test/processor.test.ts`, `packages/shell/test/shell.test.ts`.) | `supported` | D111's declined half is superseded, not falsified: its arithmetic was honest at the count it was written against, and what changed is the count and the shape of the wrapper — deletion, not renaming. The named risk is the getter: `dir` outside a test throws by design, so the misuse to watch for in review is a suite caching the path across tests instead of reading the handle. |
| D115 | **Five of the eight candidate capabilities cannot be built on the catalogue as it stands** (2026-08-17). Converting the candidates from prose to the declaration anatomy forced each private vocabulary onto the real catalogues, and the invented type names had been hiding a gap: `assignment` has no assign operation (`unassign` is the only one, and `issueUpdated` carries no assignee, comment body, or actor); `inactivity` has no close operation, because closure is a reason READ from GitHub and never written (D47), so "warn then close" is not an available final action — the choice is warn, or warn then unassign; `notifications` has no external-delivery operation and no observation for its workflow-failure subscription; `review-routing` has no request-reviewers operation and no ownership resolver; `progression` cannot write a level at all, because `skill:` labels sit outside `MAPPABLE_MEANINGS`, making `applyMappedLabel` a compile error before it is a policy question; and `admin`'s propose-configuration-change is neither catalogued nor an approved adapter operation. Recorded per candidate as a §8 catalogue-review row. **The coupling constraint the same pass surfaced:** `ready` has three writers — `intake` via `triageCompleted`, `assignment` via `lastContributorUnassigned`, `inactivity` via `reclaimCompleted` — which is lesson A1's shape (a shared position no single capability owns) and A3's pair (assignee and position mutated together by separately togglable features). `ready` needs a named owner and one documented edge per writer before any two of those three can be ranked together. Every other meaning has at most one writer, and `blocked` is read-only everywhere (D79). | `supported` | The cost of finding out this way rather than at build time is one day of document conversion; the cost of the alternative was five capabilities whose headline write does not exist, discovered by a contributor mid-implementation. Q2's ranking conversation now has a hard input it lacked: a candidate whose central operation is uncatalogued cannot be ranked first without also funding the catalogue work. Deliberately NOT resolved here: whether the missing operations are added (a D61-class catalogue review) or the candidates are rescoped to what exists — that is maintainer and stage-four territory. |
| D116 | The 2026-08-20 review corrected D115's inactivity and shared-writer clauses without changing its other catalogue findings. The admitted inactivity probe declares only `postManagedComment` and `unassign`; it declares no `applyMappedLabel`, so it cannot perform `reclaimCompleted` or write `ready`. Its scheduled observation also carries no projection, so both direct intents claim an open item that the engine cannot verify and are refused `preconditionStale`. Independently, the platform derives `unassign` as `reversibleStateChange` from `INTENT_OPERATIONS`; a capability cannot elevate it, so the clock-triggered destructive door and its warning/grace gates are unreachable. Inactivity is therefore neither a current `ready` writer nor a safely buildable automatic reclaim. The shared `ready` question currently has two candidate writers — intake and assignment — and inactivity joins only if a later catalogue review adds a mapped-position outcome. (`packages/probes/src/inactivity.ts`, `packages/probes/test/engine-matrix.test.ts`, `packages/core/src/capability/catalogue.ts`.) | `supported` | Stage four must choose a safe first scope (report-only or warning-only), decide how scheduled work obtains authoritative projection at decision and act time, and add a clock-triggered operation rather than routing an automatic reclaim through the ordinary reversible path. The assignee and position outcome needs one owner or one explicit combined operation before inactivity can join the `ready` transition. |
| D117 | D39's kill-switch conclusion is local to `evaluateWrite`, not the composed engine (2026-08-21). The safety preflight does refuse observation-class intents before the observation short-circuit, but `decide()` invokes enabled capabilities and their declared resolvers before it has an intent to gate. Repository mode `disabled` is likewise an intent verdict, not an evaluation stop, and `observe` and `dry-run` currently share the same `modeRecordsOnly` output path. The shell separately intercepts `active` before `decide()`, so even `KILL_SWITCH=1` does not replace that result with `killSwitch`. Documentation now names these boundaries rather than presenting the narrower verdict order as a transport-wide emergency stop. (`packages/core/src/engine/decide.ts`, `packages/core/src/safety/rules.ts`, `packages/shell/src/processor.ts`.) | `supported` | Stage four must decide whether the operational brake must stop transport intake, capability evaluation, resolver reads, only effects, or some layered combination. If total stop is required, it belongs before evaluation and needs explicit exceptions for durable intake and operator/security reporting. The eventual distinction between `observe` and `dry-run` also needs a concrete report or rollout contract before the modes can promise different output. |
| D118 | `DecideExternals.latestHumanChangeAt` accepts a promise (2026-08-26): the lookup returns `HumanChangeOrdering \| Promise<HumanChangeOrdering>`, awaited inside `gateIntent` — now async, absorbed by `decide()`'s existing async signature — because the live answer is a timeline read that cannot be prefetched: the item exists only after `capability.evaluate()` returns, inside `decide()`. The union rather than a bare `Promise` is what kept D93's "the shell does not change" literally true — `stubbedExternals` and every test's synchronous lambda satisfy the seam unchanged, while the adapter's `liveExternals` (#134) will return a promise. A lookup that returns a promise is still a lookup, not I/O in core. The await sits after the screen, so a screened-out intent costs no read. Alternatives — a two-pass `decide`, hoisting normalization out of it, answering from the store's delivery history — were rejected in #111's design comment and stand. (`packages/core/src/engine/decide.ts`.) | `supported` | #134 fills the seam: grants from the mint response, timeline-derived ordering answering `"unknown"` on any failed or over-budget read (D51), memoized within a delivery and never across one. |
| D119 | Live issue ordering (#134): count `User` events `labeled`, `unlabeled`, `assigned`, `unassigned`, `closed`, `reopened`; ignore known bots (bot-vs-bot remains manual-edits.md §9). Missing actor identity or an unreadable counted timestamp answers `"unknown"` (D51). Exclude at most one cause matching item number, action, label/assignee target, sender and `updated_at` second. Other actions still count, including identical same-second actions (D33); an incomplete cause excludes nothing. Read page one, then descend from the last page, under a three-call cap. A next link without a known last page answers `"unknown"`. A find in the visited newest block is authoritative; a page-one find with unvisited middle pages is not. Only complete coverage can answer `null`. Memoize per item within one delivery, sharing concurrent reads; never across deliveries. ETags revalidate with GitHub and reduce quota, not freshness. This fills manual-edits.md §10 for issue items; fixtures remain synthetic pending a recorded capture. (`packages/adapter/src/externals.ts`.) | `supported` | Extend the six-kind list when a new intent writes another surface; the register, not the type system, enforces that obligation. PR commit activity remains unaddressed until a PR-writing capability exists. |
| D120 | Live bootstrap (#134): `main.ts` selects adapter facts when `APP_ID`, `INSTALLATION_ID` and `PRIVATE_KEY_PATH` are all present; a partial set fails before listening. No credentials selects CI stubs. One token source, mint and HTTP client serve delivery-scoped `liveExternalsForDelivery` calls. The processor requests externals per delivery (`ExternalsForDelivery`), binding the ordering memo and D119 cause to that delivery. Failed setup releases the claim for retry, never substitutes stubs or decides on unavailable facts; this also makes ignored deliveries depend on auth availability. The mint must own its POST because its App assertion creates the token the shared client needs. It retains the same pinned origin, refused redirects, timeout and classified no-throw outcomes. (`packages/adapter/src/mint.ts`, `packages/adapter/src/externals.ts`, `packages/shell/src/main.ts`, `packages/shell/src/processor.ts`.) | `supported` | #134 closes when this lands with D118 and D119. The dependency rule held: the adapter enters the shell at `main.ts` only, which the cruiser enforces. `githubConfigSource` and the resolvers remain the stubs still named in the shell README's hole table. |
| D121 | The scale pass over the adapter, done and declined (2026-08-26). MOVED: `lastPageFromLink` — pagination is the HTTP client's vocabulary (its cache retains `link` for exactly this read), so it lives in `http.ts`, where every future paginated operation finds it instead of importing it backwards from `externals.ts`. EXTRACTED: `untrusted.ts` — the total readers for GitHub's bytes (`field`, `jsonRecordOf`, `jsonArrayOf`), consolidating the two dialects that had already appeared; response-body parsers use this one idiom so the next operation copies a pattern, not a variant. DECLINED, with the triggers that reopen each: unifying the mint's transport with the client (the auth models differ on purpose — the assertion cannot travel through the client that needs its product — and one bounded sibling that changes never is cheaper than a shared layer both must trust; reopen if a THIRD transport path appears); splitting ordering evidence out of `externals.ts` (one file still answers one question — which external facts GitHub answers live; reopen when a third live fact arrives); sharing the fetch/timeout seam-option trio across option interfaces (two copies; reopen at the third). (`packages/adapter/src/untrusted.ts`, `packages/adapter/src/http.ts`.) | `supported` | The clarity skill (`.claude/skills/clarity/`) records the narration test and the one parsing idiom this row consolidates; a new operation that parses in a private dialect is the drift signal. |
| D122 | The config seam speaks typed outcomes, and absence is corroborated (2026-08-26, #133 via #149 review): `ConfigSource.load()` returns `ConfigLoadOutcome` — a document, or a failure carrying the ONE fact the processor branches on, `permanent` — restoring the no-throw seam rule #149's draft had weakened in the guide rather than deciding. Permanent means the COMMITTED FILE is defective (not a file, contents arriving `encoding: "none"` past 1 MB, invalid base64 or UTF-8): the processor completes the delivery as `configRejected`, reporting `documentUnparseable` with the unreadable detail in the message — the catalogue admits only document-demonstrable codes (D76), and an unreadable file is the parse failure's upstream twin. Every other failure is transient — released and retried — including a bare 404, because 404 is `notFoundOrNotInstalled`, never confident absence (D51). Absence is CORROBORATED: config 404 plus a visible repository (one extra, cacheable read on that path only) is the live equivalent of `fileConfigSource`'s ENOENT. The seam interface lives in `core/src/config/source.ts` beside the document it returns; the shell moved because the seam's CONTRACT moved, the only occasion it may (guide rule now says so). The thrown error class, the dead `unsafeRef` guard, and the operation-level re-guarding of the client's never-throw contract were removed with it. (`packages/adapter/src/config.ts`, `packages/core/src/config/source.ts`, `packages/shell/src/processor.ts`.) | `supported` | Follow-on stages of the same review: one revision scheme across both sources, then memoizing corroborated absence — today a config-less repository costs two reads per delivery, accepted briefly and fixed next. Also recorded here: at Q10's fleet design point (50×20 issues) the ETag cache's 1,000-entry bound equals the timeline working set alone — the fleet stage revisits `DEFAULT_ETAG_CACHE_ENTRIES` before conditional reads quietly stop being free. |
| D123 | `linkedIssues` means `closingIssuesReferences(excludeUserLinked: true)` and initially supports same-repository references only (2026-08-30, protocol 6.8). A private App-auth sandbox confirmed same-repository and visible cross-repository closes, but also found the unsafe ambiguity: an inaccessible target repository and a missing Issues grant both return a clean empty connection, exactly like an unlinked pull request. The resolver therefore checks live `issues: read` and `pull_requests: read` grants before querying and returns unknown when either is absent; within that prechecked scope, any GraphQL error or malformed shape is unknown. Cross-repository support stays closed because the current item carries no target-repository identity and GitHub does not disclose a hidden target in the connection. Every valid query cost one point. (`design/findings/linked-issues.md`, `packages/dev/lab/protocols/6.8-linked-issues.md`.) | `supported` | Reopen cross-repository support only with a repository-aware contract and a way to prove every target is visible; an empty connection alone can never supply that proof. Manual links remain excluded deliberately and were not re-probed under App auth. |
| D124 | The #111 shell diff is composition plus seam-contract adaptation, not composition-root only (2026-08-30). #139 and #142 made no shell change, #144 introduced delivery-scoped externals, #149 introduced typed config outcomes, #151 was evidence only, and #153 changed shell tests and documentation only. No GitHub operation or decision rule entered the shell. The sequence added no CI credential and tracked no lab credential or private evidence. `core/src/github` stays in core: four files across capability, safety, and engine still depend on its permission vocabulary, so a split would add core's first internal workspace dependency without reducing coupling. The directory remains a one-way root and is smaller than `config` and `workflow`. (`.github/workflows/ci.yml`, `packages/dev/checks/test/never-tracked.test.ts`, `packages/shell/src/main.ts`, `packages/shell/src/config.ts`, `packages/shell/src/externals.ts`, `packages/shell/src/processor.ts`, `packages/core/src/github/README.md`.) | `supported` | Reopen the package split when core has almost no imports from the directory, when the permission vocabulary moves, or when the directory becomes core's largest. |
| D125 | Managed-comment identity is platform-owned. Core derives the marker — schema version, capability, and comment kind — and it carries the effect id, the shape protocol 6.5's harness proved — implemented at `packages/core/src/capability/managed.ts` (the harness itself is local-only by design); capabilities supply body content only. A marker counts only under App authorship, so a marker copied into a repository user's comment can never trigger an operation. `prQuality`'s config-supplied `marker` key is removed. Answers Q9. (Evidence: 6.5's duplicate-on-blind-retry in `design/findings/storage-decision.md`; `design/guides/managed-output.md` §4's metadata rules; the D12 ownership split that the capability-supplied string in `packages/core/src/capability/catalogue.ts` contradicted.) | `supported` | Stage A implements it; D12's managed-comment sandbox test — idempotent update plus the authorship check — is what ratifies it. |
| D126 | The minimum architecture is ratified, scoped to ring-zero (2026-09-02, Sophie Bulloch / exploreriii — the stage-four gate). What is ratified: the five-package layout and its cruiser-enforced layer policy; the owned single-file store, its v5 schema and DELETE+FULL crash model (per the storage decision's approving review of the same date); the safety ladder as ordered rules with the branded derived world; and the effect path's five-stage shape — identity at approval, write verbs behind a structural allowlist, journal-before-send, apply-time re-derivation, read-back reconciliation. The scope boundary is the pilot gate: enablement beyond the personal sandbox requires the volunteer repository's maintainers and the named operator (Q1/Q13), per P8. | `ratified` | Extending the scope past ring-zero is the pilot gate's question, answered by its named signers — never by this row alone. |

### Adoption record — 2026-07-25

Sophie (exploreriii), as project architect, adopted the following rows as **working architecture**: strong
enough to guide the next work (`supported`), with formal `ratified` status still requiring the stage-four
review per §5. Adoption fixes the working values; stage four may revise them.

- **Storage and recovery**: the storage decision's closures (D1, D13, D24, D27, Q15) stand as working
  architecture; formal close-out happens at stage four. D41 adopted with a **15-minute lease**; D42
  adopted; D43 adopted with **requeue threshold = 2× lease** and **90-day retention** for `seen_delivery`
  and done journal rows; D44 adopted at **5 attempts**; D45 adopted as **operator action** for the first
  version; D46's precondition accepted, with the staleness measurement **gating `active` mode**.
- **Safety and taxonomy mechanics**: D29 and D33 adopted as encoded.
- **Operations**: D40 adopted with a **quarterly** re-probe cadence; the fixture-refresh owner falls out
  of Q13.

Not adopted — deliberately left `hypothesis` for the stage-two maintainer conversations and the named
reviews: D28, D30, D31, D34, D35, D38, D39 (including D39's security-control sub-question for D22). The
working defaults these rows encode remain in force until those reviews answer; the retired
ratification packet's §7 carried the questions (deleted 2026-08-16, in git history).

**Subsequent evidence (2026-07-28):** the PR #25 risk review reopened D41. The 15-minute working value is
not an exactly-once proof; live lease takeover remains merge-blocking until the overlap contract and oracle
named in the row exist. The other adoption entries are unchanged.

**Rows added after this record.** D47–D50 (2026-07-29) postdate the adoption above and are **not** covered
by it. D50 is mechanical and needs no review; D47, D48, and D49 join the workflow-profile agenda and the
stage-two questions the retired ratification packet collated in its §3 and §7. They arrived from a
different source than D28–D46 — not from coding ambiguous prose, but from reading the finished tables back
against `design/audit/`, which found an edge missing from the design document itself. That suggests the audit is
worth a second pass against every table the implementation now encodes, not only this one.

**Audit rows — 2026-07-30.** D51–D59 came from a deliberate adversarial read of `core/`, and their source is
worth recording because it differs again: not ambiguous prose (D28–D46) and not the audit of existing Hiero
automation (D47–D50), but asking of each module "what does this claim, and can I make it false?". Four of the
nine were live defects rather than unstated choices — D52 (a destructive request could be applied through the
general write path), D51 (unknown ordering read as no conflict), D57 (org-scoped intents falsely rejected),
D58 (the retirement tombstone unenforced). D52 is the one to learn from: it survived 152 passing tests,
including a sweep advertised as exhaustive, because the sweep was exhaustive in seven of eight input
dimensions and the omitted dimension was exactly where the defect lived. The lesson for
`design/guides/testing.md` tier 2 is that "exhaustive" must name the dimensions it enumerates.

**Rows added after this record (2026-08-05).** D61–D73 come from a fourth source: building the seam BETWEEN
the finished packages rather than auditing any one of them. `core/`, `store/`, and `executor/` were each
verified against their own source documents, and the only coupling between them was a branded id and a type
import — nothing had ever run a capability decision through to an effect. Writing the runtime boundary
(`packages/core/src/capability/`), the intent-to-plan translator (`packages/executor/src/planner.ts`), and three disposable
probe capabilities (`probes/`) surfaced eight gaps that no further work inside a single package would have
found, because each package was individually correct. D73 is the clearest of them: four separate rows now
close the same shape of defect — a fact stored twice, free to disagree — and reading them together showed the
fourth instance had never been closed at all. Two of them (D62, D64) are cases where the safety
engine and the capability contract had been written against each other and did not meet. One (D67) was
found by a failing test rather than by reasoning, and is recorded that way deliberately. The probe
capabilities are throwaway — see [`probes/README.md`](../packages/probes/README.md) — but these rows are not.

D72 deserves its own note, because neither change produced it alone: the 2026-07-30 audit made destructive
warnings branded and immutable (D60), this branch made a destructive intent carry its own warning (D64), and
only merging the two exposed that a branded object cannot cross the store a warning must actually cross.
Two individually correct changes, one gap between them — the same shape as the rest of this block, one level
up. It is worth expecting more of these wherever an audit and a seam meet.

None of D61–D73 depends on the stage-two ranking, on D32's precedent: capability *choice* waits for
maintainer data, but the capability *boundary* names no capability. They are collated for review in
the retired ratification packet's §2b — the stage-four architecture session, not the
workflow-profile one. Only D69 reaches into the first-capability choice, as a gate recorded in the
retired stage-four review packet's §7.

**D74 and D75 (2026-08-05) are a different kind of row again.** They are not defects and not boundary
decisions: they are the two cross-cutting inconsistencies that a read of the whole package surfaced, where
every individual choice is locally defensible and the aggregate costs a conversion or an adapter at every
seam. They are proposed together with D73's recommended derivation in
the retired ratification packet's §2c, because all three are the same request: let stage
four decide the simplification BEFORE the code is written, rather than ratifying guards that a later
refactor would retire.

D110 supersedes D93's filesystem-report persistence and projection clauses. D93 remains the historical
record for the shell boundary, configuration location, and first-slice scope; SQLite is the only canonical
report source.
The implemented D110 fingerprint is stricter than the row's original table/column shorthand: it matches
exact owned table and index definitions and rejects extra active schema objects. Canonical reports are
readable in deterministic order through `deliveryReports`; no automatic filesystem projection is built.

## 4. Open product and engineering questions

| Identifier | Question | Decision owner or evidence | What the answer blocks |
|---|---|---|---|
| Q1 | Which organization hosts and operates the GitHub App? | The Hiero or LFDT infrastructure owner must decide. | Production deployment is blocked. |
| Q2 | Which maintainer problems become the first user-facing capabilities? | Maintainer demand and the capability review must decide. | The user-facing minimum viable product is blocked. |
| Q3 | Do any repositories want the optional skill ladder, and what counts as a completion? | The affected repository maintainers must decide. | Skill-based assignment and progression are blocked. |
| Q4 | Which human actions may automation ever refuse or reverse? | Maintainers must review the selected capability policies. | Manual-edit enforcement is blocked. |
| Q5 | How does assignment behave with native assignment and multiple assignees? | The assignment specification and sandbox scenarios must decide. | The assignment capability is blocked. |
| Q6 | Is review queue state stored, derived, or left to native GitHub review rules? | Repositories that request review routing must decide. | Review routing is blocked. |
| Q7 | How are old labels, configuration, and writers migrated without two systems writing the same state? | A per-repository migration plan must decide. | Existing repository cutover is blocked. |
| Q8 | Which repository volunteers for a read-only or reversible pilot? | Repository maintainers and the operator must decide. | Ring-one testing is blocked. |
| Q9 | What is the managed-comment marker and schema migration format? | Answered by D125 (2026-09-02): the marker is platform-owned and carries schema version, capability, comment kind, and the effect id — the shape protocol 6.5's harness proved. Migration policy for a v2 schema remains open until a v2 exists. | Unblocked: the managed-identity module (effect-path stage A) can be built against D125. |
| Q10 | What rate and timeline-read budget is safe for the fleet? | Answered by experiment 6.4 (2026-07-23): every call costs one unit of the 5,000-per-hour installation budget, conditional 304 re-reads are free, and content creation hits an unsignaled secondary limit near eighty writes per minute — write pacing must stay well below that, serialized per GitHub's guidance. A sweep of fifty repositories with twenty active issues each costs about one fifth of an hour's budget uncached; with conditional reads, steady-state cost tracks changed issues only. | Unblocked: sweep cadence can be designed against these numbers. |
| Q11 | Is the App installed only on selected repositories or more broadly with repository configuration as a second gate? | Organization governance must decide. | Installation guidance is blocked. |
| Q12 | Which implementation documents should be written alongside working code? | The first implementation work packet must decide. | This question does not block feasibility work. |
| Q13 | Who owns architecture review, the development App, the platform components, testing, and rollout? | Sophie and the project maintainers must name owners. | Committed dates and production work are blocked. |
| Q14 | What path, strict YAML schema, and schema migration policy should the App use? | The path is decided: `automations.yml` at the repository root (D93, 2026-08-07) — the file configures the platform, not GitHub, so it does not live in `.github/`. Experiment 6.3 (2026-07-23) validated the mechanics (path-independent): strict parsing, unknown-key rejection, and `schemaVersion` rejection all fail closed, and a default-branch config change is event-observable within seconds. Inheritance is deferred from the first version. | Only the schema migration policy remains blocking. |
| Q15 | What minimum owned operational storage is required, and which technology should provide it? | Answered for recovery by experiment 6.5 (2026-07-23): a single-file SQLite store with four small tables — seen delivery ids, effect intent/done journal, claims, schedules. D110 adds a fifth canonical-report table for atomic delivery completion, a boundary the recovery experiment did not exercise. GitHub keeps all effect outcomes and resolves every sent-but-unconfirmed write. See `design/findings/storage-decision.md`; ratification pending per §5. | Unblocked: production recovery and hosting design can proceed from the storage decision. |
| Q16 | What exact narrow operations and typed results belong in the first adapter? | Answered by the endpoint and permission matrix (`design/findings/endpoint-permission-matrix.md`, completed 2026-07-23): every operation the first capabilities need is confirmed with its permission, quota cost, conditional-read support, and failure shape; the matrix rows are the first adapter's operation list, and the failure catalogue fixes the error types it must expose. | Unblocked: the adapter implementation can be specified from the matrix. |
| Q17 | What deployment shape must hosting provide? Six sub-questions, absorbed 2026-08-17 from the retired architecture prose: how the webhook endpoint answers inside GitHub's time limit; whether a durable queue is required before acknowledgement; how failed and missing deliveries are found; whether one or several App processes may run at once; how primary and secondary rate-limit headers affect scheduling; and how long operational and audit records are retained. | Partly narrowed already: the storage decision (Q15) requires a persistent single-writer disk, which rules out stateless and multi-writer shapes; P9 fixes the durable-accept-before-acknowledge ordering; Q10 measured the rate budget. The retention windows were proposed (90 days, D43) but never ratified, and missing-delivery detection has no answer. The remainder needs an operator and a hosting owner (Q1, Q13). | Production deployment and the operator surface are blocked. |

## 5. Ratification rule

A hypothesis becomes `ratified` only after the register names the approving maintainers, the date, and the
evidence they reviewed. A sandbox result may reject a hypothesis without selecting the final replacement.
When that happens, the row becomes `reopened` until a later decision is ready.

The stage-four agenda is read straight from this register: every row still `hypothesis` is pending,
and its notes carry the working recommendation. The ratification packet that once collated pending
rows into per-venue agendas was retired on 2026-08-16 (git history); outcomes have only ever landed
here.
