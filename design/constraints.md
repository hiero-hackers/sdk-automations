# Constraints

> The rows of the register that still BIND a future change: the principles, the ratification, the
> questions nobody has answered, and the decisions whose reopening trigger stands against the code
> as it is today. If you are changing this platform, these are the rules you can break.

**What earns a row.** A decision that constrains or reverses future work, in at most a hundred
words: what was decided, the evidence, and the trigger that would reopen it. A refactor, a move or
a build earns no row — its record is its commit message and the file headers it leaves behind.

**Rows are never edited**, here or in history, so a row keeps naming the tree as it stood on the day
it was written and may cite a file that has since moved or gone. The reference checks read every
document except the register's two files; a file that moves changes nothing here. A row stops
appearing on this page when it stops binding — it is never deleted, only left in history.

**History is [`history/decisions.md`](history/decisions.md)** — every row ever written, in the order
it was written, with the status meanings, the adoption record and the ratification rule. This page
is the binding subset of it, and every identifier here resolves there.

## 1. Supported product principles

| Identifier | Principle | Evidence |
|---|---|---|
| P1 | The product is one hosted GitHub App with repository-selected capabilities. | The accepted proposal and `design/goals.md` support this direction. |
| P2 | A repository must explicitly enable every workflow-changing capability, and every user-facing capability defaults to off. | Maintainer consent and the different C++, Python, and JavaScript workflows require explicit opt-in. |
| P3 | A capability does not call or import another capability. | The coupling audit shows that direct and hidden sibling contracts are difficult to test and change. |
| P4 | A capability receives only its own configuration and a narrow platform interface. | The C++ shared-config coupling and GitHub API mock drift support a smaller boundary. |
| P5 | GitHub remains authoritative for visible repository facts. | Labels, assignees, reviews, comments, and open or closed state must continue to reflect human edits. |
| P6 | The design does not assume that GitHub alone stores every operational recovery record. | Webhook delivery, multi-call recovery, and coordination require feasibility experiments. |
| P7 | Repository labels and fields are mappings or profile defaults rather than universal platform strings. | The three audited SDKs use materially different workflow models. |
| P8 | Testing begins with a development App and personal sandbox before any Hiero repository receives writes. | The permission and failure risks require an isolated validation path. |
| P9 | A production webhook receiver durably accepts work before it acknowledges the delivery. | GitHub requires a quick response and does not automatically redeliver every failed delivery, so an acknowledged in-memory event cannot provide reliable recovery. Demonstrated in the sandbox (experiment 6.2, 2026-07-23): a crash after acknowledgement but before durable acceptance lost the delivery while GitHub's ledger recorded success; an ack-first relay in the transport path recreates the same loss window with no crash at all. |

## 2. Decisions that still bind

Copied verbatim from the register, in identifier order. The status values are history's
(`supported`, `hypothesis`, `reopened`, `ratified`); a `hypothesis` row binds as the working default
until the review its notes name answers it.

| Identifier | Decision | Current status | Required next evidence |
|---|---|---|---|
| D4 | The platform removes only named managed labels and never removes a namespace prefix. | `supported` | Adapter tests must enforce the rule for every label operation. |
| D12 | Capabilities provide structured comment content, while the platform owns managed comment identity and writes. | `supported` | The managed-comment sandbox test must prove idempotent update and authorship checks. |
| D34 | Label mappings are fully injective: `parseConfig` rejects any two meanings mapped to the same label string. `schema.md` §3 forbids "two incompatible meanings" sharing a label but never defines *incompatible*; full injectivity is the strictest defensible reading, and the observation projection (D35) depends on label→meaning being unambiguous. A same-entity-only rule (`ready` and `needsReview` sharing one "attention" label, disambiguated by entity) would be a deliberate relaxation. (`packages/core/src/config/sections.ts`, `FINDING(config-label-injectivity)`.) | `hypothesis` | The configuration review under Q14 must define "incompatible" — confirm full injectivity or ratify the same-entity relaxation. |
| D41 | Effect claims are leases, not permanent locks: `claim` atomically takes over a claim whose timestamp is at or before the caller's stale threshold. The earlier claim that journal plus read-back makes live takeover safe was falsified by the 2026-07-28 risk review: a still-live worker can resume from a pre-takeover snapshot or have a non-idempotent request in flight while its successor sees absence and resends. SQLite ownership checks cannot cancel a remote GitHub request. (`packages/store/src/store.ts`; `packages/executor/src/recovery.ts`; `FINDING(store-claim-lease)`.) | `reopened` | Stage four must choose and test an explicit overlap contract: renewal and adapter deadlines shorter than the lease, takeover delayed beyond the maximum in-flight lifetime, and an operator stop rather than resend when absence cannot be established safely. A deterministic overlapping-worker oracle is required. |
| D47 | Closure is recorded as a REASON on the work-item state, not as a position: `closedBy` is `merged`, `closedByHuman`, or `completedByLinkedMerge`, `null` while open, read from GitHub's own fields (`merged_at`, `state_reason`) and never written as a label. `taxonomy.md` §5 wrote "The pull request closes or merges" as one edge, discarding the distinction at exactly the point downstream policy needs it: progression credits a merged linked pull request and not an abandoned one (`design/guides/capabilities/advancement.md`), and the audited C++ post-merge cleanup is gated `merged == true` (`design/audit/services-cpp.md`). Modelled orthogonally for the same reason as `blocked` (D28) — as a meaning it would become mappable, and a merged pull request still carrying `status: needs review` would project as a two-position conflict, silencing observation on the items most worth reporting. (`packages/core/src/workflow/state.ts`, `packages/core/src/workflow/project.ts`, `FINDING(taxonomy-closure-reason)`.) | `hypothesis` | The D6/D8 manual-edit scenario review confirms the reason set; the first capability that consumes closure (progression or post-merge cleanup) proves the three values are sufficient and that no fourth is needed for an automation-initiated close. |
| D51 | Ordering evidence for rule 5 is three-valued, not two: `latestHumanChangeAt` is a `Date`, `null` ("the shell checked and found none"), or `"unknown"` ("ordering could not be established"), and `"unknown"` refuses with `humanOrderingUnknown`. The first implementation collapsed the last two into `null`, so unavailable evidence read as "no conflict" and the write **applied** — the exact opposite of `manual-edits.md` §2 ("if reliable ordering evidence is unavailable, the safe default is to return a conflict and do nothing"). Not hypothetical: D9 records that timestamp reliability for the newer-fact rule is still open, so unknown ordering is an expected state. (`packages/core/src/safety/write.ts`, `FINDING(safety-ordering-unknown)`.) | `supported` | The shell must distinguish the two when it computes the value; a shell that reports `null` for a failed lookup silently restores the old behaviour, so this is an executor-boundary test under D7 rather than a maintainer question. |
| D52 | The clock-triggered destructive gates are inescapable, and the kill switch is reported first on that path too. `evaluateWrite` refuses `clockTriggeredDestructive` outright (`wrongEntryPoint`); the general rules moved to an internal function that both entry points share. Found by audit, and it was the module's headline claim: `evaluateWrite` previously answered `apply` for a destructive request in an otherwise-permissive active context, so "a destructive action cannot fire without a recorded warning and an elapsed grace period" was enforced by which function the caller happened to call. Separately, the kill switch was reached only via the general rules at the END of `evaluateDestructive`, so an operator who had pulled the emergency brake was told `noWarning` — a refusal either way, but D39 freezes the verdict codes as contract and claims kill-switch-first. (`packages/core/src/safety/destructive.ts`, `FINDING(safety-destructive-entry-point)`, `FINDING(safety-killswitch-order)`.) | `supported` | None for the mechanism. The exhaustive sweep now crosses action class, capability-link state, and the remaining context dimensions (5,120 combinations), asserting `apply` per combination rather than only checking an aggregate count; a destructive request can never apply from the general path. |
| D55 | Label-mapping injectivity is judged case-insensitively and ignoring surrounding space, because that is how GitHub judges label-name uniqueness. Exact-string comparison accepted `status: ready` and `Status: Ready` as mappings for two different meanings — one label on GitHub, therefore one label carrying two meanings, which is precisely the ambiguity D34 exists to prevent and which the observation projection depends on being impossible. The configured spelling is preserved for writes; only the uniqueness key is folded. The old code deferred case to "the shell's concern", but the shell does not exist and D34's recommendation to reviewers claims full injectivity, so the implementation was weaker than the register said. (`packages/core/src/config/sections.ts`, `FINDING(config-label-case)`.) | `supported` | Rides with D34's review: confirming full injectivity now also confirms the folding rule. If reviewers ratify the same-entity relaxation instead, the folding rule survives unchanged — it constrains sameness, not scope. |
| D57 | An intent may require any permission its capability declares, repository- or organization-scoped. The check compared only against `permissions.repository`, so a grant legitimately declared under `organization` produced "which the capability does not declare" — a false failure that would have blocked any org-scoped capability, and `progression` needs organization-wide data by its own module document. (`packages/core/src/capability/declaration.ts`, `FINDING(contract-intent-org-permissions)`.) | `supported` | None; this was a defect against `contract.md` §1 as written, not a policy choice. The permission *ceiling* remains stage-four policy and is unaffected. |
| D61 | The capability vocabularies are CLOSED platform catalogues, not per-capability extensions: `packages/core/src/capability/catalogue.ts` enumerates every observation, resolver, and intent operation, and a capability chooses from them. This answers contract.md §9's first open question ("select the first concrete observation, resolver, and intent types") and it was not a preference — per-capability intent shapes are unimplementable at the far end, because the adapter would need an executor for a type it has never seen. P3 falls out as a consequence rather than a rule to police: capabilities that share no vocabulary have nothing to call each other through. (`packages/core/src/capability/catalogue.ts`.) | `hypothesis` | Stage four confirms the catalogue is the right closure point, and the first ratified capability proves the initial vocabulary is sufficient. A capability that needs an operation outside the catalogue extends the catalogue by review, which is the intended cost. |
| D62 | The platform owns an operation's idempotency class; the capability's declaration is a redundant statement that must MATCH. `capability/declaration.ts` accepted any class on any intent name, so a capability could declare `postManagedComment` as `idempotent` — and the executor trusts that class to pick its retry rule, so the declaration would route a lost response down the blind-retry path and reproduce experiment 6.5's demonstrated comment duplication, the exact failure the class exists to prevent. A per-capability field cannot be authoritative about a per-endpoint fact. `checkAgainstCatalogue` rejects the mismatch at registry build; `idempotencyOf` is what the planner reads. (`packages/core/src/capability/catalogue.ts`, `FINDING(runtime-idempotency-declared-not-checked)`.) | `supported` | None for the mechanism — the failure mode is demonstrated and the check is tested. Stage four folds `checkAgainstCatalogue` into the platform's registry build so no shipped declaration can skip it. |
| D65 | `idempotencyKey` has exactly one derivation, in `deriveIdempotencyKey`: capability, repository, item, operation, and the CAUSE with its timestamp — deliberately not the desired payload, so a capability that recomputes a slightly different comment body for the same occasion does not create a second comment. contract.md §3 required the field and never said what produced it, while the store's primary key silently depended on the answer: two intents that collide are one already-done effect and the second is never performed, and the same intent keyed differently on redelivery duplicates. Batch-level collisions are refused in the planner, which is the only place a whole batch is visible — the store cannot see them, because from its side a collision is indistinguishable from a legitimate redelivery. (`packages/core/src/capability/intent.ts`, `packages/executor/src/planner.ts`, `FINDING(runtime-idempotency-key-underived)`, `FINDING(planner-key-collision)`.) | `hypothesis` | Stage four confirms the derivation, in particular that the cause timestamp is the right occasion boundary. A capability whose cause is not event-shaped (a sweep over items with no per-item event) would need a different occasion key, and `inactivity` is the probe that will test it first. |
| D71 | A capability sees which meanings are MAPPED, never what the repository calls them. contract.md §6 says a capability "refers to internal meanings rather than repository label strings", and passing `mappings.labels` into the capability view would have satisfied every type while quietly breaking that rule. `CapabilityView` therefore reports availability only, and omits three further things a capability must not decide: the repository mode (rule 10's job), its own enabled flag (it is never evaluated while off), and any other capability's block. A capability that never sees a label string cannot hard-code one. (`packages/core/src/capability/boundary.ts`.) | `supported` | None for the projection. Whether `mappedMeanings` is enough for a capability that wants to explain itself in the repository's own words is a stage-four question — the managed comment is rendered by the platform, which does have the mapping. |
| D74 | One internal representation for an instant: epoch MILLISECONDS, converted only at the two real edges — GitHub in, SQLite out. A single logical moment currently crosses three encodings between capability and journal: `Date` through most of core (`WriteRequest.causeObservedAt`, `DatedCause.observedAt`, every observation payload), `number` inside the destructive warning (`warnedAtMs`, `earliestActionAtMs` — D60 chose primitives to avoid aliasing a mutable `Date`), and canonical ISO strings in the store, which validates the exact `Date.toISOString()` shape defensively because it cannot trust its caller. The executor takes `now: () => string` while the planner beside it takes `now: Date`. Each choice was locally right — `Date` is ergonomic, milliseconds are immutable, ISO sorts lexicographically in SQLite — and the aggregate is a conversion at every boundary plus a validator that exists only because the boundaries disagree. Milliseconds are the honest internal form: immutable like D60 requires, comparable without parsing, and needing no format check. ISO stays where it earns its keep, in the store's sort order. | `hypothesis` | Stage four accepts or rejects the single representation. If accepted, the store's timestamp validator is deleted rather than ported, and D60's aliasing rationale is satisfied by the type rather than by convention. Cost is honest: most test fixtures construct both forms today. |
| D75 | One result convention, and machine-readable codes on configuration errors. Core currently discriminates failure three ways — `ok` in `config/`, `capability/declaration.ts` and `capability/intent.ts`; `outcome` in `safety/` and the executor; `kind` in `workflow/project.ts` and the planner — and carries two incompatible error payloads: bare `readonly string[]` from `parseConfig` and `validateDeclaration`, versus `code` plus `reason` everywhere else. The discriminant spread is ordinary inconsistency; the payload split has a concrete consequence. D38's fail-closed granularity is acceptable only because of two named mitigations — the effective-configuration report and PR-time validation as a check run — and both are downstream consumers that can only ECHO prose, because configuration errors carry no code to group, count, link, or localise by, while every refusal elsewhere in core does. The mitigation that makes D38 humane is weaker than it needs to be, for a reason that is purely a type choice. Landed 2026-08-05 as `ConfigError { code, message, path }` — eleven codes over fifteen error sites, each carrying the dotted path a check run needs to annotate a line (`FINDING(config-error-codes)`, `packages/core/src/config/schema.ts`, `packages/core/src/config/sections.ts`). The discriminant half of this row — collapsing `ok`/`outcome`/`kind` into one word — was deliberately NOT done: it churns every consumer for a consistency nobody is blocked on, where the payload split had a blocked consumer in D38's configuration report. | `hypothesis` | Stage four picks one discriminant and rules that every failure carries a `code`. The prose stays prose — `safety.test.ts`'s convention of asserting `reason.length > 0` and never the wording is the right one and should extend rather than change. Mostly mechanical; no behaviour moves. |
| D76 | Every exported enumeration is declared ONCE and its union derived from it — `export type X = (typeof XS)[number]` — and a set keyed by a union is written as a mapped type, so adding a member breaks compilation until every consumer is updated. `REPOSITORY_MODES` lived in the configuration module while a hand-written `RepositoryMode` union lived in the safety module: the same four strings in two files with nothing linking them, and a `value as RepositoryMode` cast in the parser quietly covering the seam. Adding a mode to the array alone would have let `parseConfig` accept a value the safety engine's type had never heard of. `PROFILE_EDGES` had the same shape more mildly, listing `issue` and `pullRequest` literally instead of mapping over `EntityKind`. Fixing the first also removed the type-only import cycle D73 introduced, because a repository mode belongs to configuration and nothing in `config/schema.ts` now imports from `safety/write.ts`. This is the FIFTH sighting of one fact stored in two places — after D53, D62, D67 and D73 — so the repair is not another comparison but a test: `packages/dev/checks/test/enumerations.test.ts` now fails on any exported const array in `core/src` with no union derived from it, with a negative control proving the detector can fail. Found by reading, which does not scale; the next one will be found by the suite. (`packages/core/src/config/schema.ts`, `packages/core/src/safety/write.ts`, `packages/core/src/workflow/transitions.ts`, `FINDING(config-mode-union-derived)`.) | `supported` | None for the mechanism — the invariant is enforced and controlled. The pattern's recurrence is the thing worth a decision: five instances in one package suggests it should be named in `design/guides/testing.md` as a class to check for, alongside the tier definitions. |
| D77 | The shell supplies OBSERVATIONS; core computes every CONCLUSION. `WriteContext` carried five facts, two of which core already knew: `itemBlocked` duplicated what `project.ts` computes from the same observed meanings, and `installationHasPermission` was a boolean summary of a comparison core could make itself. Both are now derived — `isBlocked` is defined once in `workflow/state.ts` and used by the projection and the safety engine alike, and `missingPermissions` compares an intent's requirements against the grant the shell reports. The derivation is not only safer, it is more useful: a refusal now names the missing grant, where a boolean could only ever say that one was missing. `RepositoryConfig` also gains its own `revision` — the executor guards every in-flight effect on that string and D45 rules old-revision intents unresumable, yet the parsed configuration carried no identity, so two configurations could share a revision with nothing able to notice. `PermissionGrant` moved to `packages/core/src/github/permissions.ts`, both because `safety/` needed it without importing `capability/` and because a `scope:level` string is a GitHub fact. This is the eighth and last sighting of one fact stored twice — after D53, D62, D67, D73, D76 and the two closed here — and the first closed by REMOVING the second copy rather than comparing it. (`FINDING(config-revision-detached)`, `FINDING(safety-context-derived)`; `packages/core/src/safety/types.ts`, `packages/core/src/safety/rules.ts`, `packages/core/src/workflow/state.ts`, `packages/core/src/github/permissions.ts`, `packages/core/src/config/schema.ts`.) | `supported` | One framing question for stage four: `RepositoryConfig` now carries a field no maintainer writes, so a parsed configuration is no longer a valid document — the round-trip property test had to strip the stamp. The alternative is a wrapper (`{ revision, document }`), which is purer but adds indirection at every `config.mode` read and reintroduces two things that can drift. The stamp was chosen; the wrapper is the named alternative. |
| D78 | The workflow transition tables are RULES, and `screenIntent` enforces them. D29 already said capabilities move along documented edges while humans may land anywhere; nothing enforced the first half. The tables were exhaustively tested, checked against the design document by `doc-drift.test.ts`, and corrected three times by D48's audit — with ZERO callers. A capability could put `readyToMerge`, a pull-request meaning, on an issue, and both the intent screen and the safety engine passed it, because safety asks about permission, mode, blocking and human conflict, never about workflow legality. The missing link was one field: `Intent.cause` was `string` while `ISSUE_CAUSES` and `PR_CAUSES` had been closed, entity-scoped unions all along. Only the MOVING operation gets a typed cause — `applyMappedLabel` carries a `TransitionCause`; a comment and an unassign have reasons but not transitions, and keep the free-text dated cause that identifies the occasion. The check is self-contained in `screenIntent`: entity kind, the claimed `from` (the same `expected` safety rechecks as `preconditionHolds`), the desired `to`, and the cause. Two new screen codes: `meaningWrongEntity` and `transitionNotOnMap`. (`packages/core/src/capability/intent.ts`, `packages/core/src/capability/catalogue.ts`.) | `hypothesis` | The review should confirm the tables are rules rather than a profile — the alternative, and the one P7 and the build plan's "optional Hiero workflow profile" language point at, is that they are one repository family's documentation and belong outside `core/`. The type change immediately proved its own worth: `intake` stopped compiling until someone named its cause from the map, and the answer was `intakeObserved`. Whether every capability can express its reason in the closed set is now a compile-time question rather than one somebody has to remember to ask. |
| D79 | Only a human may pause an item: `screenIntent` refuses a capability that applies the mapped `blocked` meaning, with its own code `pauseNotCapabilityWritable`. Pausing is the ONE write whose blast radius is other capabilities — `itemBlocked` is not a rule about the item, it is a rule about every capability's access to it — so a capability able to set it would hold a veto over the others, acquired through shared state and without calling them. That is the coupling P3 forbids in its least visible form: the vetoed capability sees `itemBlocked` and has no way to learn who caused it. It is also the consistent reading of D54, which already refuses `immediatePreventive` outright pending an explanation-and-reversal gate: "detect a problem, freeze the item" is exactly that class, and letting a capability reach the same outcome by writing a label would be a hole in D54. The refusal is deliberately NOT `meaningWrongEntity` — `blocked` is legal on both entity kinds, and telling a maintainer they got the entity wrong when they actually tried to withhold an item from every capability would be a false reason; a test pins the rule order so the two cannot be swapped silently. Nothing about pausing changes, only who may do it. (`packages/core/src/capability/intent.ts`.) | `hypothesis` | Stage four confirms that capabilities never pause. The named alternative is to allow it for a capability that can show an immediate explanation and a simple reversal — which is D54's gate, so the answer is to build that gate rather than to reopen the label path. A capability that genuinely needs to stop work is the evidence that would reopen this. |
| D80 | `removeMappedLabel` is deleted from the operation catalogue, and `applyMappedLabel` means SET the position rather than add a label — the adapter removes the position label the item previously held as part of realising it, which is D4's rule that the platform removes only named managed labels. The operation had no legitimate use left once D78 and D79 landed, and no capability ever declared it. Removing a POSITION is not on the map: every `to: null` edge is a closure, and D47 makes closure a reason read from GitHub, never a label the platform writes — so leaving a position without closing is not a documented move, and a position CHANGE is an apply. Removing `blocked` is unpausing, which needs the same authority as pausing and D79 reserves to humans. Removing a label a human misapplied is exactly the automatic repair D29 and D35 refuse. An operation with no legal use is dead vocabulary in a closed catalogue (D61) — the kind that gets used later without anyone rechecking whether it was ever allowed, which is how the transition tables went unenforced for months. Deleting the name is safe in a way deleting a CAPABILITY name is not (D58): operations never appear in a repository's configuration, so no reviewed file can fail closed over this. (`packages/core/src/capability/catalogue.ts`, `packages/executor/src/planner.ts`.) | `hypothesis` | The evidence that would reopen it is a capability that must take a position OFF an item without closing it or replacing it — at which point the answer is a documented edge to `null` with a non-closure cause, not an unscreened operation. The ambiguity that made this operation look necessary was `applyMappedLabel` reading as "add a label"; that is now stated as "set the position" in the catalogue. |
| D84 | A capability must declare the mapped meanings it needs. Writing the user-facing configuration guide asked a question the platform cannot answer: which labels must a maintainer map to use a given capability? `CapabilityDeclaration` states triggers, config keys, observations, resolvers, intents, permissions and operational needs — not meanings. So the requirement is discovered at RUNTIME, when the capability finds its meaning unmapped and skips itself with an explanation, and the maintainer's experience of enabling `intake` against an unmapped repository is silence plus a buried report line. With meanings declared, `parseConfig` gains the check it obviously wants — an enabled capability whose meanings are unmapped is a configuration error with a path, caught by the PR-time check run before anything runs — and `docs/` gains a per-capability requirement table that can be locked like every other list. The gap is documented honestly in `docs/configuration.md` rather than papered over. Related: `design/contracts/config-schema.md` §227 still leaves the reviewed file's path unchosen, and a confident quickstart cannot have a blank there; `.github/hiero-automations.yml` is used throughout `docs/` as the working assumption. | `hypothesis` | Two decisions for stage four, both small and both blocking a claim the docs already make. The declaration change adds one field, one validator rule and one error code. The path needs choosing, not designing — the docs assume `.github/hiero-automations.yml` and every example will need a rename if that is wrong. |
| D85 | Tests about the REPOSITORY live in their own workspace package, `checks/`, not inside core. Four files in `packages/core/test/` read outside the package: `docs.test.ts` (reads `docs/`), `examples.test.ts` (reads `docs/examples/`), `doc-drift.test.ts` (reads `design/`), and `repo-artifacts.test.ts`, which reads the whole repository root and imported nothing from core's `src/` at all. The costs were not hypothetical: core's suite failed when a markdown file changed; the rejection fixtures scored `document.ts` at 0.00% because Stryker's sandbox is `core/` and root-level files are not in it (D82); and core source had begun to justify an export by naming `docs/`. The boundary now points one way — `checks/` depends on core through its public barrel (all nine imports were already public; none needed opening), and core does not know the repository exists. `checks/` has no `src/`, which is the point: nothing in it can or should kill a mutant, so nobody will again mistake a repository check for a package test. Verified during the move: `transitions.ts` holds 95.56% mutation without `doc-drift.test.ts` in the package, so the relocation cost core no coverage, and CI's `pnpm -r test` picks the package up with no workflow edit. The citation invariant caught all six stale paths the move created, including in its own D-rows. | `supported` | The inclusion rule for the future: a test that reads another package's files, or the repository root, goes in `checks/`; a test that can kill a mutant in `core/src` stays in `core/test`. The named risk is drift toward a junk drawer — `checks/` accepting behaviour tests because it is easy; the no-`src/` shape is the guard, and the trigger to revisit is the first test there that imports more than core's barrel. |
| D89 | Two moves from auditing the lab against the package boundary, plus the naming rule for `checks/`. (1) Webhook signature knowledge leaves the lab: `capture.ts` privately held GitHub's signing scheme — header name, `sha256=` format, HMAC over the raw body, constant-time compare — and the shell must verify the identical thing before it acks (P9), which made it the next one-fact-twice on a schedule. Now `packages/core/src/github/signatures.ts` (DOCUMENTED knowledge, so no `probedAt`, unlike its perishable neighbours), exported through the barrel, consumed by the lab, at 100% mutation. Getting there taught a testing lesson worth keeping: two mutants survived because breaking `signBody` crashed test COLLECTION — headers were built at describe-time — and vitest reports a collection crash as "no tests", which Stryker scores as survived; the fix is lazy fixture construction, so a mutant dies on an assertion. (2) `packages/dev/checks/test/` becomes one-file-per-invariant — `sources`, `mutation-coverage`, `enumerations`, `citations`, `toplevel`, shared `repository.ts` — because the original artifact-invariants file had absorbed seven unrelated describes and the next reader would have added an eighth. The naming rule going forward: a file per watched TARGET (`docs`, `examples`, `lab`) or per INVARIANT, and a target earns a subdirectory only when it needs a second file. (`packages/core/src/github/signatures.ts`, `packages/dev/checks/test/repository.ts`, `packages/dev/checks/test/citations.test.ts`.) | `supported` | The signature scheme is GitHub's published contract, so unlike `failures.ts` there is no re-probe obligation — but the shell's consumption is the real test, and if the shell needs a different shape (streaming bodies, say) the function signature is the thing to revisit, not the location. |
| D125 | Managed-comment identity is platform-owned. Core derives the marker — schema version, capability, and comment kind — and it carries the effect id, the shape protocol 6.5's harness proved — implemented at `packages/core/src/capability/managed.ts` (the harness itself is local-only by design); capabilities supply body content only. A marker counts only under App authorship, so a marker copied into a repository user's comment can never trigger an operation. `prQuality`'s config-supplied `marker` key is removed. Answers Q9. (Evidence: 6.5's duplicate-on-blind-retry in `design/findings/storage-decision.md`; `design/guides/managed-output.md` §4's metadata rules; the D12 ownership split that the capability-supplied string in `packages/core/src/capability/catalogue.ts` contradicted.) | `supported` | Stage A implements it; D12's managed-comment sandbox test — idempotent update plus the authorship check — is what ratifies it. |
| D126 | The minimum architecture is ratified, scoped to ring-zero (2026-09-02, Sophie Bulloch / exploreriii — the stage-four gate). What is ratified: the five-package layout and its cruiser-enforced layer policy; the owned single-file store, its v5 schema and DELETE+FULL crash model (per the storage decision's approving review of the same date); the safety ladder as ordered rules with the branded derived world; and the effect path's five-stage shape — identity at approval, write verbs behind a structural allowlist, journal-before-send, apply-time re-derivation, read-back reconciliation. The scope boundary is the pilot gate: enablement beyond the personal sandbox requires the volunteer repository's maintainers and the named operator (Q1/Q13), per P8. | `ratified` | Extending the scope past ring-zero is the pilot gate's question, answered by its named signers — never by this row alone. |
| D131 | The capability seeds become a package of folders and a registry the shell never edits (2026-09-09). `packages/probes` is renamed `packages/capabilities` and each seed promoted in place: `src/intake/`, `src/prQuality/` and `src/inactivity/` each hold `capability.ts` (the declaration and `evaluate`, verbatim), `settings.ts`, that capability's own spec, and the design doc it is built against, moved out of `design/guides/capabilities/` to sit beside the code it is the standard for. D93 wrote the rename's trigger down as pattern evidence rather than a first instance; the six capability designs plus the suite redesign ARE that evidence, and they redesignate the three as seeds to grow rather than stubs to delete. `src/index.ts` is now the registry: it exports `CAPABILITIES`, the shell composes that list and names no capability, and the P3 engine matrix derives its capability list and all its subsets from it. Completeness is enforced by the compiler and by the matrix rather than by three hand-kept lists — before this, the shell hand-listed the three and the matrix hand-listed them again. Measured, not asserted: a fourth folder added to the registry made the matrix run sixteen subsets instead of eight with no test edit at all. The declaration type is erased once at the registry, to the `TypedDeclaration` shape the engine already works against, so no cast reaches the shell. A pure move: no `evaluate()` changed, no assertion changed, and the strip-diff of each seed against its former single file shows only the import and export lines the split adds. DECLINED, with the triggers that reopen each: no settings toolkit, so `settings.ts` for the two seeds that declare no settings today carries its header and nothing else (R4 lands the toolkit there, and inventing a settings type now would fix a shape before a second reader exists); no design doc moved for a capability that has no folder — `advancement`, `assignment` and `notifications` stay in `design/guides/capabilities/`, whose new README states the rule that a design moves in the day its folder is created; no test-title bijection between a spec and the folder it specs (R5); and the designs stay markdown beside the code rather than becoming docstrings, because a README renders in a directory listing where a docstring does not (the `placement` skill's rule, and the reason every source directory here has one). | `supported` | The next capability is the test of the claim: it should join production and the P3 matrix by adding one folder and one line, with no edit to the shell and no edit to any test. If either needs touching, the registry is not carrying what this row says it carries. The layer policy still files the package as non-production, which was true of the stubs and is not true of the capabilities — the cruiser's `production-imports-no-checks-or-lab` rule and the architecture gate's non-production set should be revisited together, deliberately, rather than drifting apart. |
| D132 | The normalizer becomes one module per event family, and a registry instead of an if-chain (2026-09-09). `packages/core/src/engine/events.ts` answered "what does a raw delivery MEAN" in one 207-line file: a shared preamble reading the payload, then two per-event tails selected by string comparison. D89's rule fired — a target earns a subdirectory when it needs a second file — and the third family is already in flight with the commands and observation vocabulary, so the shape was built from the two families that exist rather than after the third made it urgent. `packages/core/src/engine/normalize/verdict.ts` holds the vocabulary every normalizer speaks, including the constructor for a refusal; `packages/core/src/engine/normalize/payload.ts` holds the total readers of untrusted bytes and the facts they leave behind; `packages/core/src/engine/normalize/issues.ts` and `packages/core/src/engine/normalize/pull-request.ts` hold one family each — the family's closure reader and its tail, and nothing else. Imports run one way, verdict → payload → the two families → the registry, and nothing in the subdirectory imports the file that routes to it. THE REGISTRY IS THE POINT: `NORMALIZERS` is a mapped type over the consumed-event union, so adding an event name without writing its module is a compile error at the registry rather than a delivery that quietly answers `ignored` in production — the recipe for a new family is now one module, one union member, one registry line, and the compiler checks the third against the first. The preamble is deliberately NOT duplicated into the modules: payload object, repository, item, number, labels, timestamp and meanings are read once, in the order the malformed-code tests pin, and each family receives the facts and reads only what no other family reads (`merged` on a pull request, `state` on an issue). A pure move: no behaviour, no logic and no public surface changed — every fixture test moved with its assertions byte-identical, the core slice parity test and the shell's use of the repository reader passed untouched, and the package barrel's exported name set is identical, which took an explicit re-export list rather than `export *` because the refusal constructor is now exported for the family modules and must not reach the barrel. The suites split by subject the way the source did: each family's fixture cases sit in the mirroring test directory, while the routing, the preamble's refusals and the sweep over every captured fixture stay with the registry. DECLINED, with the triggers that reopen each: no module for the comment or sweep families yet — they land with their own vocabulary waves, and a module for a family nobody delivers is a boundary guessed at; no per-module refusal codes — one closed code list stays in the verdict module (D75), reopened only if a family needs a code no other family could ever raise; no further split of the verdict and reader modules — reopen when one family needs a reader the others do not, which is the first honest signal that "what the payload says" has become two questions; and no shared helper module for the three suites' fixture helpers — they are duplicated into each file, because the repository admits shared test support at three consumers and this is two. | `supported` | The third family is the test of the recipe: if adding it costs more than one module, one union member and one registry line, the shape is wrong and this row is where that is recorded. The per-file mutation picture is the other thing to watch — the directory inherits `engine/`'s ≥90% threshold, and a family module whose branches all die in a sibling's suite is the signal the split went one file too far. |
| D133 | A write operation is three same-named files, one per layer, each registered in a mapped-type registry (2026-09-09, the R2 contract — `design/guides/write-operations.md`). Core's module carries the platform's facts and the `describeChange` wording and lives under `capability/operations/`, not the engine: `idempotencyOf` and the declaration screen read the facts below the engine, so an engine-side registry would mint the cycle the placement skill forbids; `INTENT_OPERATIONS` is derived from the registry and keeps its name and barrel place. The shell's handler owns every call verb of its operation — plan, serialize, parse, send, confirm — and the applier's four-state choreography stays generic and untouched. The adapter's transport holds the endpoint shapes and the verb builders as two spellings, D129's gate rule kept as a one-screen review. The journal row is unchanged byte for byte: `serializeCall` writes the head then the handler's fields, `parseJournaledCall` reads every row an armed sandbox has written, and literal row strings per verb are pinned by test before the move. This supersedes the registry decline in D128, whose own reopening trigger — a real fourth operation — the `assign` verb meets; D128's other declines (identity minted with the approval it serves) stand. DECLINED, with triggers: one cross-layer operation type (a module importing its same-named siblings; never); deriving the `EffectCall` and `WriteEndpoint` unions from the registries (reopen past a dozen members); a per-operation read-back split (reopen with the real unassign, which brings the third resource read). | `supported` | Three packets, core → shell → adapter, each a pure move verified by strip-diff, the full suite and the per-layer compile proof (a scratch key that fails at the registry). The FX-gate protocol 8.2 is re-run armed once, after the adapter packet and before any new verb uses the shape. |
| D134 | An operation's platform-owned half becomes one module per operation, under `capability/operations/` (2026-09-09). Before this, one operation was spread over two files in two directories: its facts in the catalogue's `INTENT_OPERATIONS` table, its change wording in an exhaustive switch in the engine, and adding an operation meant remembering both. D133's contract (`design/guides/write-operations.md` §2) makes it one module — `facts` plus a `describeChange` method — registered once in `OPERATIONS`, whose mapped type over `IntentOperation` is the completeness check: a catalogue key with no module is a compile error on the registry object and nowhere downstream, which is the acceptance proof §5 asks for and which was run. The modules live under `capability/`, not the engine, for the reason the contract gives: the facts table is consumed BELOW the engine — the idempotency lookup in the screens and the boot-admission check in the declaration validator both read it — and `engine/` imports `capability/`, never the reverse, so a registry in the engine would mint the cycle the placement skill forbids. `INTENT_OPERATIONS` is DERIVED from the registry, one `facts` per key, and keeps its name, its declared type and its place in the public barrel, so the slice parity test, the capability boundary test and the catalogue drift lock all read it unchanged. `describeChange` in the engine is now the generic recipe over the registry, with one cast correlating an intent's operation with its module, argued in place the way the engine's capability erasure is; `writeRequestFor` and `wouldApplyFinding` did not change. A pure move: no behaviour, no wordings and no public surface changed — the package barrel's exported name set is byte-identical at 208 names, every one of the three change wordings is character-for-character what it was, and the whole suite passed with zero test edits. DECLINED, with the triggers that reopen each: no per-module tests, because the facts and the wordings are already pinned by the boundary test and the slice parity test respectively, and a test per module would only restate them — reopen when a module carries logic beyond a wording; the desired-outcome shapes stay in the catalogue, because they are vocabulary a capability sees and the key list is what the registry is checked against; the registry does not enter the public barrel, because nothing outside the package needs a module — reopen if a consumer does; and `INTENT_OPERATIONS` is spelled key by key rather than walked generically, because the generic walk needs a cast and this move spends its one cast on the engine's recipe. | `supported` | The shell and adapter packets land their layers' registries on this shape, and the FX-gate protocol re-runs armed once all three are in. A fourth operation should then touch one module and one catalogue key per layer, and nothing else — that is the claim to check. |
| D135 | Each write operation becomes one handler in the shell (2026-09-09). D133's contract asked the shell layer for one module per operation and generic walks over a registry; `packages/shell/src/operations/` is that, and `effects.ts` keeps the vocabulary alone — the call union, the journal row shape, the outcome words. Three handlers now hold what two per-verb switch families held: a comment's plan, row spelling, D12 create-or-update and the read-back that recognises this effect's own comment; a label move's add-then-remove plan, its two rows and its two presence reads; and an unassign, complete and refused at the send because no confirmed endpoint does it. `HANDLERS` is the only place the operations are listed, and it is where the compiler lands: deleting a line reports `Property 'unassign' is missing in type … but required` on the registry object, and a call verb no handler owns fails at the three walks that hand a call to its handler. The journal row is untouched — `serializeCall` still writes the head then the handler's fields in insertion order — and the four literal-string pins that prove it now cover all four verbs, the add-label one captured from the implementation before anything moved. The applier's four-state choreography is not touched: `journalAndSend`, `resolveOpen`, `runFrom`, `continueOpen`, the brakes, the fresh gate and the lease read exactly as before, and its `send` and `confirm` shrink to one dispatch each. A pure move: no behaviour changed and the barrel's exported name set is identical at 51 names, with the seam vocabulary re-exported from the file that used to declare it. DECLINED, with triggers: composing the call union from the modules, because the union line is the closed vocabulary a reader wants to see whole — reopen past a dozen members; moving the comment-authorship check into the comment handler, because identity is minted at approval and checked by the applier, so it stays where the pass is, and it reaches the handler through the send context instead; splitting the suites to mirror the modules, because they reach through the walks and would have to be rewritten rather than moved — reopen when a module's own test would kill a mutant the walk's cannot. | `supported` | The next verb the vocabulary waves name lands as one file here plus one union line, with no switch to find; that is the shape this row was taken for. The row pins are the guard that it stays a move: any edit to one is a row format change and a redeliverable sandbox row that no longer parses. |
| D136 | The adapter's write endpoints become one transport per operation (2026-09-09). The gate's four path matchers and the four request builders were two files apart, each ordered by method rather than by operation; they are now `packages/adapter/src/operations/`, one module per operation of the intent catalogue, registered once in `packages/adapter/src/operations/index.ts` under `{ readonly [K in IntentOperation]: OperationTransport }` — so an operation core names and the adapter cannot reach is a compile error at the registry rather than a gap discovered at send (D133's third layer). `packages/adapter/src/operations/transport.ts` holds the contract and the vocabulary that must sit below both readers: the endpoint-name union, the two segment judgements, the shape, the result and verb-surface types, the send-and-classify seam a builder receives, and the one spelling of an item path. The D129 rule survives intact and gets cheaper: a shape matches a URL structurally and never derives its match from the builder's string, and the two spellings are now adjacent in one module, so the review that keeps them honest is one screen instead of two files. The preamble every shape shares — no query string or fragment, `repos/{owner}/{repo}/issues`, both names encoded — stays single, in the walk, and each shape judges only the method and the path's tail; a shape that re-checked the preamble could disagree with the walk. `unassign` is a registered transport with no endpoints and no verbs, which is exactly why the shell refuses that verb at send, and is where the real unassign will land. The gate keeps its origin pin, its GraphQL gate, its per-endpoint body rule and its grant precheck; the classifier keeps every word it maps. A pure move: no behaviour, no logic and no public surface changed — the barrel's exported name set is identical, and the whole suite passed with two test import lines rewritten and no assertion touched. DECLINED, with triggers: **Splitting the read-back per operation**: deferred, not refused. Two resources do not earn a split (D89); the third — an assignee read — arrives with the real unassign and is the trigger to move each operation's reader beside its transport (D133 §4). **A per-operation test split**: the client's allowlist cases and the verb rows reach through the seams the transports sit behind, so they hold every shape and every builder to account without naming a module. Reopen when a shape's own test would kill a mutant the walk's cannot. **Moving `LABEL_ABSENT` and the failure classification into the operations**: declined. One classifier answers for every verb, and the 404 disagreement is already carried as a word a builder states. Reopen if a verb needs a class of its own. | `supported` | The next write family — `assign`, the real unassign, the lock and close verbs — lands as one module plus one union member here, and the FX gate (protocol 8.2) is re-run armed once before any of them ships. Reopen the shape of the registry only if the endpoint union passes a dozen members, which is where deriving it from the transports starts to pay. |
| D138 | Both kits are built and all three seeds climb them (2026-09-09, D137's contract — `design/guides/capability-kits.md`). `packages/core/src/capability/guards.ts` is §2 whole: `Verdict`'s four words, `proceed`/`halt`/`explain`/`refuse`, `climb` (the only combinator, ending at the first rung that does not proceed and handing an `explain` to `platform.explain` there, once, under the capability's name), and `refusalOf`, which builds one `postManagedComment` claiming only `closed: false`. The fourth word is what the first attempt at this row was waiting on and it is the whole difference: `halt` is silence, `explain` is for the operator, `refuse` is for the person, and only the last writes — so a flowchart's plain "nothing" branch is now expressible without posting an operator note the seed never posted. Six of the eight guards in the three seeds are exactly that branch. The `DeclaringComment` constraint is carried by the FACTORY a declaration produces rather than by its `intents` tuple, because no tuple type says "contains this member" without fixing its position; a factory from a declaration that never listed `postManagedComment` is not assignable, so the compile error lands in the capability's own file, and a `@ts-expect-error` test pins that it does. `packages/core/src/capability/settings.ts` is §3 whole: eleven constructors, `readSettings`, `unusable`, the most-specific-first cascade, the `above` relation with `MIN_GRACE_DAYS` imported from `safety/destructive.ts` as the floor UNDER any gap a spec states, and the inferred `SettingsOf<Spec>`. The two new constructors are the group readers the fixtures demanded: `section` is a plain group with no consent of its own — every inner field read whether the group was written out or not, an absent section reading as every field at its default — and `sections` is the open-keyed mapping of them; a group that carries an `enabled` key is a `block` and the section docstring refuses the confusion, because a section would read `enabled` as an unknown key and run a group the repository turned off. Cascades and `above` resolve THROUGH a section exactly as through a block, both being levels with an `outer`. Both kit files are at 100% statements, branches, functions and lines under core's own suite. THE RETROFIT IS STRUCTURAL DIFFS ONLY: `intake` is four rungs (`notConflicted`, `stillOpen`, `meaningMapped("awaitingTriage")`, `withoutPosition` — the first and third explain with the seed's exact strings, the second and fourth halt), `prQuality` is two (`stillOpen`, then the resolver question as an async rung that explains on `!linked.ok`, halts when links exist and proceeds on a real empty answer — the answer is never carried out of the rung, so the resolver is asked once and the act needs nothing stashed), and `inactivity` is two per swept item (`hasAssignee`, `notABot` — both halting, the bot rung halting on an undetermined answer too). Every explanation string, cause, expected fact and intent order is byte-identical; all 45 tests in `packages/capabilities` pass with ZERO assertion edits, `engine-matrix.test.ts` and `boundary.test.ts` untouched, and the package measures 100/100/100/100 with forty branches where it had twenty-eight. ONE BOUNDARY CHANGE, as the contract's principal constructor requires: `CapabilityView` grows `principals: readonly string[]` — NAMES only, `Object.keys(config.principals)`, never a team string — argued in `boundary.ts` as not a fifth omission for the reason `mappedMeanings` is not one: a settings value naming a principal must be checkable against the ones the document declares, or a subscription pointing at a team nobody declared is silently ignored instead of reported; `design/contracts/contract.md` §2 was updated in the same breath. All thirteen `automations.yml` blocks of the six designs are fixtures under `packages/core/test/capability/fixtures/settings/`, extracted verbatim and travelled through `parseConfigDocument` → `RepositoryConfig` → `projectCapabilityView` → `readSettings`, and with the group readers in place every one of them is now READ rather than merely carried: `intake`'s `onOpen`, `approval` and its advisory `checklist`; `assignment`'s four skill-gate tiers inside the gate that consents to them; `advancement`'s `pillars` under each enabled role; `notifications`' `subscriptions`, each to a principal the file declares. What the fixtures found is still as valuable as what they pin: **`mappings` ships one family (`labels`)**, so seven of the thirteen are REFUSED today — `intake.2` for `skills` and `types`, `inactivity.1` for `commands`, `advancement.1` for `skills`, `assignment.1` for `commands`, `assignment.2` for `skills` and `commands`, and both `notifications` blocks for `alerts` — every one of them named as pending work in its own design's phase table, and the harness records which family refused each before reading the settings without it. DECLINED, with triggers: the contract's three unchanged (a schema library, never — a library's error shape would replace `ConfigError`'s; running the spec inside the parser, deferred to the configuration-check build; a per-rung harness, a rung is a function); the contract's own new ones, each left visibly unread rather than approximated — a reader for the other mapping families (`advancement`'s `minTier`, `notifications`' alert KEYS, `intake`'s checklist demanding a `skills` mapping) arrives with each family's vocabulary wave, a list of free display text (`advancement`'s `uncounted`) arrives with that design's own build, and a cross-field rule (`noticeIssue` required by `noticeOn: trackingIssue`) is answered by moving the DESIGN's config to a structural form, never by growing the toolkit; plus three taken here — an optional form for `oneOf`, which is what reading `advancement`'s per-pillar `outcome` would need, refused because the table is the lead's and a test may not widen it; a cascade for `count`, which is what `assignment`'s tier-level `maxOpen` override implies, refused because §3.1's cascade is a `days` relation and which cap wins is the capability's own arithmetic; and reading `roles` as `sections`, refused because every role in both fixtures states `enabled` and a section would read the pillars of a role its repository had turned off — `blocks` is the row that fits, and the contract lists `roles` under both. `prQuality` still does not call `readSettings`: an empty spec cannot fail, and the unusable branch would be dead code its package's 98% gate would score — reopen the day it declares a key. | `supported` | The sign-off is the flowchart reading: each `evaluate` now names its diamonds in order, and holding `intake`'s four rungs, `prQuality`'s two and `inactivity`'s two against the three `design.md` flowcharts is the check D137's evidence column asks for. Two things stay the lead's: when the three missing mapping families land, since four designs' config sections cannot parse without them, and whether `oneOf` grows an optional form — `advancement`'s `outcome` is the first field that wants one and the second will settle it. Stryker was not run locally, per the packet; both kits' mutation score is CI's to report, and the `capabilities` package is not yet in the matrix because its rename is uncommitted. |
| D141 | The first seed promotion: `inactivity` is aligned to its design (2026-09-09, the lead's decisions for the build; D142 records what landed). FOUR decisions the platform needed before the capability could be written. (1) The sweep observation carries what the ladders judge — a per-entry projection at sweep time, every assignee with their own clock (`assignedAt`, `lastWorkingAt`), pull-request facts (draft, changes-requested, `reapableSince`, `lastCommitAt`, linked issues), an issue's open linked pull requests, and the item's standing reminder — because an unprojected sweep refuses every intent `preconditionStale`, reminders included, so "phase 1: reminders only" was unreachable on the old shape. The open-linked-PRs fact rides on the entry rather than as the design's proposed resolver: the sweep reads the timeline anyway, and a per-item live question would cost a point per stale issue for a fact already in hand. (2) The clock-triggered release is its own operation, `releaseAssignment`, `clockTriggeredDestructive` — D63's decided split, executed now: `unassign` stays the reversible self-release assignment will use, and reaping keeps inactivity's own verb (P3). `closePullRequest` joins beside it in the same class — the catalogue's first close, phase 3 of the design, with the D61 review still owed. Both are refused `wrongEntryPoint` at the general door until the destructive door is wired to the sweep path, which is the safe reading: nothing destructive can be approved without the warning-and-grace gates, and the finding says exactly why. (3) The engine projects per item: a sweep carries many items, and a world derived from one projection for intents on different items would be wrong for all but one. (4) Cycle scoping is a comparison, not a record: a reminder older than the clock's start belongs to a previous cycle, so a re-assignment or a reopened pull request starts fresh without any durable state beyond what the sweep already reads back. DECLINED, with triggers: the sweep driver (the shell has schedules and no driver — phase 1's own item); the commands mapping family (the design's example config is refused by the parser today; D127's vocabulary wave); the adapter endpoints for release and close (the assignee write family and the pull-request write, each an armed FX-gate run); claiming native pull-request modes in `ClaimedFacts` (meanings and closure only — reopen when a second capability needs to claim a mode); `exemptBlocked: false` is unreachable in effect, because the general rules refuse every write to a blocked item regardless — the design should drop the key (a design edit for Sophie, not taken here). | `supported` | D142 is the build; the sweep driver is the next platform build and the first consumer of this entry shape. |
| D143 | Two simplifications the first promotion paid for (2026-09-09, executing `design/findings/first-promotion.md` §2.4 and §1 — a ledger retired in the 2026-09-10 teardown). **THE GUARD KIT IS ONE HELPER AND TWO RULES.** `Verdict`, `Rung`, `proceed`, `halt`, `explain`, `refuse`, `climb`, `refusalOf` and `DeclaringComment` are gone; `packages/core/src/capability/guards.ts` is `skipped(platform, capability, summary, ...detail)` — one explanation on the operator surface, an empty list back, typed `readonly never[]` so `return skipped(…)` fits any `evaluate`. What the ladder was worth was never its shape but its DISCIPLINE, and the discipline is now stated where a reviewer meets it: every stop a capability makes is silent (`return []` / `continue`), an operator note (`skipped`), or a comment to the person (an ordinary intent through the factory), and only the last writes; and the guards are written in the order the design doc's flowchart reads its diamonds, which is a REVIEW rule rather than a type. The evidence for demoting it to a review rule is §2.4: a verdict carries no subject, so every enabled block a ladder narrowed was read a second time after the climb purely so the type would narrow — three times in one file, each with the same apologetic comment — and the bot check, which judges an assignee rather than the item, ran as a one-rung ladder inside a loop because the kit had nothing smaller than `climb`. Early returns cost none of that: an early return NARROWS, so `intake` lost its `kind === "position"` re-read, `inactivity` lost both `!issues.enabled` and `facts === null` re-checks, and the per-assignee ladder became a loop with `continue`. `unusable` in `settings.ts` is unchanged in output and now speaks through `skipped` — it is the settings caption's own stop. The three capabilities' behaviour is identical: every explanation string, cause, expected fact and intent order byte-identical, and all three `capability.test.ts` files plus `engine-matrix.test.ts` passed with ZERO edits at the end of this half, which is the sign-off. **THE SWEEP IS TWO OBSERVATIONS.** `staleItemsDue` had become one entry shape for two ladders — `pullRequest: null` on every issue, an empty linked list on every pull request, and a capability re-checking nulls the type kept (§1, §2.4). `StaleCandidate` and `PullRequestFacts` are replaced by `StaleIssue` and `StalePullRequest` under `staleIssuesDue` and `stalePullRequestsDue`; each entry carries only its own kind's facts and nothing is nullable. `evaluate` branches once on `observation.kind`. `projectionFor` in the engine now finds the entry for any observation carrying `items`, so the two stale kinds share one walk and every future multi-item observation joins them by carrying a projection per entry — the trap is the shape, not the branch. ONE BEHAVIOUR CHANGE, deliberate and this row's only one: `linkedIssues` on a stale pull request carries each linked issue WITH its own assignees' clocks, so `releasesAlongside` reads them from the entry. The old rule "a linked issue this sweep did not also carry releases nobody" is gone — it was an artefact of one observation serving two ladders, and under two sweeps a pull-request sweep has no issue sweep to cross-reference. The spec's fixture for it now models the same outcome honestly, as a linked issue nobody is assigned to. DECLINED, with triggers: `proceed(value)` and `runRung` — the kit ceiling stays shut, and reopening it means reopening both together on evidence of the same kind, a capability whose guards cannot be read against its flowchart without one; a `Verdict`-returning `unusable` (§2.3) — with no `Verdict` left there is nothing to return, and inactivity's mapping demand spells `unusable`'s wording by hand in one place, which is the same cost as before and no worse; rewriting `inactivity/design.md` beyond its Declaration table — the design is the finished design and its flowchart node and phase-1 row still name `staleItemsDue`, a platform build nobody has done, so they are the sweep driver's to correct. (`packages/core/src/capability/guards.ts`, `packages/core/src/capability/catalogue.ts`, `packages/core/src/engine/decide.ts`, `design/guides/capability-kits.md`.) | `supported` | The sign-off is the flowchart reading, twice over: each `evaluate` still names its diamonds in order, and the three capabilities' suites proved part A with zero edits. Part B's test edits are all forced by the split — two sweep builders where there was one, per-kind entry builders, and three scenarios that mixed both kinds in one fixture becoming two calls. One test left the suite, `reads a pull-request entry with no pull-request facts as nothing to do`, because the type no longer admits that entry; its parked-drift entry left `verified-by.test.ts` in the same edit, which is the ratchet working. |
| D144 | The checks read the working tree as git would record it (2026-09-09; the finding in `design/findings/first-promotion.md` §2.10, a ledger retired in the 2026-09-10 teardown). `repositoryFiles` in `packages/dev/checks/test/repository.ts` lists tracked files still on disk plus untracked files the ignore rules admit — what `git add -A` would commit — and every check about the repository's CONTENT (citations, links, sources, the cruiser roots, the invariants table, the mutation matrix, provenance, the top level, the workflows, the documented commands) reads it. `trackedFiles` stays for the one check that asks about TRACKING, the never-tracked credential invariant, which must not count a file git has not been told about. The earlier corpus — the index alone — cost every build the same round: a new module was invisible to the citation checks until committed, and a tracked document naming it failed until then, so new files went unnamed in prose for one commit and CI could not reproduce what a local run saw. The concern that chose the index (untracked scratch files feeding every invariant) is answered by the ignore rules: a file they admit is a file the next `git add -A` commits, and a check that sees it first is doing its job. The bare-filename citation check also now matches the repository's actual naming — camelCase module names were invisible to it. | `supported` | A scratch file that trips a check locally is the signal to ignore or delete it, not to narrow the corpus again. |
| D145 | Managed-comment identity is per ITEM and PURPOSE, not per occasion (2026-09-10; the capability study's D1, `design/findings/capability-study.md` §1 and §5 K1, a page retired in the 2026-09-10 teardown). D125 gave the marker a digest of the effect id, and the effect id folds in the cause's `observedAt` — so a second delivery about one item minted a second identity, the read-back never found the standing comment, and a second one posted. `ManagedIdentity` is now `{ capability, item, kind, topic }`; `postManagedComment.desired` and `DestructiveGrace` each gain `topic?: string` (default `""`), the capability's discriminator for a purpose that may stand more than once on an item — an assignee's login on a per-person warning, the reason on a pull-request one. The marker's payload becomes v2, publishing `subject` (a digest of capability, item kind, item number, comment kind and topic) where v1 published `effect`; the two cannot be told apart by shape, so v1 is refused as `priorVersion` rather than translated — nothing armed has posted one outside the sandbox. The journal effect id is unchanged: redelivery of one occasion is still `already` through the journal, and a NEW occasion is a new effect whose read-back finds the standing comment and updates it in place, or answers `already` when the body has not changed. The repository is not in the identity, because a comment's location fixes it and a recovery pass has none to supply. `isMine` in the applier no longer takes the pass's effect id: it takes the call's rendered body and reads the identity out of the marker the platform put there, which is the whole of what a journal row carries — so a resend and a fresh send ask the same question. What a copied marker can and cannot do is unchanged: authorship is still answered before the bytes are read (D125). | `supported` | The reproduction is `packages/runtime/test/shell/apply.test.ts` "a second occasion of the same purpose on the same item". Reopen if a capability needs two comments of one purpose and topic on one item, or if a marker written before this change is found in a live repository. |
| D146 | D126's ratification does not describe the migrated implementation (2026-09-11). The current layout is three implementation packages: `core`, `capabilities`, and `runtime`; the owned store is schema v6. D126 remains the dated approval of the earlier five-package, schema-v5 stage and grants no approval to the migration by itself. | `supported` | Review and sandbox evidence for the migrated write path remain required before enablement beyond ring-zero. |

| D191 | One reconciliation tick owns one request cap and one write cap across every due repository (2026-09-15), revising D167 and D170. Configuration, facts, resolvers, apply-time checks, writes, retries and read-back use one budget-bound client. A repository not started when either cap is spent stays due for the next tick. `SWEEP_REQUESTS` replaces the narrower name `SWEEP_READ_REQUESTS`. The request count is an outbound bound, not a claim that GitHub's REST, GraphQL, search and secondary-limit buckets are interchangeable. | `supported` | Reopen when measured fleet use supports resource-aware scheduling. |
| D195 | The sweep reads only the fact groups an enabled schedule-triggered capability declares as needs (2026-09-15). `groupsNeeded` unions those needs and intersects them with the sweep's own `PRODUCERS` row; the driver hands the two sets to the reader, and every other group answers `unread`. The engine already tolerates this: it skips a disabled capability before checking its needs. Nothing changes today — `inactivity` is the one sweep capability and needs all four groups. | `supported` | Reopen if a sweep capability wants a group it did not declare. |
| D194 | A sweep reads its links for the whole list, before the item walk (2026-09-15). One aliased GraphQL query answers a hundred pull requests for about one point, `LinkedIssuesBatch` beside `LinkedIssues` in the admission gate with the same two read grants; a pull request advertising a second page is answered by the per-item query. The inverse an issue's `links` are built from is therefore complete whatever the request cap cut short, so a partial firing no longer answers every issue `unread`. | `supported` | Reopen if GitHub's cost formula charges the aliased query per alias. |
| D192 | The sweep spends a share of GitHub's limits, in GitHub's units (2026-09-15), revising D170 and D191. One `Allowance` replaces the four budgets, debited from the response — a 304 nothing, a GraphQL query its points, a mutation one request and one mutation (6.4 step 2; `2026-09-15T15-27-51-748Z#595,#647`). Each pool's cap is `SWEEP_SHARE` of its `x-ratelimit-limit`, over GitHub's window; `SWEEP_WRITE_CALLS` is a per-tick lane on it. The webhook lane holds the rest; a refused read retries the delivery. An unfinished firing is due at once, oldest-`started_at` first. `CONTENT_CREATION_HOURLY` bounds creation. | `supported` | Reopen if either lane starves. |
| D193 | The sweep decides an unchanged item from its last read (2026-09-15). `item_snapshot` keeps one row per open item: the list's `updated_at`, the read's instant, and its groups. A row is reused only while `updated_at` is unchanged, `REVIEW_SETTLE` (60 s) older than the list read, inside `SNAPSHOT_MAX_AGE` (24 h), and answering the groups now needed — 6.12 found every sweep input moves `updated_at`, reviews 10-30 s late (`2026-09-15T15-18-19-663Z`, `2026-09-15T15-24-19-397Z`). A reused record is observed now. `SWEEP_SHARE` falls to 0.4. | `supported` | Reopen if a sweep input is found that leaves `updated_at` alone. |

## 3. Open product and engineering questions

| Identifier | Question | Decision owner or evidence | What the answer blocks |
|---|---|---|---|
| Q1 | Which organization hosts and operates the GitHub App? | The Hiero or LFDT infrastructure owner must decide. | Production deployment is blocked. |
| Q2 | Which maintainer problems become the first user-facing capabilities? | Maintainer demand and the capability review must decide. | The user-facing minimum viable product is blocked. |
| Q3 | Do any repositories want the optional skill ladder, and what counts as a completion? | The affected repository maintainers must decide. | Skill-based assignment and progression are blocked. |
| Q4 | Which human actions may automation ever refuse or reverse? | Maintainers must review the selected capability policies. | Manual-edit enforcement is blocked. |
| Q5 | How does assignment behave with native assignment and multiple assignees? | The assignment specification and sandbox scenarios must decide. | The assignment capability is blocked. |
| Q6 | Is review queue state stored, derived, or left to native GitHub review rules? | Repositories that request review routing must decide. | Review routing is blocked. |
| Q7 | How are old labels, configuration, and writers migrated without two systems writing the same state? | A per-repository migration plan must decide. | Existing repository cutover is blocked. |
| Q8 | Which repository volunteers for a read-only or reversible pilot? | Repository maintainers and the operator must decide. | Ring-one testing is blocked. |
| Q11 | Is the App installed only on selected repositories or more broadly with repository configuration as a second gate? | Organization governance must decide. | Installation guidance is blocked. |
| Q12 | Which implementation documents should be written alongside working code? | The first implementation work packet must decide. | This question does not block feasibility work. |
| Q13 | Who owns architecture review, the development App, the platform components, testing, and rollout? | Sophie and the project maintainers must name owners. | Committed dates and production work are blocked. |
| Q14 | What path, strict YAML schema, and schema migration policy should the App use? | The path is decided: `automations.yml` at the repository root (D93, 2026-08-07) — the file configures the platform, not GitHub, so it does not live in `.github/`. Experiment 6.3 (2026-07-23) validated the mechanics (path-independent): strict parsing, unknown-key rejection, and `schemaVersion` rejection all fail closed, and a default-branch config change is event-observable within seconds. Inheritance is deferred from the first version. | Only the schema migration policy remains blocking. |
| Q17 | What deployment shape must hosting provide? Six sub-questions, absorbed 2026-08-17 from the retired architecture prose: how the webhook endpoint answers inside GitHub's time limit; whether a durable queue is required before acknowledgement; how failed and missing deliveries are found; whether one or several App processes may run at once; how primary and secondary rate-limit headers affect scheduling; and how long operational and audit records are retained. | Partly narrowed already: the storage decision (Q15) requires a persistent single-writer disk, which rules out stateless and multi-writer shapes; P9 fixes the durable-accept-before-acknowledge ordering; Q10 measured the rate budget. The retention windows were proposed (90 days, D43) but never ratified, and missing-delivery detection has no answer. The remainder needs an operator and a hosting owner (Q1, Q13). | Production deployment and the operator surface are blocked. |
