{
    "name": "@hiero-hackers/automation-probes",
    "version": "0.0.0",
    "private": true,
    "description": "DISPOSABLE. Three deliberately different capability stubs that test the runtime boundary and P3 isolation — not to ship. Deleted when stage four names the first real capability. See README.md.",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "test": "tsc --noEmit && vitest run",
        "test:coverage": "tsc --noEmit && vitest run --coverage --testTimeout=15000",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@hiero-hackers/automation-core": "workspace:*"
    },
    "devDependencies": {
        "@stryker-mutator/core": "^10.0.0",
        "@stryker-mutator/vitest-runner": "^9.6.1",
        "@types/node": "^24.0.0",
        "typescript": "^5.6.0",
        "vitest": "^4.0.0"
    }
}
