{
    "name": "@hiero-hackers/automation-testkit",
    "version": "0.0.0",
    "private": true,
    "description": "Test-only support shared by more than one package: captured webhook payloads with their provenance, and temp-dir helpers. Nothing here runs in production and nothing here can kill a mutant.",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "test": "tsc --noEmit && vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "typescript": "^5.6.0",
        "vitest": "^4.1.11"
    }
}
