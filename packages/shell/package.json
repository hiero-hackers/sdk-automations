{
    "name": "@hiero-hackers/automation-shell",
    "version": "0.0.0",
    "private": true,
    "description": "The transport shell: webhook in → verify → durably accept → 202 → mode check → persisted outcome. Owns ordering, decides nothing — see README.md and D93.",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "test": "tsc --noEmit && vitest run",
        "test:coverage": "tsc --noEmit && vitest run --coverage --testTimeout=15000",
        "test:watch": "vitest",
        "start": "tsx src/main.ts"
    },
    "dependencies": {
        "@hiero-hackers/automation-adapter": "workspace:*",
        "@hiero-hackers/automation-core": "workspace:*",
        "@hiero-hackers/automation-probes": "workspace:*",
        "@hiero-hackers/automation-store": "workspace:*"
    },
    "devDependencies": {
        "@hiero-hackers/automation-testkit": "workspace:*",
        "@stryker-mutator/core": "^10.0.0",
        "@stryker-mutator/vitest-runner": "^10.0.0",
        "@types/node": "^24.0.0",
        "tsx": "^4.23.13",
        "typescript": "^5.6.0",
        "vitest": "^4.1.11"
    }
}
