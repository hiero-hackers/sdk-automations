# A reserved active configuration.
#
# Core retains active in its configuration vocabulary, but the runnable shell
# records `modeUnsupported` and rejects this mode before deciding. It performs
# no GitHub effect.
#
# `inactivity` demonstrates the opaque settings shape while disabled. The
# shared parser confirms only that `settings` is a mapping; safe activation
# still requires that capability's own settings validator, and none ships.

schemaVersion: 1
mode: active

capabilities:
  intake:
    enabled: true
    settings:
      announce: true

  prQuality:
    enabled: true

  inactivity:
    enabled: false
    settings:
      gracePeriodDays: 21

mappings:
  labels:
    awaitingTriage: "status: triage"
    ready: "status: ready for dev"
    inProgress: "status: in progress"
    needsReview: "status: needs review"
    needsRevision: "status: needs revision"
    readyToMerge: "status: ready to merge"
    blocked: "status: blocked"

principals:
  maintainerTeam: hiero-sdk-js-maintainers
